---
archived_reason: "pointer file only — actual skill at /slava:think:innovate:index"
disable-model-invocation: true
---

# Innovation Agent

**Source of truth:** `.claude/commands/innovate/index.md`

When running as prep-spec agent, read and execute the `/innovate` skill.

This file exists so prep-spec's agent roster table remains accurate. The actual methodology lives in `/innovate` to avoid duplication.
