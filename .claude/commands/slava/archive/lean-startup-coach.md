---
archived_reason: "pointer file only — actual skill at /slava:think:lean:index"
---

# Lean Startup Coach

**Source of truth:** `.claude/commands/lean/index.md`

When running as prep-spec agent, read and execute the `/lean` skill.

This file exists so prep-spec's agent roster table remains accurate. The actual methodology lives in `/lean` to avoid duplication.
