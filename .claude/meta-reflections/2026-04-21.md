# Meta-Reflections — 2026-04-21

Session: `/fix` execution of plan "Explicit-Only Reproduce Gate in /fix + /create-bug (Candidate B)". 2 skill files edited on `main`, 1 commit (`8bbfa57a`). No worktree, no spec, no migrations.

---

## Items

**1. `/pick-flow` "trivial" path and the Phase -1 gate — friction gap is narrower than initially diagnosed**
`/pick-flow/SKILL.md:48` routes trivial bugs as `/fix → done`, creating the expectation that no `/reproduce` utterance is needed. Under the new Phase -1 gate in `/fix`, a "skip reproduce" utterance is required. Initial diagnosis: `/pick-flow` never tells the user this — friction gap.

*Opus critic correction:* The canonical path is `/pick-flow` → "trivial" → `/create-bug` → `/fix`. `create-bug.md:443-445` already adds a "say 'skip reproduce' when you run `/fix p{N}`" hint into the bug spec body when the bug is trivial. On the canonical path, the spec surfaces the required utterance before `/fix` runs. Root cause WEAKENED.

Corrected diagnosis: the gap only bites on the `/fix <architect-plan>` shortcut (no `/create-bug` step, no spec, no hint surfaced) — which is exactly P775's failure mode. The `/pick-flow` → canonical path is already handled. The residual gap: when a user runs `/fix <plan>` directly without going through `/create-bug` for a plan-classified-trivial bug, Phase -1 stops them with no contextual hint about the required phrase.
Category: process-gap
Severity: minor (narrower than stated — canonical path is covered; only the architect-plan shortcut has the cold-stop UX)

---

## Overall

Clean session. Pre-adjudicated plan executed linearly: no errors, no retries, no wrong assumptions, no scope creep, no tool fumbles. One structural edge (Item 1) was noted in the commit message itself — not discovered during execution.

---

## Session 2 — P779 /reproduce + KDD

Session: `/slava:build:reproduce` on P779 (letter-sourced /live session-end doesn't propagate to joiner). Canary written, Layer 0 root cause discovered in `live-session-banner.tsx`, spec updated, KDD committed.

---

## Items

**1. Spec hypothesis anchored investigation to the wrong file — Layer 0 was in an unmentioned file**
P779 spec named `targetListenerId`-gated branches as the locus. The canary's `waitForDBStateKey` timeout surfaced the real failure: `src/app/components/partners/live-session-banner.tsx:58-73` — the End Session button bypasses `onExit`/terminate entirely when `returnTo` is set. The spec never mentioned this file. Discovery required reading the e2e failure plus `error-context.md` yaml to locate the banner. No time was wasted chasing Layer 2, but the spec's framing caused the investigation to start one abstraction layer downstream of the actual fault.
Category: wrong-assumption
Severity: moderate

---

**2. Parallel session's bare `git commit` swept staged P779 files**
A concurrent session ran `git commit` without `-- <files>` while P779 content was staged. The P779 files landed in commit `5ea4f979` ("chore(skills): gate unnamed deferrals") — wrong attribution. This violates `.claude/rules/git.md` (explicit-path staging). Content was preserved but the commit message is permanently wrong. No history rewrite was made.
Category: process-gap
Severity: moderate

---

**3. Canary timeout diagnosed as DB-write failure — actual cause was creator-side bypass, not DB**
`waitForDBStateKey` hit 15s timeout. Initial read: the DB write didn't happen, suggesting a detection-layer gap (Layer 1/2). Actual cause: the creator never called `onExit`/terminate at all (Layer 0), so no DB write was ever attempted. The timeout was a correct symptom, but the diagnostic framing pointed at the wrong subsystem first. One round of re-investigation was needed to locate the banner file.
Category: wrong-assumption
Severity: minor (same session, corrected before any code was written)

---

**4. Context compaction mid-session required re-reading two files to resume**
Compaction fired during the session. Post-compaction recovery required re-reading the P779 spec and `e2e/p779-reproduce.spec.ts` before resuming. This is expected behavior but represents a real cost: two file reads and a state-reconciliation pass before work could continue.
Category: missing-context
Severity: minor

---

**5. Layer 2 hypothesis in spec was a false signal — disproved without needing investigation**
The spec filed a Layer 2 hypothesis (detection gap in letter-sourced flow via `targetListenerId` branches). This was disproved by the Layer 0 discovery: if the creator never fires terminate, there is nothing to detect. The spec author treated a plausible-sounding code path as a distinct hypothesis without verifying whether Layer 0 was clean first. Speculating about downstream layers before confirming the upstream layer is clean wastes spec credibility.
Category: wrong-assumption
Severity: minor

---

## Session 3 — P778 ship + KDD

Session: `/ship p778` — code review fixes committed, migration re-applied to test DB, spec archived to `features/done/2026-04-21/`, KDD updated.

---

## Items

**1. `migrate.sh` skipped already-applied migration filename — re-apply required Management API call**
The migration file had been renamed/updated but its filename matched a previously recorded entry in Supabase's migration history. `migrate.sh` skipped it on filename match. Applying the updated content required bypassing the script and calling the Management API directly. The script has no mechanism to detect content drift on an already-registered filename.
Category: missing-context
Severity: moderate

---

**2. `git mv` followed by `git commit -- <destination>` left the source deletion orphaned**
After `git mv features/p778... features/done/2026-04-21/p778...`, the commit was scoped to `-- features/done/2026-04-21/p778...` (explicit destination path only). Git committed the add half but not the deletion. The orphaned source deletion required a second cleanup commit (`chore(p778): remove original spec path after git mv to done/`). Correct form for `git mv` commits: either omit path filter entirely, or include both source and destination explicitly.
Category: tool-fumble
Severity: moderate

---

**3. `git commit` failed twice with `fatal: cannot lock ref 'HEAD': is at X but expected Y`**
The pre-commit hook was itself committing (other staged content) mid-session, causing HEAD to advance between the `git diff --cached` read and the `git commit` call. Two retries were needed. Root cause: pre-commit hooks that commit are inherently racy with agent commit flows. No fix was applied — the agent retried after re-checking HEAD.
Category: process-gap
Severity: moderate

---

**4. `.git/index.lock` existed on two occasions, requiring manual `rm`**
Consecutive occurrences in the same session suggest the pre-commit hook (or a parallel process) left locks behind after an incomplete commit attempt. Each required `rm .git/index.lock` before the next operation could proceed. No diagnosis of the lock source was performed.
Category: tool-fumble
Severity: minor

---

**5. Cherry-pick to main without `/ship` approval — ship session was a remediation path**
A prior session cherry-picked P778 content to main before `/ship` was invoked. This session was a continuation: the ship path was reduced to spec-only archiving because the code was already on main. The incident itself was captured in decisions.md, but the `/ship` skill has no guard against this state — it silently proceeds on spec-only path with no warning that code landed outside the normal flow.
Category: process-gap
Severity: significant

---

## Session 4 — P776 CORS fix + P780 spec + KDD

Session: Completed P776 (CORS class bug across 12 edge functions). Added `deno check` gate to `scripts/pre-commit-checks.sh`. Filed P780 for pre-existing deno check failures. Captured KDD.

---

## Items

**1. P780 root cause misdiagnosed as "stale generated types" — project has never had generated types**
Agent filed P780 with title "Stale Supabase TypeScript types — deno check failures" and described root cause as stale generated types. The actual failure pattern was `createClient()` called without `<Database>` generic, causing `.from('table')` to resolve to `never`. Error messages showed `Property 'x' does not exist on type 'never'` and the function code showed `createClient(url, key)` with no generic — both are direct evidence of the missing-generic pattern, not a type-generation problem. The spec had to be rewritten by the user.
Category: wrong-assumption
Severity: significant

---

**2. P776 INDEX.md entry was factually wrong — ALLOWED_ORIGIN was removed, not given a fallback**
Agent wrote the INDEX.md entry for P776 as "PROD_ORIGIN env var fallback when ALLOWED_ORIGIN absent." The actual change was that ALLOWED_ORIGIN was removed from the codebase entirely and PROD_ORIGIN is hardcoded in `supabase/functions/_shared/cors.ts`. The entry described a fallback relationship between two env vars that does not exist. Correction required during /kdd.
Category: wrong-assumption
Severity: moderate

---

**3. CORS gate logic inversion in `scripts/pre-commit-checks.sh` shipped before review caught it**
The gate as originally written checked `corsHeaders = { AND NOT buildCorsHeaders` — this exempted partial migrations where both patterns coexist, defeating the gate for the cases it was meant to catch. The error was only found during post-ship code review, not during implementation or self-review. Fix was a one-liner (remove NOT condition), but the gate was live with wrong logic between ship and review.
Category: missed-signal
Severity: moderate

---

**4. DEV_ORIGIN_RE comment in `cors.ts` was inaccurate at time of writing**
Comment said "w1..w7=5100..5700" but the regex covers 5100–5799 (intentional Vite HMR offset). Agent wrote the comment without verifying the regex upper bound. Required follow-up correction during code review.
Category: wrong-assumption
Severity: minor

---

## Session: unnamed-deferrals gate (Sonnet, 2026-04-21)

Session: Executed architect plan (`~/.claude/plans/ok-lets-create-a-distributed-cake.md`) — 4 patches to `.claude/commands/slava/build/fix.md` and `.claude/commands/slava/build/ship.md` adding spec-body grep gate for unnamed deferrals.

---

## Items

*Opus critic falsified all 5 items from initial Sonnet extraction. Corrected findings:*

**1. "Contaminated commit" — FALSIFIED.** `git show 5ea4f979 --stat` shows exactly 5 files: `fix.md`, `ship.md`, `e2e/p779-reproduce.spec.ts`, `features/p776...md` (deleted), `features/p779...md`. The initial dirty paths (`day.md`, `src/...letters-service.ts`, `src/...letter-reading-page.tsx`, `scheduled_tasks.lock`, p777 migration) are still dirty today — they were never swept into the commit. The 5 files are coherent skill patches + p779/p776 operations that were already staged. No contamination.

**2–3. Reset targeted wrong commit / cascading resets — FALSIFIED.** Both `5ea4f979` and `486c555f` are still in `git log`. The reflog shows the reset targeted `9e752f10` (a p778 KDD commit that was immediately recommitted). No other session's commits were destroyed.

**4. Plan verification post-commit — MOOT.** Premise (contamination) falsified.

**5. Concurrent sessions advanced HEAD — WEAKENED.** Reflog is linear single-actor. Concurrent session commits don't land on `main` without a merge commit, and none exists in this window. Checking HEAD before resets remains good practice, but the "8 commits from another session" narrative is not supported.

**Overall: Clean session.** The 4 patches applied correctly. The in-conversation post-mortem was a hallucinated incident — the agent constructed a contamination narrative from git noise that wasn't real.

---

## P780 session

Session: Fix deno check never-type errors in 5 edge functions. Applied `ReturnType<typeof createClient<any>>` fix, caught a pre-existing host auth security gap in `send-event-emails`, fixed a `handleUpdate` feedback rescheduling guard, deployed all 13 edge functions + 3 pending migrations, shipped to main.

---

## Items

**1. ESLint suppress comment ordering required two Python script runs — wrong position on first attempt**
First Python replacement script inserted `// eslint-disable-next-line` before `// deno-lint-ignore`, but `eslint-disable-next-line` only suppresses the immediately following line. With the deno-lint comment between the disable comment and the `type` declaration, ESLint still flagged the type line. Second script used the correct ordering (disable comment immediately before `type` line). Between runs, a linter hook stripped the misplaced comment, requiring the second invocation.
Category: wrong-assumption
Severity: moderate

---

**2. Fourth TS error category in `send-event-emails` missed during initial analysis**
Initial analysis identified three error categories resolved by `ReturnType<any>`. A fourth category — `TS2352: array cannot be directly cast to object type` on `rsvp.profiles as { email: string; name: string | null } | null` — was only discovered after running `deno check` post-fix. Both the array shape and the cast target were visible in the file; a more careful pre-fix read would have caught it. Required adding `as unknown as` double-cast.
Category: missed-signal
Severity: minor

---

**3. Cherry-pick to main blocked by two unrelated dirty-state leftovers from prior sessions**
`supabase/deploy-manifest.json` was modified by deploy scripts but never committed (prior session). A staged deletion of `features/p779_session_end_joiner_return_to_letter.md` was left by another prior session. Both required manual resolution before cherry-pick could proceed. Additionally, a stale cherry-pick state needed `git cherry-pick --quit` to clear. Three separate blockers — each from unfinished prior-session cleanup.
Category: process-gap
Severity: moderate

---

**4. Blank-line-with-space artifact left in `generate-banner/index.ts`**
The first Python script's eslint-disable insertion was subsequently stripped by the linter hook, but the surrounding whitespace wasn't fully cleaned, leaving a `0a 20 0a` (blank line with a space character) artifact visible via `xxd`. Not blocking, but a cosmetic residue of the failed first attempt that required a follow-up whitespace fix.
Category: tool-fumble
Severity: minor

---

**5. Context compaction mid-session forced re-reading 5 edge function files from worktree paths**
Compaction fired after the initial analysis phase. All 5 function files had been read in the prior context and needed re-reading before edits could resume. Standard compaction cost but noteworthy as a pattern: long multi-file sessions with compaction-midpoint are consistently more expensive than short focused passes.
Category: missing-context
Severity: minor

---

## P781 /decompose session

Session: User invoked `/dev p781` → Opus flagged self-modification risk and scope concerns → recommended `/decompose` first → Sonnet ran `/decompose` → 17 tasks produced and appended to spec → `git commit` failed with ref-lock collision from concurrent session. Plan file `~/.claude/plans/and-what-about-push-wild-pony.md` was overwritten by concurrent session mid-flight.

---

## Items

**Overall: Clean session.** All friction was external:

1. Ref-lock race (`fatal: cannot lock ref 'HEAD'`) — caused by concurrent P778 KDD session winning the lock. Not an agent error; the losing `git commit` is inherent to Git's single-lock model. Documented in decisions.md as live evidence supporting P781's design.

2. Plan file overwritten — concurrent session overwrote the P781 architect plan. The Opus agent had already read the plan; context survived. No internal process failure. Documented in decisions.md as "plan files are ephemeral."

3. Wrong files in concurrent session's commit — P778 KDD session used `git add -A`, sweeping the P781 spec into the wrong commit. This is a violation of the existing `.claude/rules/git.md` ban, not a new failure mode. Documented in decisions.md as live field evidence.

Internal process was clean: Opus correctly identified scope + self-modification risk before a single line of code was written, recommended `/decompose`, and produced a usable 17-task manifest. Pre-flight waivers (missing `## Technical Architecture`, missing spec-review READY) were correctly identified, surfaced to the user, and founder-approved. No retries, no wrong assumptions internal to the session.
