# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Feature Flags
# Allow multiple users to register with the same name
# When enabled, duplicate names will get unique URLs (e.g., john-doe-1, john-doe-2)
# Default: false (duplicate names are blocked)
# VITE_ALLOW_DUPLICATE_NAMES=true

# Sentry Build Variables (for source maps upload in CI/Vercel)
# DSN is hardcoded in main.tsx - these are only for source map uploads
# SENTRY_ORG=your-sentry-org
# SENTRY_PROJECT=clarity-pledge
# SENTRY_AUTH_TOKEN=sntrys_xxxxx
