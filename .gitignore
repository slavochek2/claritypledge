node_modules/
dist/
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env*.local*
.env*.bak*
.env.production
.env.development
.env.prod

# Personal/local files
.local/

# Feature planning work-in-progress files
features/_personal/
features/learning-plan.md

# Design inspiration and mockup screenshots
docs/inspiration/

# Playwright test artifacts
test-results/
playwright-report/

# Playwright MCP server state (auto-generated)
.playwright-mcp/
**/.playwright-mcp/


.vercel
.cloud-agent.local.md

# Temporary commit message drafts
.commit-message.txt

# MCP server config (contains secrets)
.mcp.json
.mcp.json.bak-*

# Agent orchestration state (ralph, local agent memory)
.agent/
.ralph/
PROMPT.md
ralph.yml

# Gemini CLI files
GEMINI.md
global_GEMINI.md
.gemini/

# Dead AI tool artifacts (Aider, BMAD, old IDE configs)
.aider.chat.history.md
.aider.tags.cache.v4/
.bmad/
bmad/
# .agents/ is now generated-and-tracked (P1151) — do NOT re-add as a dead artifact
.cursor/
.opencode/

# Screenshots and temp files saved to project root (from browser automation)
/*.png
/*.jpg
/*.jpeg
TEST_*.md

# Agent-generated temporary files (analysis, audits, migration outputs)
# TEMPORARY: After 1 month of zero violations (target: 2026-03-16),
# upgrade pre-commit check #13 to ERROR and remove these patterns.
/*_AUDIT*.md
/*_ANALYSIS*.md
/*_SUMMARY*.md
/*FIXES*.md
/*DUPLICATE*.md
*-results.json
*-report.json
*-audit*.json

# Personal data
private/personal/
.private/

# Sifter session files — contain private brain dumps, real names, source messages
content/sifter/sessions/

.claude/.expected-branch
.claude/.privacy-reviewed
# Note: the active stamp is now at .git/.privacy-reviewed (git-common-dir, P950) -- untracked by design
.claude/.finish-reviewed
# Note: the active stamp is now at .git/.finish-reviewed (git-common-dir, P1002) -- untracked by design

# Git worktrees — ephemeral slots with lockfiles / journals (P781)
.claude/worktrees/

# Privacy-gate override (one-shot, created by human)
.allow-pii-next-push
deno.lock

# Ad-hoc diagnostic scripts (may contain local credential paths)
tools/*
# ...but the kanban tool is a tracked, first-class app — keep its source committable
!tools/kanban/
tools/kanban/coverage/

# Throwaway event-creation input files
events/*.json

# Throwaway UAT/verify screenshots (scratch evidence, discarded after ship)
.tmp-shots/

# P955 UI-gate override sentinel — FOUNDER-created filesystem action only.
# The agent never creates this. Non-overridable when .tsx files are in the diff.
.ui-gate-override

# Video pipeline scratch artifacts (captures, voice renders) — P973
tmp/
__pycache__/
