# E2E triage — overnight run 20260831-232031

**What this is.** A per-cause triage of the 1,624 failures from the first completed
overnight full-suite run against the hosted test project. It exists so the next person
does not re-derive it. Method, coverage and the parts that are *not* covered are stated
plainly; the numbers here are reproducible from
`.private/p1043-sweep/nightly/20260831-232031/{failures,classified}.json`.

## The run is real

`verdict: complete` in `status.json` is not sufficient on its own — the runner had a known
defect where the verdict keyed only off the window cutoff. Corroborated instead from
`runner.log`:

- `batches: 38 ran (0 with global errors), 0 skipped (resume), 0 produced no usable report`
- no `Phase C classifier FAILED` line
- `tests=4141 passed=2206 failed=1624 rate_limited=34 bare_timeouts=290 clusters=318`

Reproduced independently before any triage: `npx playwright test e2e/03-login-flow.spec.ts`
→ 2 failed / 2 passed, same errors as the night. The failures are real, not a harness artifact.

## Headline

**The suite is not mostly broken. It is mostly obsolete.**

| Category | Failures | Share |
|---|---:|---:|
| Tests for **rejected or superseded** specs | 580 | 36% |
| Blocked by one undismissed first-run modal (P852) | ≤155 | ~10% |
| Bare timeouts (no assertion error) | 290 | 18% |
| Test code that errors on itself (ReferenceError/TypeError) | 22 | 1% |
| Auth rate-limiting (not defects) | 34 | 2% |
| **Genuine product defects found** | **~20** | **~1%** |

Categories overlap; they are diagnostic lenses, not a partition.

### How the 580 was measured — and how the first attempt undercounted

Every one of the 244 failing spec files was mapped to its P-number and that P-number's spec
frontmatter was read. **61 files** carry `status: rejected` (17 files / 216 failures),
`superseded_by:` (39 / 320), or are named by a *successor's* `predecessor:` field (5 / 44).

**The first pass of this sweep reported 536 and was wrong.** It checked only the failing spec's own
frontmatter — `status:` and `superseded_by:`. But a supersession can be recorded only on the
*successor* side: `p502_anon_position_optimistic_ui.md` carries `predecessor: p458`, while P458's own
spec still reads `status: all-done` with no marker at all. A check that reads only the predecessor's
frontmatter cannot see that, and silently classified 44 failures across 5 files as "live spec".
The gap was found by the p458 triage agent, not by the sweep, and the number above is the corrected
one. **Any future sweep must walk the chain from both ends.**

Six files are confirmed dead by independent reproduction plus a traced superseding commit:
p526, p523, p476, p456 (rejected/superseded) and p458, p152 (predecessor chain).

**Caveat that must travel with this number:** for the remaining files, the frontmatter is a strong
prior, not proof. A superseding spec may have kept most behaviour. Each still needs a per-file check.

## Genuine product defects (each verified in source by the orchestrator — two agent claims were corrected in the process)

1. **Nested interactive controls on the point card** — `src/app/components/social/point-card-with-links.tsx:272`
   sets `role="button" tabIndex={0}` on the card wrapper with **no** `aria-label`, while the CTA
   `<button aria-label={ctaCopy.ariaLabel}>` (line 109) is a DOM descendant. The wrapper's accessible
   name is computed from its subtree, so it inherits the inner button's label and both elements match
   the same locator. A real HTML/ARIA violation. p465's own test for exactly this
   (`e2e/a11y/p465-accessibility.spec.ts:288`) never fired, because that file navigates to the wrong route.

2. **"Add image" has nowhere to render in view mode** — `src/app/components/social/StoryCardDetail.tsx:343`
   gates the entire media block on `{(story.videoUrl || imageUrl) && …}`. A story with no image has no
   media block, so no add-image affordance can appear. The only such button lives behind `isEditMode`.
   Spec `features/done/27_mar_27/p591.md` UAT-7 and UAT-19 expect it in the plain view state.
   **This is a shipped-as-done AC that was never built.**

3. **Lightbox accessible name is silently overridden** — `src/app/components/shared/image-lightbox.tsx:42`
   sets `aria-label="Full-size image view"` on the Dialog content, but a sibling
   `DialogPrimitive.Title` holds the per-story alt text. Radix wires `aria-labelledby` to the Title,
   and `aria-labelledby` beats `aria-label`, so the intended name never applies.

4. **Feed tab bar has no keyboard navigation** — `src/app/pages/feed-page.tsx:350-379` renders
   `role="tablist"` with plain `<button role="tab" onClick>`. `grep -c "onKeyDown\|ArrowRight\|ArrowLeft"`
   over the whole file returns **0**. Spec `features/done/22_mar_26/p491_hashtag_feed.md:670` required
   "Arrow keys navigate between tabs (Left/Right)", and explicitly rejected a tabs library on the
   grounds that "the accessibility requirements … are simple enough to implement inline."
   **A second shipped-as-done AC that was never built.**

5. **The empty-creator-name guard on agreement creation has never worked** —
   `src/app/pages/create-agreement-page.tsx:308` computes
   `creatorHasNoName = creatorName !== undefined && !creatorName`, where `creatorName` comes from
   `user?.name` (line 113). But `src/app/data/api.ts` maps every profile as
   `name: dbProfile.name || 'Anonymous'` (lines 556 and 593), so `user.name` is never falsy and the
   guard's second clause is always false. A user with a blank name silently creates an agreement as
   "Anonymous" instead of being blocked.

   **The triage agent attributed this to a later commit (`40aaa82a`, p1149) as a side effect. That
   attribution is wrong, and the truth is worse.** `40aaa82a` does touch `api.ts` but adds no
   `|| 'Anonymous'` line (`git show … | grep -c "^+.*|| 'Anonymous'"` → 0). The fallback entered on
   **2025-12-01** (`9c9b6015`); the guard was written on **2026-03-02** (`3a658df0`, P466). Reading
   `api.ts` *as of the guard's own commit* finds the fallback already present, twice.
   **The guard was born dead — it has never fired once.** Its test (`agreements.spec.ts` TC-10) has
   been failing the whole time.

   **This is the third instance of the same class**, alongside P591 UAT-7/19 and P491's arrow keys:
   an acceptance criterion that shipped, was never satisfied, and had a test saying so in a suite
   nobody ran.

### Needs a product decision, not a fix

- **Accept page overrides `partner_display_name`** — `src/app/pages/accept-agreement-page.tsx:156-159`
  (P483) overwrites the pre-filled partner name with the logged-in user's own profile name whenever
  they have one, and only wires the editable input when they do not. This contradicts P466's stated
  design ("pre-fills partner name from `partner_display_name`; editable before signing"). One of the
  two is wrong and no doc reconciles them.
- **Letter-flow CTA copy** — tests expect "Submit Your Position" / "Submit My Rating" / "Next";
  the product says "Lock in your position" / "Continue" / "Next point"/"Next chapter"/"Complete Letter".
  Either the labels regressed or a later redesign renamed them and P696's spec text is stale.
- **P696's "position selector lives in a Drawer" premise** — `PositionButtons` renders inline in
  `LetterPointCard`; `FixedBottomBar` is a plain `<div>` with no dialog/drawer semantics. Either the
  migration was never finished or the design changed.
- **P683 click-wrap consent** — three tests assert a checkbox that commit `0941ec81` deliberately
  deleted. A prior triage already recorded this and declined to rewrite them, on the grounds that
  rewriting the assertions re-specifies an AC. That call still stands and still needs an owner.

## The test project's schema has drifted from prod — three independent instances

This category is the one no single triage agent could see whole, because each met only its own
symptom. **In every case the test project is behind, and prod is correct.** The E2E suite runs
against test, so these failures are environment drift, not product defects — but the drift itself
is a real operational problem, and one instance is security-shaped.

### 1. P1114 column grants on `event_room_members` — test is stuck at the base migration

Measured by read-only REST with controls that discriminate (`42703` = no such column,
`42501` = column not granted, and a control column that cannot exist returns `42703`):

| column | TEST | PROD | intended (per `20260821170000`) |
|---|---|---|---|
| `comprehension_rating` | **42501 denied** | readable | readable |
| `readiness_value` | **readable** | 42501 denied | denied |
| `client_secret` | — | 42501 denied | denied |

TEST's live grant is exactly the base `20260819161000` list. Both later migrations
(`20260821120000`, `20260821170000`) use an explicit **REVOKE-then-GRANT** idiom, so the "column
grants are additive in Postgres, therefore nothing was revoked" explanation is ruled out — had
either applied, `readiness_value` would be gone and `comprehension_rating` present. Neither grant
block took effect on test.

**`readiness_value` is anon-readable on the test project when the chain intends it revoked** — the
migration's own comment says it "joins `client_secret`" in being excluded. **Prod is not affected.**

The knock-on explains all 20 p1114 failures as one swallowed error, not seven UI bugs:
`src/app/data/event-room-service.ts:183-186` selects `comprehension_rating` by name, catches the
`42501` and returns `[]` by design ("never an error state or an empty wall"), so
`EventRoomMeet.tsx:410-418` renders "Loading who is here…" forever.

**The p1114 integration test is not stale — it is correct, and it caught this.** It is one of the
few tests in the run doing exactly the job a test exists to do.

*Open question, not resolved:* why the GRANT statements specifically failed to apply while
`ALTER TABLE`, `POLICY` and `FUNCTION` statements in the same files did. That needs direct
`information_schema` introspection, which a read-only REST probe cannot do.

### 2. P967 — a migration edited in place, so the fix never redeployed

`supabase/migrations/20260627120000_p967_listener_calibration_rpc.sql` now reads
`NULL::TEXT AS story_title` (line 37). `git log` on that file shows **two** commits: `09825301`
created it, and `6fb85e10` ("RPC uses NULL story_title — stories.title was dropped in P701")
fixed it **by editing the same file**. Migration tooling tracks applied state by version/filename,
so the corrected body was never pushed; the deployed function on test still references `s.title`
and returns `42703` once P701's drop finally landed on test (2026-08-24).

This is the P1042 anti-pattern the repo has already been bitten by: **fixing an applied migration
in place instead of adding a forward one.**

### 3. P651 — an orphaned RPC overload that was never dropped

`seal_and_send_letter` is defined by four separate `CREATE OR REPLACE FUNCTION` statements across
the chain, and `DROP FUNCTION` for it appears **nowhere**. P952 added a 4th parameter with a
`DEFAULT`, which creates a *new* signature that coexists with the 3-arg one rather than replacing
it, so a 3-arg call gets `PGRST203` ("could not choose the best candidate function"). The shipped
app always passes 4 arguments and never hits it; the backward-compatibility path the `DEFAULT` was
presumably added to support is broken. Low severity, real, and a forward migration fixes it.

## The single largest mechanical cause: the P852 tutorial modal

`src/app/components/letters/intensity-tutorial-modal.tsx` is a **hard-mandatory** first-run dialog
("ESC + backdrop blocked, no close X. Continue is the only exit"). It opens whenever the letter flow
enters `point-engage` and `localStorage` key `letter_intensity_preview_seen_at_v2` is unset
(`letter-flow-content.tsx:216-225`, `src/hooks/use-intensity-preview-seen.tsx:14`). Every fresh
Playwright context has empty localStorage, so it fires on **every test**, and as a Radix dialog it
removes the real buttons from the accessibility tree beneath it.

Measured across the suite: **no e2e spec file anywhere seeds or dismisses that key** —
`grep -rl "letter_intensity_preview_seen_at_v2" e2e/` returns nothing. 19 letter-flow spec files
that reach `point-engage` carry **155 failures** between them. That includes `p852-verify.spec.ts`,
the verification spec for the very feature that introduced the modal.

This is one shared helper away from being fixed, and it is the highest-leverage single change
available.

## The shared test database is polluted, and the suite runs against it

Measured by read-only REST count against the **test** project, with a control that returns 0:

| | count |
|---|---:|
| `profiles` total | **5,268** (1,006 named `*test*`) |
| `points` total | **3,481** |
| — of those, statement matching `*test*` | **2,107 (61%)** |
| — of those, statement matching `*E2E*` | 432 |
| `stories` total | 374 |
| control — a statement that cannot exist | 0 |

**61% of every point in the test database is leaked test fixture data.**

### Proven mechanism, in one file

`e2e/point-position-persistence.spec.ts:32` inserts its fixture point with a **hardcoded literal**
statement — `'E2E Test Point: Remote work increases productivity'` — with no uniqueness suffix,
unlike the repo's own convention elsewhere (`statement: \`P1104 marker test point A ${Date.now()}\``).
Its `afterEach` deletes the test *user* and never the point. The `first_validator_id` FK is
`ON DELETE CASCADE` (`20260204_stories_points_calibration.sql:58`), so in principle the point should
go with the user.

**In practice it does not: 110 rows carry that exact statement right now.** Declared cleanup and
actual cleanup are different things, and only the count tells you which you have.

That single fact explains the contradiction that defeated the per-file hypothesis. The same locator
shape fails at L55 with a **strict-mode violation (2+ matches)** and at L219 with **0 matches** —
irreconcilable under a "two renderers on the page" theory, but exactly what you get when the page
is rendering an arbitrary subset of 110 identically-worded rows belonging to long-deleted users.

### Why this matters beyond one file

A suite running against a database with 2,107 leftover fixture points is not measuring what it
believes it is measuring. Any assertion that locates an element **by fixture text** is exposed:
`getByText`, `filter({ hasText })`, and every `.first()` written to paper over an earlier collision.
This is a plausible contributor to the run's strict-mode violations generally and to some of the
290 unexplained bare timeouts, since heavier pages take longer to settle.

**Stated honestly:** the causal link is *proven* for the point-position file (110 identical rows,
counted). Elsewhere it is a well-supported hypothesis, not a measurement — no other file was traced
to a specific leaked row. Nobody should quote this section as explaining a failure it has not been
tested against.

**This is also a warning about the run itself.** Before the next overnight run is treated as a
clean baseline, someone has to decide whether the test project gets truncated first — and that is a
destructive operation on a shared database, so it is a founder call, not a cleanup task.

## A second copy drift with a wider blast radius than any one group saw

`create-story-page.tsx:428/433` renders "Save Private Story" / "Publish Public Story"
(visibility-conditional) — the plain "Save Story" label is gone. **Six** spec files still locate the
button with `/save story/i`, which substring-matches neither label. Four of them carry failures in
this run: `manual-points.spec.ts` (12), `p151-regression.spec.ts` (6), `story-position-isolation.spec.ts`
(4), `story-detail-page-loads.spec.ts` (4) — **26 failures**. The triage agent that found this saw
only its own two files and reported 8; the other two files belong to groups no agent owned.

The remaining two (`p427-story-edit-delete.spec.ts`, `a11y/p427-accessibility.spec.ts`) use the same
stale locator but recorded **zero** failures, which is itself unexplained — they may be skipped or
may never reach that line. Worth a look before anyone assumes the locator fix is complete.

## Test maintenance that was reported but not done

Commit `8f5165fa` ("feat(p878): relationship-scoped people picker") states in its body:
"p466/p682/p688/p728 locators updated to the new picker contract." It touched **no** p466 spec file.
Both `e2e/p466-agreement-creation.spec.ts` and `e2e/a11y/p466-accessibility.spec.ts` still use
`getByLabel(/partner.*email/i)`, which cannot match the picker's accessible name. The same commit is
the root cause of 7 more failures in `e2e/p688-add-recipient-flow.spec.ts`.

## Stale record: `supabase/migrations/.duplicate-version-allowlist`

Its "DELIBERATELY NOT ALLOWLISTED — live P1042 defects" block (dated 2026-08-24) is now false on all
three entries. Verified by read-only REST calls this morning, with controls:

| Claim in the file | Measured |
|---|---|
| `stories.title` still present on **test** | **Absent** — `42703 column stories.title does not exist` (control: `stories` 200, bogus table 404) |
| `p1114_event_room_tables` never ran on **prod** | `event_room_members` returns a row on prod |
| `p1114_event_room_rpcs` never ran on **prod** | `event_room_answers` returns `42501 permission denied` — the table exists (not-found control returns `PGRST205`) |

It also cites version `20260819160000` for the P1114 tables; the real files are
`20260819161000_p1114_event_room_tables.sql` and `20260819171000_p1114_event_room_rpcs.sql`.

This file misled the orchestrator before it was checked. It will mislead the next agent.

## Runner defects fixed in this branch

Three defects were found by an adversarial review *after* the run started and deliberately left
unfixed (editing a running bash script can corrupt the process). All three are fixed here, and each
fix was exercised on its failure path, not assumed.

1. **`verdict: "complete"` could be a lie.** `CLASSIFY_RC` and `BATCHES_FAILED_TO_START` were logged
   and never consulted; the verdict keyed only off `$CUTOFF`. Now keys off all three.
   Exercised: all five input combinations run: the known-good control still returns `complete`;
   both previously-silent paths now return `partial`.

2. **Playwright reused a foreign dev server.** `reuseExistingServer: !process.env.CI` and the runner
   never set `CI`, so Playwright reused whatever was on port 5001 and never applied its own `env:`
   block — a Phase A local unlock would never reach the browser while the report still claimed
   "Ran on a local unthrottled stack". Setting `CI=1` would have fixed the reuse but also silently
   changed workers (3→2) and retries (1→2), i.e. changed what the run measures. Instead the runner
   now **starts and owns its own dev server** with the env the run resolved, and **refuses to start**
   if the port is already served by a foreign process.
   Exercised: probe returns true on an occupied port, false on a free one; the abort branch exits 3.

3. **A dev server dying mid-night silently invalidated every later batch.** `ensure_dev_server` now
   health-checks between batches and restarts, with the restart count surfaced in the morning report.

## Coverage — what this triage does NOT cover

27 agents were spawned across three waves; **25 reported** at the time of writing. Wave 1 = the 12
largest groups by failure count. Wave 2 = the 12 largest whose spec is still live. Wave 3 = the
no-P-number files, spawned specifically to close a gap the first two waves left open.

**Two agents (p504, p458) went idle without delivering, and only reported after being chased.**
Both then produced substantive findings — p458's included the `predecessor:` gap that corrected this
document's headline number. A silent agent is indistinguishable from one that found nothing, which
is exactly why the count is stated rather than the intent.

Still outstanding (2 of 27): `nopnum-live`, `nopnum-integration`.

Not covered even when those land:
- **The long tail:** ~126 small groups, roughly 600 failures, never individually diagnosed.
- **290 bare timeouts** were not root-caused as a class. 42 are one file (p676) and are explained by
  the P852 modal; the rest are unexplained.
- **The 55 rejected/superseded files that were not independently confirmed** — frontmatter is a
  prior, not proof (see the caveat above).
- **Why the P1114 GRANT statements failed to apply on test** while sibling statements in the same
  migrations succeeded. Needs `information_schema` introspection; a read-only REST probe cannot see it.

## What this triage did NOT do, deliberately

- **No test file was edited.** Not one — including the files whose specs are rejected, the ~19 files
  blocked by a modal none of them dismisses, and `p160-private-session.spec.ts`, which asserts on an
  undeclared `body` variable and has therefore never passed since its only commit.
- **No product fix was applied.** Every defect above involves either user-facing accessible-name copy
  or a UX placement decision. Those are founder calls, and a triage pass is the wrong place to make
  them silently.
- **No migration was written or run**, and the uncommitted migration fixes in the working tree were
  left uncommitted, as instructed.

The retirement of dead tests, the shared modal helper, and the schema re-deploys are each a real
piece of work with a real owner. None of them is a test edit made to turn a run green.
