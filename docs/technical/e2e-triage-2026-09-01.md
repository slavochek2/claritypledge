# E2E triage — overnight run 20260831-232031

**What this is.** A per-cause triage of the 1,624 failures from the first completed
overnight full-suite run against the hosted test project. It exists so the next person
does not re-derive it. Method, coverage and the parts that are *not* covered are stated
plainly; the numbers here are reproducible from
`.private/p1043-sweep/nightly/20260831-232031/{failures,classified}.json`.

## The run is real

`verdict: complete` in `status.json` is not sufficient on its own — the runner had a known
defect where the verdict keyed only off the window cutoff. Corroborated instead from
`runner.log`:

- `batches: 38 ran (0 with global errors), 0 skipped (resume), 0 produced no usable report`
- no `Phase C classifier FAILED` line
- `tests=4141 passed=2206 failed=1624 rate_limited=34 bare_timeouts=290 clusters=318`

Reproduced independently before any triage: `npx playwright test e2e/03-login-flow.spec.ts`
→ 2 failed / 2 passed, same errors as the night. The failures are real, not a harness artifact.

## Headline

**The suite is not mostly broken. It is mostly obsolete.**

| Category | Failures | Share |
|---|---:|---:|
| Tests for **rejected or superseded** specs | 536 | 33% |
| Blocked by one undismissed first-run modal (P852) | ≤155 | ~10% |
| Bare timeouts (no assertion error) | 290 | 18% |
| Test code that errors on itself (ReferenceError/TypeError) | 22 | 1% |
| Auth rate-limiting (not defects) | 34 | 2% |
| **Genuine product defects found so far** | **~15** | **<1%** |

Categories overlap; they are diagnostic lenses, not a partition.

### How the 536 was measured

Every one of the 244 failing spec files was mapped to its P-number and that P-number's spec
frontmatter was read. 56 files carry `status: rejected` or `superseded_by:`. Four of those
were then independently confirmed by separate agents that reproduced the failure and traced
the superseding commit (p526, p523, p476, p456).

**Caveat that must travel with this number:** `superseded_by:` is a strong prior, not proof.
A superseding spec may have kept most behaviour. Four files are confirmed dead; the other 52
need the same per-file check before anyone deletes them.

## Genuine product defects (each verified in source by the orchestrator, not taken on an agent's word)

1. **Nested interactive controls on the point card** — `src/app/components/social/point-card-with-links.tsx:272`
   sets `role="button" tabIndex={0}` on the card wrapper with **no** `aria-label`, while the CTA
   `<button aria-label={ctaCopy.ariaLabel}>` (line 109) is a DOM descendant. The wrapper's accessible
   name is computed from its subtree, so it inherits the inner button's label and both elements match
   the same locator. A real HTML/ARIA violation. p465's own test for exactly this
   (`e2e/a11y/p465-accessibility.spec.ts:288`) never fired, because that file navigates to the wrong route.

2. **"Add image" has nowhere to render in view mode** — `src/app/components/social/StoryCardDetail.tsx:343`
   gates the entire media block on `{(story.videoUrl || imageUrl) && …}`. A story with no image has no
   media block, so no add-image affordance can appear. The only such button lives behind `isEditMode`.
   Spec `features/done/27_mar_27/p591.md` UAT-7 and UAT-19 expect it in the plain view state.
   **This is a shipped-as-done AC that was never built.**

3. **Lightbox accessible name is silently overridden** — `src/app/components/shared/image-lightbox.tsx:42`
   sets `aria-label="Full-size image view"` on the Dialog content, but a sibling
   `DialogPrimitive.Title` holds the per-story alt text. Radix wires `aria-labelledby` to the Title,
   and `aria-labelledby` beats `aria-label`, so the intended name never applies.

4. **Feed tab bar has no keyboard navigation** — `src/app/pages/feed-page.tsx:350-379` renders
   `role="tablist"` with plain `<button role="tab" onClick>`. `grep -c "onKeyDown\|ArrowRight\|ArrowLeft"`
   over the whole file returns **0**. Spec `features/done/22_mar_26/p491_hashtag_feed.md:670` required
   "Arrow keys navigate between tabs (Left/Right)", and explicitly rejected a tabs library on the
   grounds that "the accessibility requirements … are simple enough to implement inline."
   **A second shipped-as-done AC that was never built.**

### Needs a product decision, not a fix

- **Accept page overrides `partner_display_name`** — `src/app/pages/accept-agreement-page.tsx:156-159`
  (P483) overwrites the pre-filled partner name with the logged-in user's own profile name whenever
  they have one, and only wires the editable input when they do not. This contradicts P466's stated
  design ("pre-fills partner name from `partner_display_name`; editable before signing"). One of the
  two is wrong and no doc reconciles them.
- **Letter-flow CTA copy** — tests expect "Submit Your Position" / "Submit My Rating" / "Next";
  the product says "Lock in your position" / "Continue" / "Next point"/"Next chapter"/"Complete Letter".
  Either the labels regressed or a later redesign renamed them and P696's spec text is stale.
- **P696's "position selector lives in a Drawer" premise** — `PositionButtons` renders inline in
  `LetterPointCard`; `FixedBottomBar` is a plain `<div>` with no dialog/drawer semantics. Either the
  migration was never finished or the design changed.
- **P683 click-wrap consent** — three tests assert a checkbox that commit `0941ec81` deliberately
  deleted. A prior triage already recorded this and declined to rewrite them, on the grounds that
  rewriting the assertions re-specifies an AC. That call still stands and still needs an owner.

## The single largest mechanical cause: the P852 tutorial modal

`src/app/components/letters/intensity-tutorial-modal.tsx` is a **hard-mandatory** first-run dialog
("ESC + backdrop blocked, no close X. Continue is the only exit"). It opens whenever the letter flow
enters `point-engage` and `localStorage` key `letter_intensity_preview_seen_at_v2` is unset
(`letter-flow-content.tsx:216-225`, `src/hooks/use-intensity-preview-seen.tsx:14`). Every fresh
Playwright context has empty localStorage, so it fires on **every test**, and as a Radix dialog it
removes the real buttons from the accessibility tree beneath it.

Measured across the suite: **no e2e spec file anywhere seeds or dismisses that key** —
`grep -rl "letter_intensity_preview_seen_at_v2" e2e/` returns nothing. 19 letter-flow spec files
that reach `point-engage` carry **155 failures** between them. That includes `p852-verify.spec.ts`,
the verification spec for the very feature that introduced the modal.

This is one shared helper away from being fixed, and it is the highest-leverage single change
available.

## Test maintenance that was reported but not done

Commit `8f5165fa` ("feat(p878): relationship-scoped people picker") states in its body:
"p466/p682/p688/p728 locators updated to the new picker contract." It touched **no** p466 spec file.
Both `e2e/p466-agreement-creation.spec.ts` and `e2e/a11y/p466-accessibility.spec.ts` still use
`getByLabel(/partner.*email/i)`, which cannot match the picker's accessible name. The same commit is
the root cause of 7 more failures in `e2e/p688-add-recipient-flow.spec.ts`.

## Stale record: `supabase/migrations/.duplicate-version-allowlist`

Its "DELIBERATELY NOT ALLOWLISTED — live P1042 defects" block (dated 2026-08-24) is now false on all
three entries. Verified by read-only REST calls this morning, with controls:

| Claim in the file | Measured |
|---|---|
| `stories.title` still present on **test** | **Absent** — `42703 column stories.title does not exist` (control: `stories` 200, bogus table 404) |
| `p1114_event_room_tables` never ran on **prod** | `event_room_members` returns a row on prod |
| `p1114_event_room_rpcs` never ran on **prod** | `event_room_answers` returns `42501 permission denied` — the table exists (not-found control returns `PGRST205`) |

It also cites version `20260819160000` for the P1114 tables; the real files are
`20260819161000_p1114_event_room_tables.sql` and `20260819171000_p1114_event_room_rpcs.sql`.

This file misled the orchestrator before it was checked. It will mislead the next agent.

## Runner defects fixed in this branch

Three defects were found by an adversarial review *after* the run started and deliberately left
unfixed (editing a running bash script can corrupt the process). All three are fixed here, and each
fix was exercised on its failure path, not assumed.

1. **`verdict: "complete"` could be a lie.** `CLASSIFY_RC` and `BATCHES_FAILED_TO_START` were logged
   and never consulted; the verdict keyed only off `$CUTOFF`. Now keys off all three.
   Exercised: all five input combinations run: the known-good control still returns `complete`;
   both previously-silent paths now return `partial`.

2. **Playwright reused a foreign dev server.** `reuseExistingServer: !process.env.CI` and the runner
   never set `CI`, so Playwright reused whatever was on port 5001 and never applied its own `env:`
   block — a Phase A local unlock would never reach the browser while the report still claimed
   "Ran on a local unthrottled stack". Setting `CI=1` would have fixed the reuse but also silently
   changed workers (3→2) and retries (1→2), i.e. changed what the run measures. Instead the runner
   now **starts and owns its own dev server** with the env the run resolved, and **refuses to start**
   if the port is already served by a foreign process.
   Exercised: probe returns true on an occupied port, false on a free one; the abort branch exits 3.

3. **A dev server dying mid-night silently invalidated every later batch.** `ensure_dev_server` now
   health-checks between batches and restarts, with the restart count surfaced in the morning report.

## Coverage — what this triage does NOT cover

24 agents were spawned across two waves; **16 of 24 reported** at the time of writing (wave 1
complete, 4 of 12 in wave 2). Groups covered: the 12 largest by failure count, then the 12 largest
whose spec is still live. Outstanding: p504, p458, p542, p272, p651, p745, p967, p1114 — counted as
UNCOVERED until their reports land and their load-bearing claims are re-run. Two of those
(p504, p458) went idle without delivering a report; a silent agent is indistinguishable from one
that found nothing, so they are not credited as coverage.

Not covered:
- **39 spec files with no P-number in the filename, 179 failures** — `agreements.spec.ts`,
  `speak-freely-button.spec.ts`, `manual-points.spec.ts`, and 36 others. No agent was assigned to these.
- **The long tail:** ~126 small groups. Roughly 600 failures were never individually diagnosed.
- **290 bare timeouts** were not root-caused as a class. 42 of them are one file (p676) and are
  explained by the P852 modal; the rest are unexplained.
- The 52 rejected/superseded files that were *not* independently confirmed (see caveat above).
