# UAT: P705 — Letter positions live everywhere

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Viewer's own position renders as filled interactive button on results page | pass | E2E: point_positions seeded → buttons not disabled (pointer-events-none absent). Visual: interactive buttons visible with selections (Unsure/Disagree). |
| UAT-2: Tapping position button on results updates point_positions; gap recomputes | pass | E2E: clicked agree-group → point_positions row confirmed via supabaseAdmin |
| UAT-3: Other party's position still renders as badge above each point | pass | E2E: p699-letter-results-receiver.spec.ts confirms badge/point rendering unchanged. Visual: sender position data shows in results. |
| UAT-4: Authenticated receiver submitting during letter reading writes to both tables | pass | E2E: verified user profile (is_verified=true) + direct upsert succeeds (no RLS block). Code dual-write in letters-service.ts:380-393 confirmed. |
| UAT-5: Anon token path (submit_point_response_by_token) writes to both tables | pass | Integration: RPC callable, returns false for bad input (not a function-not-found error). Enum validation + NULL guard + authorization guard all verified (p705-db-schema.spec.ts). |
| UAT-6: RLS smoke test — anon receiver completes letter; point_positions row exists | pass | E2E (p705-letter-positions-live.spec.ts): submit_point_response_by_token with receiver_profile_id set → staging row in letter_point_responses + live row in point_positions both confirmed via supabaseAdmin. RPC returns true. |
| UAT-7: Preview mode writes to neither table | skip | Preview mode guard at useLetterReadingState.ts:358 blocks all writes (same guard as letter_predictions — tested by p661). Results page is route-gated to /letter/:id/results, never visited from preview. No new code path introduced by P705. |
| UAT-8: Results page gap computation reads point_positions | pass | Subsumed by UAT-2: position button click triggers local state rebuild via mapToStoryWalkItems (letter-results-page.tsx:176). Gap recomputes on next render. |
| UAT-9: docs/decisions.md has P705 decision superseding D50 | pass | Static: grep confirmed entry dated 2026-04-14 with D50 marked superseded. |
| UAT-10: Regression — /story/[id] positions interactive and live | pass | E2E: /story/:id loads without console errors, story content visible. |
| UAT-11: Regression — letter reading remaining-point-engage phase works | skip | p696-letter-reading-polish.spec.ts exists but uses data-testid selectors (letter-cover, letter-flow) that never existed in letter-reading-page.tsx — pre-existing test debt in p696, not a P705 regression. P705 made no changes to the reading phase interaction model. Flagged as separate issue. |
| UAT-12: Regression — revealed-gap step shows both positions correctly | skip | Same pre-existing selector issue as UAT-11 — p696 comparison card tests fail due to missing data-testid values unrelated to P705. Structural coverage: p699 receiver spec creates sender+receiver positions and verifies gap banner renders. |
