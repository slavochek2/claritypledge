# UAT: P852 — Letter full-flow UX redesign (Phase B)

> Auto-generated by /verify — edit freely. Scenarios derived from the spec's Done-When + Locked Decisions.

## Test Execution Log

Environment: `claritypledge.com` (production, post-deploy smoke 2026-05-31). Test letter: `442661e7-e732-4fad-81ad-9ff2a2ed1eb2` (founder's own one-to-many).

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Cover renders with #0044CC full-width CTA pill + microcopy | pass | DOM snapshot + screenshot at desktop + 390px. Avatar + pledger ring + name + meta line all present. Source: `.tmp-shots/p852-prod-cover-{desktop,390}.png` |
| UAT-2: Top-left chapter progress bar persists; label "Chapter X of N" | deferred | Interactive — needs Open the Letter click + walk through chapter. Smoke skipped per user (pragmatic smoke scope). |
| UAT-3: Anti-point engage — big-central statement, PositionButtons size=lg, "Lock in your position" CTA | deferred | Interactive. |
| UAT-4: Engage screens hide community counts (ZERO_COUNTS priming gate) | deferred | Interactive. Code-level: `letter-flow-content.tsx` passes `counts={ZERO_COUNTS}` at both call sites with inline comment (verified in code review). |
| UAT-5: Point reveal — side-by-side ordinal stances, no scale/number | deferred | Interactive. Component `LetterRevealOrdinal` shipped (verified in code review). |
| UAT-6: Story reveal — 0–10 numeric gap (two markers + highlighted segment) | deferred | Interactive. Component `LetterRevealNumeric` shipped (verified in code review). |
| UAT-7: Reveal is unmissable and outweighs story card at 375/390/desktop | partial | Cover only verified at desktop + 390px (clean). Reveal screens require interactive flow — deferred. |
| UAT-8: Anti-point-lead chapter flows end-to-end | deferred | Interactive, end-to-end. |
| UAT-9: Story-first chapter (0–1 visible points) flows correctly | deferred | Interactive, requires specific seed. |
| UAT-10: Advance CTAs name what's next | deferred | Interactive. |
| UAT-11: Story-rate question prominence upgraded; "Continue" CTA | deferred | Interactive. `ComprehensionRatingCard` extended with `ctaClassName` prop (verified in code review). |
| UAT-12: Completion — "A Moment of Intellectual Integrity" + subtext + #0044CC CTA | deferred | Interactive. `letter-completion-summary.tsx` extended (verified in code review). |
| UAT-13: No regressions — existing letter e2e specs pass | pass | Production build succeeded (2026-05-31 14:39 UTC, 58s). Build runs test suite. |
| UAT-14: Preview route /_preview/letter-redesign dev-gated | pass | `App.tsx:742` wraps the route in `{import.meta.env.DEV && ...}` — Vite tree-shakes the route + the lazy `LetterRedesignPreviewPage` import out of the production bundle. Mechanical, not behavioral. |
| UAT-15: 320px no overflow on reveal card (avatar pair + ordinal labels) | deferred | Interactive — needs to reach the reveal screen. |

### Migration-level verification (added during smoke)

| Check | Result | Notes |
|-------|--------|-------|
| Prod `get_letter_for_public_reading` RPC returns sender_avatar_url, sender_avatar_color, sender_has_pledged | pass | Anon-key curl against real letter returned the three fields populated. |
| Prod migration `20260530161011` applied + `search_path=''` + schema-qualified refs | pass | `pg_proc.proconfig` returns `{search_path=""}`; `pg_get_functiondef` contains `public.clarity_letters`. Recorded in `supabase_migrations.schema_migrations`. |
| Prod cover renders sender avatar visually (the user-facing outcome of the migration) | pass | Mobile + desktop screenshots show real Google photo with pledger ring. |

### Console issues (pre-existing, not P852-related)

- LogRocket: script could not load (network/blocker — observability degradation, not a regression).
- Mixpanel: `cdn.mxpnl.com` sourcemap blocked by CSP `connect-src` (pre-existing CSP gap for the recorder sourcemap; analytics functionality unaffected since the main mixpanel domain is allowlisted).

Neither was introduced by P852.

### Verdict

**Ship with caveats — pragmatic smoke only.** The highest-stakes change (the new `get_letter_for_public_reading` RPC + cover avatar wiring) is verified end-to-end (data layer, function settings, visual outcome). 11 of 15 UAT scenarios that require interactive flow walks are deferred to manual verification on the founder's next real reading session, per the user-chosen smoke scope. No regressions visible in the build or in cover render.
