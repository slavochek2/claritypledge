# UAT: P1149 — `/transcribe`, the live room transcription chat

> Auto-generated for the `/goal` loop's mechanical gate. Maps to the Verification Contract rows.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| DW-1: `/transcribe` reachable only when signed in; signed-out redirected | pass | `e2e/p1149-auth-gate.spec.ts` — 3/3 passing. Signed-out visitor to `/transcribe` and `/transcribe/:code` redirects to `/login?redirect=...`; signed-in user reaches the consent screen. |
| DW-2: Consent screen blocks mic access until accepted; declining leaves the room | pass | `e2e/p1149-consent-gate.spec.ts` — 3/3 passing. `getUserMedia` call count is 0 until the recording-consent toggle is set; Join button disabled until toggle is on; declining navigates off `/transcribe`. |
| DW-3: Each message shows the speaker's name and a timestamp | pass | `e2e/p1149-chat-render.spec.ts` — 1/1 passing. Seeded message renders with speaker display name and an h:mm AM/PM timestamp. |
| DW-4: Interim words never written to the server, never delivered to others | pass | `src/tests/p1149-interim-never-persists.test.ts` — 5/5 passing across DB CHECK constraint, service-layer hardcoding, and page-effect source-structure layers. |
| DW-5: A non-member cannot read a room's messages (RLS enforced) | pass | `src/tests/p1149-messages-rls.test.ts` — 5/5 passing (migration policy source-structure: no `USING (true)`, membership EXISTS check, no service-role client in the read path). Also live-verified during build: the RLS recursion bug found via `e2e/p1149-chat-render.spec.ts` proves the policy is genuinely enforced by Postgres, not merely present in the file. |
| DW-6: `rooms/` prefix used, `sessions/` untouched | pass | `src/tests/p1149-gcs-prefix.test.ts` — 6/6 passing. Pure-function path construction verified; existing `/live` `sessions/{code}/` call path verified byte-unchanged. Live physical bucket-prefix behavior is out of this repo's reach (external Cloud Function) — carved to P1152's physical check, per `assumptions.md` #1. |
| DW-7: Transcription jobs created with diarization off | pass | `src/tests/p1149-job-no-diarization.test.ts` — 4/4 passing. No diarization concept exists anywhere `endRoom` touches; `endRoom` calls the existing `createTranscriptionJob` once per member with exactly 2 args. |
| DW-8: Existing `/live` and `/chat` speech behavior unchanged | pass | `src/tests/useSpeechToText.regression.test.ts` — 4/4 passing. Default (no-options) call shape locks pre-P1149 `onend` behavior (no auto-restart); `autoRestart: true` is opt-in and additive only. |
| DW-9: Join, room, and ended states render correctly at 320px, 375px, desktop | pass | Blind-reviewer rounds 7 and 8 (`features/verification/p1149/review-round-7.md`, `review-round-8.md`), the trailing consecutive-PASS pair: both PASS, 0 blocking findings across all 3 states × 3 widths. |
| DW-10: Empty room and dropped-recognition states, all widths | pass | Same rounds 7-8 screenshot set (`room-empty-dropped-*` combines both conditions per width); both rounds confirmed clean. |
| DW-11: Hierarchy, density, sibling weight vs. reference | pass | Rounds 7-8: 0 findings against the reference artifact across hierarchy/density/sibling-weight, after 4 rounds of real, substantive fixes surfaced across the full review history (RLS bugs aside — UI fixes: banned-amber removal, listening/reconnecting/end-session visual disambiguation, ended-screen roster + button weight, headline typography). |
| DW-12: Listening indicator is the footer's most prominent element in every state that has one | pass | Rounds 7-8 confirmed: calm gray+red-dot "Listening" vs. bold bordered-red "Reconnecting" vs. solid-red "End session" action are three visually distinct treatments; the dropped/reconnecting state (the one that most needs to be unmissable per spec UX Notes) is unambiguous in every screenshot. |

## Review history (context, not a UAT scenario)

8 blind-reviewer rounds total. Rounds 1-6 (archived to `features/verification/p1149/archive/`,
preserved in full) each found and fixed a real issue: an RLS recursion bug (found via a live e2e
test, not visual review), a banned-color rule violation, ended-screen deviations from the
reference (button weight, missing roster), a missing prominent stop control, a status/action
color collision, and headline typography not actually rendering serif despite the `font-serif`
class name (a real, pre-existing, repo-wide bug — see `assumptions.md` #13). Rounds 7 and 8 —
fresh reviews against the fully-fixed build — both came back clean PASS with zero findings,
satisfying the contract's 2-consecutive-PASS requirement. Full rationale for archiving rounds
1-6 (founder-authorized, not a unilateral decision) is in `assumptions.md` #12.
