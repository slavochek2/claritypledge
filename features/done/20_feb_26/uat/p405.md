# UAT Scenarios — P405: My Sessions (Session History in Global Nav)

**Feature spec:** `features/p405_my-sessions-history.md`
**Automated tests:** see `test_files` in spec frontmatter

---

## UAT-1: Accessing My Sessions from Mobile

### UAT-1.1: Sessions tab in bottom nav
**Given:** User is logged in (verified), NOT in an active live session
**When:** User looks at the mobile bottom nav (any page)
**Then:** A "Sessions" (or "My Sessions") tab appears alongside My Events, My Profile, Clarity Session
**Verify:** Navigate to any page on a mobile viewport (390px) — count 4 tabs in the bottom nav

### UAT-1.2: Tapping Sessions tab navigates to /sessions
**Given:** Sessions tab is visible in the bottom nav
**When:** User taps the Sessions tab
**Then:** App navigates to `/sessions`
**Then:** Sessions tab shows the active-tab indicator (top bar, bold label)
**Verify:** URL bar shows `/sessions`, active tab styling applied

### UAT-1.3: Sessions list renders with past sessions
**Given:** User has at least one past session with ≥1 completed round
**When:** User opens My Sessions
**Then:** Past sessions appear in reverse chronological order
**Then:** Each row shows: partner name, date, round count badge, chevron
**Verify:** Check that the most recent session appears first

---

## UAT-2: Accessing My Sessions from Desktop

### UAT-2.1: My Sessions in desktop top nav
**Given:** User is on desktop (≥1024px), logged in, NOT in active session
**When:** User looks at the top nav
**Then:** "My Sessions" link appears in the icon-link row (alongside My Events, My Profile)
**Verify:** Link is visible without opening any dropdown

### UAT-2.2: My Sessions in avatar dropdown (desktop)
**Given:** User is on desktop, logged in
**When:** User clicks the avatar in the top-right
**Then:** Dropdown shows "My Sessions" as a menu item
**Verify:** Click "My Sessions" → navigates to `/sessions`

---

## UAT-3: Sessions List Page

### UAT-3.1: Loading state shows skeleton
**Given:** User navigates to `/sessions`
**When:** Data is still loading
**Then:** 3 skeleton placeholder rows are shown (grey bars)
**Then:** No spinner — skeleton loads feel immediate
**Verify:** Hard-refresh `/sessions` and observe the transition skeleton → list

### UAT-3.2: Session rows are date-grouped
**Given:** User has sessions across multiple days
**When:** Sessions list loads
**Then:** Sessions are grouped under date headers (e.g., "Feb 19, 2026")
**Then:** Most recent session at top
**Verify:** Check date group header text format

### UAT-3.3: Zero-round sessions are filtered out
**Given:** User has a session that ended before any round was completed (0 completed rounds)
**When:** User opens My Sessions
**Then:** That session does NOT appear in the list
**Verify:** Manually confirm the DB record exists but isn't shown

### UAT-3.4: Empty state when no completed sessions
**Given:** User is logged in but has no sessions with completed rounds
**When:** User opens `/sessions`
**Then:** Empty state shows: muted icon, "No sessions yet" heading, "Start your first session" subtext, blue CTA button
**Then:** Tapping CTA navigates to `/live`
**Verify:** Create new account, navigate to `/sessions` — empty state renders

### UAT-3.5: Error state on API failure
**Given:** Network is unavailable or API returns error
**When:** Sessions page attempts to load
**Then:** Inline error message: "Couldn't load your sessions. Check your connection and try again."
**Then:** "Try again" button retries the fetch
**Verify:** Simulate offline → refresh page

---

## UAT-4: Session Detail

### UAT-4.1: Mobile — drawer slides up on row tap
**Given:** User taps a session row on mobile
**When:** Row is tapped
**Then:** A bottom sheet drawer slides up from the bottom
**Then:** Drawer header shows: date, partner name, total rounds completed
**Then:** Below: list of rounds with story/point title + status badge (Completed / Skipped)
**Verify:** Observe animation on real device or mobile viewport (390px)

### UAT-4.2: Completed round shows rating summary
**Given:** Session has completed rounds
**When:** Session detail is open
**Then:** Each completed round shows: check icon, story/point title, rating summary ("Checker: 8, You: 7")
**Verify:** Check a session where both checker and responder submitted ratings

### UAT-4.3: Skipped round shows muted styling
**Given:** Session has skipped rounds
**When:** Session detail is open
**Then:** Skipped rounds show: muted X icon, title, "Skipped" label, reduced opacity
**Verify:** Confirm visual difference vs completed round rows

### UAT-4.4: Drawer closes on swipe down (mobile)
**Given:** Session detail drawer is open on mobile
**When:** User swipes down on the drawer
**Then:** Drawer closes
**Then:** User is back on sessions list
**Verify:** Use real device or Playwright MCP swipe simulation

### UAT-4.5: Desktop — inline expand (accordion) on row click
**Given:** User is on desktop (≥1024px) and clicks a session row
**When:** Row is clicked
**Then:** An inline section expands below the row (accordion style)
**Then:** Same round details as mobile
**Then:** Clicking the row again (or a close/collapse button) collapses it
**Verify:** Check on 1280px viewport

---

## UAT-5: Active Session — Sessions Tab Suppressed

### UAT-5.1: Sessions tab hidden during active /live session (mobile)
**Given:** User is on `/live` in an active session (waiting or live state)
**When:** User looks at the mobile bottom nav
**Then:** The Sessions tab is NOT rendered — 3-tab layout (My Events, My Profile, Clarity Session)
**Verify:** Start a session → observer bottom nav has 3 tabs

### UAT-5.2: My Sessions hidden in desktop nav during active session
**Given:** User is on `/live` in an active session (desktop)
**When:** User opens the avatar dropdown
**Then:** "My Sessions" is NOT in the dropdown menu
**Verify:** Check dropdown during active session vs outside session

### UAT-5.3: Sessions tab reappears after session ends
**Given:** Sessions tab was hidden during active session
**When:** User ends the session or leaves `/live`
**Then:** Sessions tab reappears in the bottom nav
**Verify:** End session → navigate away from `/live` → sessions tab visible again

---

## UAT-6: /live Main Screen Cleanup

### UAT-6.1: "THIS SESSION" block removed from /live idle screen
**Given:** User is on `/live` in idle state (not in active round)
**When:** User has completed rounds in the current session
**Then:** No "THIS SESSION" header and round list visible on the main screen
**Then:** Screen shows only: active story/point card (if selected) + action buttons
**Verify:** Complete a round → observe the idle screen has no history block

---

## UAT-7: Auth and Privacy

### UAT-7.1: Unauthenticated visitor redirected to /login
**Given:** User is not logged in
**When:** User visits `/sessions`
**Then:** User is redirected to `/login?redirect=/sessions`
**Then:** After login, user is returned to `/sessions`
**Verify:** Open incognito → navigate to `/sessions` → observe redirect

### UAT-7.2: Guest participants see no history
**Given:** User joined a session as a guest (no account)
**When:** (Guest has no account, so this is N/A in practice)
**Then:** No "My Sessions" tab or link is rendered for unauthenticated users
**Verify:** Open incognito → bottom nav and top nav show no Sessions link

### UAT-7.3: User only sees their own sessions
**Given:** Two users (Alice and Bob) each have their own sessions + one shared session
**When:** Alice opens My Sessions
**Then:** Alice sees: her own sessions (as creator or joiner) including the shared session with Bob
**Then:** Alice does NOT see Bob's sessions that she wasn't part of
**Verify:** Create sessions for two test accounts and compare lists

---

## UAT-8: Responsive Design

### UAT-8.1: Mobile layout (320px–767px)
**Verify:**
- Full-screen sessions list
- Fixed bottom nav with 4 tabs (h-16, safe-area inset respected)
- Session rows min-height 64px (touch comfort)
- Session detail as bottom sheet

### UAT-8.2: Desktop layout (1024px+)
**Verify:**
- Bottom nav hidden (top nav only)
- Sessions list max-w-2xl centered
- Session detail as inline accordion expand (not a modal)
- My Sessions in top nav icon links

---

## UAT-9: Accessibility Quick Checks

### UAT-9.1: Keyboard-only navigation
**Given:** User navigates with Tab/Enter only (no mouse)
**Verify:**
- Tab reaches Sessions tab in bottom nav
- Enter/Space opens a session row's detail
- Escape closes the detail
- Focus returns to the row that opened it

### UAT-9.2: Screen reader labels
**Verify:**
- VoiceOver/NVDA reads session rows as: "Session with [Partner] on [Date] — [N] rounds"
- Loading state announces "Loading sessions"
- Empty state announces the "No sessions yet" message

---

*Generated by `/generate-tests features/p405_my-sessions-history.md`*
