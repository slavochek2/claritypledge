---
status: done
type: comment
rank: 1
tags: []
completed_at: "2026-02-20"
created_date: 2026-02-20
---

# UAT Scenarios — P406: Practice Rooms — Event-Native Session Start

**Feature spec:** `features/p406_event-native-session-start.md`
**Automated tests:** see `test_files` in spec frontmatter

---

## UAT-1: Practice Rooms Section — Always Visible

### UAT-1.1: Section present on every event page
**Given:** Any event page (past, upcoming, any status)
**When:** Page finishes loading — authenticated or anonymous
**Then:** A "Practice Rooms" card appears in the sidebar (desktop: right column) or below Participants (mobile)
**Then:** The card is always visible — no host action required to show it
**Verify:** Navigate to an event you do NOT host — confirm Practice Rooms section is visible

### UAT-1.2: Section renders below Participants
**Given:** Event page in desktop layout
**When:** Page loads
**Then:** The Practice Rooms card appears directly below the Participants card in the right column
**Then:** No other card appears between Participants and Practice Rooms
**Verify:** Scroll the right sidebar and confirm section order: Organizer → Participants → Practice Rooms

### UAT-1.3: Empty state shown when no open rooms
**Given:** No participants have opened a room for this event
**When:** Event page loads
**Then:** Section shows: "No open rooms yet. Be the first!" (or equivalent)
**Then:** [+ Open a room] button is visible and enabled
**Verify:** Navigate to an event with no active rooms — empty state renders

---

## UAT-2: Opening a Room

### UAT-2.1: [+ Open a room] navigates to /live waiting screen
**Given:** Authenticated user is on an event page
**When:** User clicks [+ Open a room]
**Then:** App navigates to /live (New Session waiting screen — QR code + shareable link visible)
**Then:** URL includes `returnTo=/events/[slug]`
**Then:** "← Back to event" link is visible on the /live waiting screen
**Verify:** Click [+ Open a room] → observe URL in address bar contains `returnTo` param, QR code is shown

### UAT-2.2: QR / link fallback still works
**Given:** User has opened a room (is on /live waiting screen with returnTo param)
**When:** User shares the QR code or link with someone not on the event page
**Then:** That person can join normally by scanning the QR or clicking the link
**Then:** /live behavior is completely unchanged
**Verify:** Start a session from the event page, share QR with a second device — join works normally

### UAT-2.3: [+ Open a room] disabled when you already have a waiting room
**Given:** User already has an open (waiting) room on this event
**When:** User looks at the [+ Open a room] button
**Then:** Button is visually dimmed / disabled (not clickable)
**Then:** aria-disabled="true" is set for screen readers
**Verify:** Open a room → navigate back to event page → confirm button is disabled

### UAT-2.4: Unauthenticated visitor clicking [+ Open a room] is redirected to sign-in
**Given:** User is not logged in
**When:** User clicks [+ Open a room]
**Then:** App redirects to /login or /signup (NOT to /live)
**Verify:** Open incognito → navigate to event → click [+ Open a room] → redirected to auth

---

## UAT-3: Joining a Room

### UAT-3.1: Waiting room appears with creator name and [Join →]
**Given:** Alice has opened a room and is waiting on /live
**When:** Bob navigates to the event page
**Then:** Alice's name appears in Practice Rooms with "· waiting..." label
**Then:** A [Join →] button is visible next to Alice's name
**Verify:** Have Alice open a room, then navigate to the event as Bob — Alice's row visible

### UAT-3.2: [Join →] navigates directly to /live/[code]
**Given:** Alice's waiting room is visible to Bob
**When:** Bob clicks [Join →]
**Then:** App navigates immediately to `/live/[alice's session code]`
**Then:** Bob lands on the join name-entry form (or is admitted directly if authenticated)
**Then:** NO out-of-band link exchange was needed
**Verify:** Click [Join →] → observe URL is /live/[code], join form appears

### UAT-3.3: Both parties enter session after join
**Given:** Alice is on /live waiting screen, Bob clicks [Join →] from the event page
**When:** Bob joins the session
**Then:** Alice's /live screen auto-advances (partner joined — existing /live behavior)
**Then:** Both Alice and Bob are in an active session
**Verify:** Full two-party test — Alice opens room via event page, Bob joins via [Join →]

### UAT-3.4: Room disappears from list after both parties are in session
**Given:** Alice and Bob are both in an active session
**When:** The event page polls (within ~5s)
**Then:** Alice's room row shows "in session" indicator instead of [Join →]
**Then:** OR the row transitions to a locked/dimmed state
**Verify:** Wait 5–10s after joining — observe room row state change on another browser tab

---

## UAT-4: In-Session Room (Locked)

### UAT-4.1: Active room shows both participant names + locked indicator
**Given:** Alice + Bob are both in an active /live session started via Practice Rooms
**When:** Carol views the event page
**Then:** The room row shows both "Alice + Bob · in session" (or equivalent)
**Then:** A 🔵 dot or "In session" badge is shown — NOT a [Join →] button
**Then:** The row is not tappable / not focusable (not joinable)
**Verify:** Confirm no [Join →] button appears on an active room row

### UAT-4.2: Locked room is not focusable for keyboard users
**Given:** An active room row is shown on the event page
**When:** User navigates via keyboard (Tab key)
**Then:** The in-session row is skipped (not in tab order)
**Verify:** Tab through the event page — active room row does not receive focus

---

## UAT-5: Leaving a Room

### UAT-5.1: [Leave] removes your room immediately (optimistic update)
**Given:** You have an open (waiting) room displayed on the event page
**When:** You click [Leave]
**Then:** Your room row disappears immediately from the list (no page refresh)
**Then:** Empty state re-appears if no other rooms exist
**Then:** [+ Open a room] becomes enabled again
**Verify:** Click [Leave] → confirm row disappears instantly, no loading state delay

### UAT-5.2: [Leave] does not require confirmation
**Given:** You have an open room
**When:** You click [Leave]
**Then:** No confirmation dialog appears — room closes immediately
**Then:** Action is reversible (you can open a new room right after)
**Verify:** Click [Leave] → no dialog → immediately click [+ Open a room] again → new room opens

### UAT-5.3: Room auto-expires if creator closes /live tab without leaving
**Given:** Alice opened a room but closed the /live browser tab without clicking "End session"
**When:** 30 minutes pass
**Then:** Alice's room disappears from the event page (expires_at filter)
**Then:** The room does NOT show indefinitely as a zombie waiting room
**Verify:** (Manual only) — check expires_at is set ~30 minutes ahead when a room is created. No cron job needed.

---

## UAT-6: One Room Per Person Constraint

### UAT-6.1: DB prevents two simultaneous waiting rooms for same person+event
**Given:** Alice already has a waiting room for this event
**When:** Alice attempts to open a second room (client-side: [+ Open a room] is disabled)
**Then:** IF the client check is bypassed and a second INSERT is attempted, the DB rejects it
**Then:** Client handles the error by closing the existing room first, then retrying
**Verify:** (Developer test) — use integration test `p406-practice-rooms-migration.spec.ts` test 8 to verify the unique partial index fires

### UAT-6.2: Client closes existing room before opening new one
**Given:** Alice's existing room is waiting (button should be disabled)
**When:** Alice somehow triggers a new room (e.g., direct API call or button re-enabled)
**Then:** Existing room is closed first (status → 'closed')
**Then:** New room is created and shown
**Then:** Only one waiting room per person per event at any time
**Verify:** Check via DB admin — no two rows with same (event_id, creator_id, status='waiting')

---

## UAT-7: Polling and Real-time Updates

### UAT-7.1: Section updates without page refresh
**Given:** Bob is viewing the event page (Practice Rooms section visible)
**When:** Alice opens a room (navigates to /live from another tab)
**Then:** Within ~5 seconds, Alice's room row appears in Bob's event page
**Then:** Bob did NOT refresh the page
**Verify:** Two browser tabs — Alice opens room, Bob watches event page — row appears ~5s later

### UAT-7.2: Poll error shows empty state, keeps [+ Open a room] enabled
**Given:** Network request for practice rooms fails (e.g., offline)
**When:** Poll attempt returns an error
**Then:** Empty state is shown (graceful degradation)
**Then:** [+ Open a room] button remains enabled (not disabled due to error)
**Verify:** Use DevTools Network → block the rooms endpoint → confirm empty state + button enabled

---

## UAT-8: Accessibility

### UAT-8.1: [Join →] button has descriptive aria-label
**Given:** A waiting room row is visible with "Alice" as the creator
**When:** User inspects the [Join →] button or navigates with a screen reader
**Then:** aria-label is "Join Alice's room" (or equivalent — includes creator name)
**Verify:** Inspect element → check aria-label attribute on the [Join →] button

### UAT-8.2: Room list has aria-live="polite" for screen reader announcements
**Given:** Practice Rooms list updates (new room appears or disappears)
**When:** Screen reader is active
**Then:** The room list container has aria-live="polite" so changes are announced
**Verify:** Inspect the rooms list container element — confirm aria-live attribute

### UAT-8.3: In-session rows not in tab order
**Given:** An active (in-session) room row exists
**When:** User navigates via keyboard Tab
**Then:** The in-session row is not reachable via keyboard (aria-disabled or tabIndex=-1)
**Verify:** Tab through page — in-session row is skipped

---

## UAT-9: Mobile Layout

### UAT-9.1: Practice Rooms section stacks below Participants on mobile
**Given:** User is on a mobile viewport (390px width)
**When:** Event page loads
**Then:** Practice Rooms card stacks below the Participants card (single column)
**Then:** No horizontal overflow, full width card
**Verify:** Open event page on iPhone viewport — scroll down to see Practice Rooms below Participants

### UAT-9.2: Room row buttons meet touch target size
**Given:** Room row with [Join →] or [Leave] button visible on mobile
**When:** User inspects the button
**Then:** Touch target is at minimum 44x44px
**Verify:** DevTools device mode → inspect button height ≥ 44px

---

*Generated by `/generate-tests features/p406_event-native-session-start.md`*
