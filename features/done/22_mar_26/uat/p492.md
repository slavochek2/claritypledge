# UAT: P492 — Guest post-session UI improvements

> Auto-generated by /generate-tests — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Guest does not see "Start New Session" button | ✅ | Unit test: `queryByRole('button', /start new session/)` returns null for isGuest=true |
| UAT-2: Guest sees "Keep your session insights" CTA | ✅ | Unit test: `getByText(/keep your session insights/)` passes for isGuest=true |
| UAT-3: Guest CTA body mentions calibrated communication | ✅ | Unit test: `getByText(/you just practiced calibrated communication/)` passes |
| UAT-4: Registered user sees "Start New Session" button | ✅ | Unit test: `getByRole('button', /start new session/)` passes for isGuest=false |
| UAT-5: Registered user does not see guest CTA | ✅ | Unit test: `queryByText(/keep your session insights/)` returns null for isGuest=false |
| UAT-6: CTA spacing is tighter (mt-4 not mt-8) | ✅ | Code review: `className="mt-4 p-4..."` confirmed in source |
