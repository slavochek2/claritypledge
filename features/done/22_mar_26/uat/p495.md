# UAT: P495 — Automatic Live Session Transcription with Speaker Labels

> Auto-generated by /generate-tests — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Transcript appears after processing | ✅ | Transcript row with Copy + Open buttons visible below rounds |
| UAT-2: Copy button copies full transcript | ✅ | Code verified: Speaker [M:SS]: Text format. Clipboard API hangs in automation context (known browser limitation) but code path correct |
| UAT-3: Speaker names are correct (mapped from session metadata) | ✅ | "Vyacheslav" and "Slava 098usd09f8" match session participants |
| UAT-4: Timestamps present on each segment | ✅ | M:SS format shown (0:00, 0:09, 0:15, 0:25) |
| UAT-5: Open button shows full transcript view | ✅ | Full view with "Transcript" header, back arrow, Copy button, scrollable segments |
| UAT-6: Back button returns to session detail | ✅ | Back arrow returns to session detail with rounds + transcript row |
| UAT-7: Private session shows no transcript row | ✅ | Private session shows only rounds, no transcript UI |
| UAT-8: Processing state shows indicator | ✅ | "Transcript processing..." with spinner, no action buttons |
| UAT-9: Failed state shows Retry button | ✅ | "Transcript failed" in red with Retry button |
| UAT-10: Retry triggers reprocessing | ⏭️ | Skipped — requires RPC function deployed to edge; Retry button click verified visually |
| UAT-11: Empty transcript shows "no speech detected" | ✅ | Code verified: segments.length === 0 renders "No speech was detected in this recording." |
| UAT-12: Non-participant cannot see transcript | ⏭️ | Skipped — requires second user login; RLS policy verified in migration SQL |
| UAT-13: Escape key in transcript view returns to session detail | ✅ | Escape key correctly returns to session detail view |

## Detailed Scenarios

### UAT-1: Transcript appears after processing completes

**Given** a /live session has ended and audio was recorded (non-private)
**When** the transcription pipeline completes (status = completed)
**And** the participant navigates to /sessions and opens the session detail
**Then** a "Transcript" row appears below the round list
**And** the row shows Copy and Open buttons

**Verification:** Open /sessions, tap session card, confirm transcript row is visible with both buttons.

---

### UAT-2: Copy button copies full transcript text

**Given** a session detail with a ready transcript
**When** the participant taps the Copy button
**Then** the full transcript is copied to the clipboard in the format: `Speaker [M:SS]: Text`
**And** a "Copied to clipboard" toast appears briefly

**Verification:** Tap Copy, paste into a text editor, confirm format matches spec.

---

### UAT-3: Speaker names are correct

**Given** a transcription with speaker diarization
**When** the participant views the transcript (either in session detail or full view)
**Then** known participants are labeled by their real name (from session metadata)
**And** unknown speakers are labeled as "Speaker 3", "Speaker 4", etc.

**Verification:** Compare speaker labels in transcript against session participant names.

---

### UAT-4: Timestamps present on each segment

**Given** a ready transcript in the transcript view
**When** the participant reads through segments
**Then** each segment shows a timestamp in M:SS format next to the speaker name

**Verification:** Visual check — every segment has a time value.

---

### UAT-5: Open button shows full transcript view

**Given** a session detail with a ready transcript
**When** the participant taps Open
**Then** a full-page transcript view renders (View 4 in the stack)
**And** the header shows "Transcript" with a back arrow and Copy button
**And** all segments are visible with native page scroll

**Verification:** Tap Open, confirm full view loads with header and scrollable segments.

---

### UAT-6: Back button returns to session detail

**Given** the participant is viewing the full transcript (View 4)
**When** the participant taps the back arrow
**Then** the session detail view (View 2) is shown with the round list and transcript row

**Verification:** Tap back arrow, confirm session detail is restored.

---

### UAT-7: Private session shows no transcript row

**Given** a session was started with `?insights=off` (private mode)
**When** the participant opens that session's detail
**Then** no transcript row is shown — only rounds

**Verification:** Open a private session's detail, confirm no transcript-related UI.

---

### UAT-8: Processing state shows indicator

**Given** a transcription job is in "processing" status
**When** the participant opens the session detail
**Then** the transcript row shows "processing..." with no action buttons

**Verification:** Check session detail immediately after session ends (before transcription completes).

---

### UAT-9: Failed state shows Retry button

**Given** a transcription job has failed
**When** the participant opens the session detail
**Then** the transcript row shows "Transcript failed" with a Retry button

**Verification:** Simulate failed job (or check after a known failure), confirm Retry button visible.

---

### UAT-10: Retry triggers reprocessing

**Given** a failed transcript with Retry button visible
**When** the participant taps Retry
**Then** the button shows a spinner with "Retrying..."
**And** on success, the row transitions to processing state
**And** on failure, a toast "Couldn't retry. Please try again later." appears

**Verification:** Tap Retry, observe spinner, confirm state transition.

---

### UAT-11: Empty transcript shows appropriate message

**Given** a transcript exists but contains zero segments (audio was silence)
**When** the participant opens the full transcript view
**Then** the view shows "No speech was detected in this recording." in muted centered text

**Verification:** Open transcript view for a silence-only recording.

---

### UAT-12: Non-participant cannot see transcript

**Given** a user who is NOT a participant of the session
**When** they attempt to access the session's transcript
**Then** no transcript data is returned (RLS blocks the query)

**Verification:** Log in as a different user, attempt to view the session — confirm no transcript row.

---

### UAT-13: Escape key returns to session detail

**Given** the participant is in the full transcript view
**When** they press Escape
**Then** they are returned to the session detail view

**Verification:** Open transcript view, press Escape, confirm session detail loads.

---

## Infrastructure Checklist (post-deploy)

### Completed
- [x] Cloud Run service deployed (CPU-only, MOCK_DIARIZATION=true)
- [x] Cloud Scheduler `transcribe-poll` — every 5 min POST to /poll
- [x] Scheduler SA with `roles/run.invoker`
- [x] Prod DB: `transcription_jobs`, `session_transcripts`, `user_voice_profiles` tables + RLS + RPC
- [x] GCP Secret Manager: `supabase-service-role-key` (version 1)
- [x] GCP Secret Manager: `hf-token` (version 1)
- [x] HuggingFace token created (`pyannote-diarization`, READ)

### Waiting
- [ ] **GPU quota approved** — requested 1x Nvidia L4 in us-central1 (submitted 2026-03-15). Check: email to slava@inguro.com or `gcloud services quota list`

### When GPU quota approved
1. [ ] Accept pyannote model terms: visit https://huggingface.co/pyannote/speaker-diarization-3.1 → Accept
2. [ ] Switch Cloud Run to **test** Supabase for validation:
   ```bash
   gcloud run services update transcribe-session --region=us-central1 \
     --update-env-vars="SUPABASE_URL=https://gfjctyxqlwexxwsmkakq.supabase.co,MOCK_DIARIZATION=false"
   # Also update supabase-service-role-key secret to test key
   ```
3. [ ] Redeploy with GPU:
   ```bash
   gcloud run deploy transcribe-session --region=us-central1 \
     --image=gcr.io/gen-lang-client-0869694595/transcribe-session:latest \
     --gpu=1 --gpu-type=nvidia-l4 --no-gpu-zonal-redundancy \
     --update-env-vars="MOCK_DIARIZATION=false"
   ```
4. [ ] Test end-to-end: have a /live session → confirm audio uploaded → confirm job created → confirm transcript appears
5. [ ] Switch back to **prod** Supabase URL + service role key
6. [ ] Final prod test with real session

### Test vs Prod strategy
- Cloud Run env vars control which Supabase it talks to — no code changes needed
- Test with test DB first, switch to prod URL when shipping
- The pending test job (session `2D59JB`) can validate the pipeline
