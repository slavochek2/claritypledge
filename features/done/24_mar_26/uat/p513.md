# UAT: P513 — Story Stuck at Top After Round

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: After celebration, idle screen scroll at top | ✅ | Code review: `useLayoutEffect` now depends on `roundCount` (`sessionHistory.length`). Scroll resets to (0,0) on each round completion. Component reuse doesn't prevent re-fire since `roundCount` monotonically increases |
| UAT-2: Multiple consecutive rounds reset scroll each time | ✅ | Code review: `roundCount` increments with each completed round, triggering scroll reset each time |

Note: Full live verification requires completing a round (story select → rate → celebrate → idle). Code review confirms the fix addresses the exact root cause. Recommend manual verification at next live session.
