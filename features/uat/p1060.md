# UAT: P1060 — Events belong to an organization, and the second organization that needs it

Manual validation scenarios. Automated coverage: see `## Test Coverage Strategy` in the spec.

**The scorecard is the gate's CHECK 4.** Every row below must carry a result before
`./scripts/goal-gate.sh p1060` can exit 0. A skip must name a reason from the whitelist
(`NOT-BUILT`, `ENV-UNAVAILABLE`, `HUMAN-ONLY`, `SUPERSEDED`) — an unexplained skip is decay, not a
result. This table is unmarked at pin time on purpose: it was added because the file as generated
carried no result column at all, so CHECK 4 passed over zero rows. Verified red on 2026-08-28 before
the loop started.

## Test Execution Log

| Scenario | Result | Notes |
|---|---|---|
| UAT-1.1 `events.org_id` exists, nullable, FK to `organization`, indexed | ✅ | integration: `events.org_id column exists` + `accepts NULL` + `rejects a non-existent organization (FK enforced)` + `accepts a real organization id`. 6 passed on the test DB |
| UAT-1.2 backfill on prod: the 8 named Chiang Mai slugs carry `org_id=cm`, the 2 Ko Phangan slugs stay NULL, migration comment states it touched exactly 8 | ⏭ ENV-UNAVAILABLE | the test project carries none of the 10 named prod slugs, so the integration row skips with its own console warning rather than passing. **Unverified — re-query prod post-deploy and diff against the spec's two tables.** The migration itself refuses a partial write (touched ≠ present → EXCEPTION) and asserts the 2 Ko Phangan rows stay NULL |
| UAT-2.1 `/org/online` loads, shows the · Online name, renders no blurb line and no placeholder (D7) | ✅ | integration: seed row is `visibility: public`, `has_events: true`, `blurb: null`. Confirmed independently by a read-only REST query against test. Render E photographs the page |
| UAT-2.2 · Online has an organizer membership row | ✅ | integration: at least one `role = organizer` membership row for the `online` org |
| UAT-3.1 each org's Events tab lists only its own events, both directions | ✅ | e2e: `org Events tab lists only its own events (cross-org isolation)` — both tabs, both directions |
| UAT-3.2 standalone `/events` is unaffected and still lists both orgs' events | ✅ | e2e: `standalone /events list is unaffected` — both orgs' events visible |
| UAT-4.1 an organizer sees Host Event + Co-create on their org's page | ✅ | e2e: `D4 (1/3)` |
| UAT-4.2 a non-organizer does not see them on that org's page | ✅ | e2e: `D4 (2/3)` |
| UAT-4.3 any logged-in user still sees Host Event on standalone `/events` — the ALLOWED path, gate 7c | ✅ | e2e: `D4 (3/3) — the ALLOWED path, gate 7c` |
| UAT-4.4 an event created from an org page carries that organization, end to end through the real form | ⏭ HUMAN-ONLY | contract row HUM-2, the founder's. Only the link half is automated (`the org-page Host Event link carries org context forward` — href differs from `/events/new`); the form itself is existing surface outside this spec's blast radius |
| UAT-5.1 0 upcoming and some past falls through to Past under an explicit heading | ✅ | e2e: `D6: org with 0 upcoming falls through to Past under an explicit heading` |
| UAT-5.2 0 upcoming and 0 past shows one honest line, not the generic host-invite empty state | ✅ | e2e: `D6: org with neither upcoming nor past shows one honest line` — no Host Event, no Sign Up to Host. **Whether that sentence is the right one is HUM-1, the founder's row, and is NOT claimed here** |
| UAT-6.1 org header and directory card show "N have joined events" | ✅ | e2e: `D9/D10: org header shows "N have joined events"` — verbatim string |
| UAT-6.2 the count is distinct profiles, not RSVP rows | ✅ | e2e: `D9: directory card would show the same count` — 2 distinct RSVP profiles across 2 events |
| UAT-6.3 a zero-participant org renders no row and no `0` | ✅ | e2e: `D9: zero-participant org … omits the row entirely — no row, no "0"`. Enforced at the data layer: a zero-count org is absent from the participation map, not present with 0 |
| UAT-6.4 the `+N` badge is legible and not obscured by the final avatar | ⏭ ENV-UNAVAILABLE | **NOT exercised.** The reviewer reported it untested, in its own words: no render in the set contains more participants than the avatar row draws (max 3 of a 5-avatar limit), so the `+N` overflow chip never appeared and could not be judged. Reaching it needs a fixture with >5 participants and a further round; the fixture NOW seeds 7 and asserts the `+N` chip is visible before capturing (so a future round exercises it), but the contract's 5-round bound is spent, so no round has judged it. Source side IS asserted by M1 (`relative z-10`, `-space-x-2`, PersonAvatar, badge derived from what is drawn) **and** that social-proof.tsx still carries the pattern being reused |
| UAT-6.5 the payload carries no PII beyond name, slug and avatar | ✅ | e2e: `participant payload carries only name/slug/avatar fields` — anon REST read, no `email`, no `reason` |
| UAT-7.1 `/org` lists every public org, links to each, readable signed out; no private org appears | ✅ | e2e: lists both public fixture orgs with correct hrefs; the private fixture org never appears; readable signed out; NULL blurb renders as absence with no placeholder |
| UAT-7.2 no create-organization affordance, signed in or out | ✅ | e2e: no create-organization link or button, signed out AND signed in. Also asserted in source by M1 |
| UAT-7.3 `/org` matches the approved reference at 320px, 375px and desktop | ✅ | blind-reviewer rounds 3 and 5, both PASS, judging renders at all three widths against the pinned reference — card anatomy, badge states pixel-sampled to blue-50/blue-700 vs neutral, membership badge green-50/green-700 and green nowhere else, tabs-vs-pills idiom intact, no overflow or truncation at either narrow width. Also verified structurally by e2e (`no horizontal overflow at 320px`, body scrollWidth ≤ 321) and the render confirms `window.innerWidth` actually took effect before capture |
| UAT-7.4 `/org` is registered in `PROD_HEALTH_ROUTES` | ✅ | e2e: `mechanical check: /org is registered in PROD_HEALTH_ROUTES`, plus the M1 source assertion — the standing decisions.md 2026-06-06 rule had nothing else enforcing it |
| UAT-8.1 an org-scoped event stays world-readable to anon — RLS unchanged | ✅ | integration: `events RLS is unchanged` — an anon REST read of an org-scoped event returns the row with its org_id. No policy is created, dropped or altered by the migration (asserted in M1) |

## Manual-Only Scenarios

| Scenario | Why manual |
|---|---|
| UAT-1.2 prod backfill reconciliation | The migration is a literal slug enumeration, so synthetic test data cannot exercise it. Re-query prod post-deploy and diff against the spec's two tables |
| UAT-4.4 full event-creation round trip | The form is existing `/events/new` surface outside this spec's blast radius; only the org-context link is automated |
| UAT-6.4 `+N` badge paint order | Computed z-index assertions at this DOM depth are unreliable; the blind reviewer judges it from a render instead |
| UAT-7.3 pixel match to the reference | No visual-regression tooling in this repo; owned by the blind reviewer and `/verify` against the pinned artifact |


## UAT-1: The edge + backfill

### UAT-1.1: Schema exists
**Given:** the migration has been applied
**When:** an admin queries `events.org_id`
**Then:** the column exists, is nullable, has a FK to `organization`, and is indexed
**Verify:** `e2e/integration/p1060-events-org-migration.spec.ts`

### UAT-1.2: Backfill is correct on prod
**Given:** the 8 named Chiang Mai event slugs and 2 named Ko Phangan event slugs (Solution item 2)
**When:** the migration runs on prod
**Then:** the 8 carry `org_id = cm`; the 2 stay NULL; the migration comment states it touched exactly 8 rows
**Verify:** manual — query `/rest/v1/events?select=slug,org_id` on prod post-deploy and diff against the spec's tables. The integration test only verifies this best-effort (see its header comment) — it cannot substitute for a prod check.

## UAT-2: The second organization

### UAT-2.1: · Online exists and is reachable
**Given:** the seed migration has run
**When:** a visitor goes to `/org/online`
**Then:** the page loads, shows "Clarity Practice Community · Online", no blurb line (D7 — NULL, no placeholder)
**Verify:** `e2e/integration/p1060-events-org-migration.spec.ts` (seed check) + manual visual check against the approved reference

### UAT-2.2: · Online has an organizer
**Given:** the seed migration has run
**When:** an admin queries `membership` for `org_id = online`
**Then:** at least one row with `role = 'organizer'` exists
**Verify:** `e2e/integration/p1060-events-org-migration.spec.ts`

## UAT-3: Org-scoped Events tab

### UAT-3.1: Each org shows only its own events
**Given:** Org A has an event, Org B has a different event
**When:** a visitor views Org A's Events tab
**Then:** Org A's event appears, Org B's event does not
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "org Events tab lists only its own events"

### UAT-3.2: Standalone /events is unaffected
**Given:** events exist scoped to two different orgs
**When:** a visitor views `/events/list`
**Then:** both events appear, unfiltered
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "standalone /events list is unaffected"

## UAT-4: Host-CTA visibility matrix (D4)

### UAT-4.1: Organizer sees Host Event + Co-create on their org's page
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D4 (1/3)"

### UAT-4.2: Non-organizer does not see them on that org's page
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D4 (2/3)"

### UAT-4.3: Any logged-in user still sees Host Event on standalone /events
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D4 (3/3) — the ALLOWED path, gate 7c"

### UAT-4.4: An event created from an org page carries that org
**Given:** an organizer clicks Host Event from their org's page and completes the form
**When:** the event is saved
**Then:** the created row's `org_id` matches the org they were on
**Verify:** manual — full event-creation flow is out of this suite's automated scope (form complexity); `e2e/p1060-org-scoped-events.spec.ts` asserts only that the link carries org context forward, not the end-to-end save

## UAT-5: Empty-Upcoming fallback (D6)

### UAT-5.1: 0 upcoming, some past
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D6: org with 0 upcoming falls through to Past"

### UAT-5.2: 0 upcoming, 0 past (· Online's day-one state)
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D6: org with neither upcoming nor past"

## UAT-6: Participant count + avatar row (D9/D10)

### UAT-6.1: Org header and directory card show "N have joined events"
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D9/D10: org header shows..."

### UAT-6.2: Count is distinct profiles, not RSVP rows
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D9: directory card would show the same count"

### UAT-6.3: Zero-participant org shows no row, no "0"
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "D9: zero-participant org... omits the row entirely"

### UAT-6.4: Avatar row reuses social-proof.tsx pattern, including +N badge z-10
**Given:** an org with more participants than the avatar row limit
**When:** the row renders
**Then:** the `+N` badge is visible and not obscured by the final avatar
**Verify:** manual visual check — no automated test asserts computed z-index paint order at this DOM depth reliably; flag to `/verify`

### UAT-6.5: No PII beyond public avatar fields in payload
**Verify:** `e2e/p1060-org-scoped-events.spec.ts` — "participant payload carries only name/slug/avatar fields"

## UAT-7: `/org` directory (D5)

### UAT-7.1: Lists every public org, links to each, readable signed-out
**Verify:** `e2e/p1060-org-directory.spec.ts`

### UAT-7.2: No create-organization affordance
**Verify:** `e2e/p1060-org-directory.spec.ts` — "no create-organization affordance appears"

### UAT-7.3: Matches the approved visual reference at 320px, 375px, desktop
**Given:** the approved reference at https://claude.ai/code/artifact/10cedd0b-ddac-42f6-8c45-4fa002319810
**When:** `/org` is rendered at each width
**Then:** layout, spacing, and hierarchy match — blue-500/600 actions, green-600 membership badge only, `max-w-5xl px-4 py-8 space-y-8`, underline tabs (page-level) vs pills (in-content filters), 44px touch targets
**Verify:** manual — `/verify` visual QA against the pinned reference; `e2e/p1060-org-directory.spec.ts` covers 320px structurally (no horizontal overflow) only

### UAT-7.4: /org is in PROD_HEALTH_ROUTES
**Verify:** `e2e/p1060-org-directory.spec.ts` — "mechanical check"

## UAT-8: RLS unchanged

### UAT-8.1: An org-scoped event stays world-readable
**Verify:** `e2e/integration/p1060-events-org-migration.spec.ts` — "events RLS is unchanged"
