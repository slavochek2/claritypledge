# UAT: P1068 — Put migration headers inside the privacy review, and let the reviewer see security content

> Auto-generated by /verify — edit freely.

**Routing note:** This spec has no web-app surface (git hooks, scripts, docs) — nothing for
Playwright or Claude in Chrome to exercise against `localhost` or prod. Verification instead
replays the actual git hook (`scripts/pre-push-checks.sh`) against real commits in an isolated
clone, which is the direct-evidence equivalent for this class of change. All evidence below was
produced during `/dev` (commits `904a0e9d`, `136dc972`) and is re-confirmed here, not re-derived.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Push range with only commit `78de3b83` (P1063 migration) is blocked for want of a `/privacy` stamp | pass | Replayed in an isolated clone: `PRIVACY GATE` message, exit 1. Confirmed pre-fix the same range passed Layer 2 silently. |
| UAT-2: A `src/`-only push range still needs no stamp (no new friction) | pass | Replayed in an isolated clone: no `PRIVACY GATE` message; Layer 2 skipped as before. |
| UAT-3: The gate is seen to fail, not masked by a pipe | pass | Non-zero exit (1) pasted from UAT-1's replay; `grep` confirms zero `\| tee` occurrences in every touched script — no pipefail hazard exists to guard. |
| UAT-4: All watched-path sites agree | pass | `grep -n "supabase/migrations" scripts/privacy-watched-paths.sh scripts/git-ops.sh scripts/pre-push-checks.sh .claude/commands/slava/maintain/privacy/SKILL.md .claude/commands/slava/build/push.md` — all five now list it (review found a 5th site beyond the spec's original four; fixed in `136dc972`). |
| UAT-5: `privacy/SKILL.md` rescoped for security content | pass | "not a security scan" disclaimer removed; "Technical architecture decisions" removed from `### Not a concern`; live-vulnerability red flag present, names `.private/docs/security-log.md`. |
| UAT-6: `CLAUDE.md:262` routing corrected | pass | Points at `.private/docs/security-log.md`; edit gated through `/slava:maintain:claude-md` (NEUTRAL, no budget conflict, applied). |

Functional pass: **6/6 scenarios pass** — all backed by command output pasted during `/dev`, re-verified
above by direct grep, not re-asserted from memory (epistemic gate 9).

Visual pass: **skipped — no UI files changed** (`git diff main...HEAD --name-only` shows zero `.tsx`/`.css`/`.module.css`).
Mobile (390px): **skipped (no UI changes)**.

New regression test added by this branch: `scripts/test-hook-sha-gate.sh` AC8 — passes; 4 pre-existing
unrelated failures (AC2, AC5, AC6×2) reproduce identically on `main`, confirmed not introduced by this change.
