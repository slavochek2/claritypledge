# UAT: P1076 — Org invite link — share to join

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Member invites via ShareDialog in <=3 taps (AC-1) | PASS | `e2e/p1076-org-invite-link.spec.ts` "smoke: member sees an Invite button…" + "Invite dialog: correct title…" — passed on first attempt |
| UAT-2: Signed-out visitor accepts, registers, returns as member (AC-2) | FAIL | "auto-join:…" test timed out both attempts (30s each) waiting for `/org/{slug}` after magic link. Control run this session: `e2e/integration/p458-auth-callback-position.spec.ts` "Full magic-link round-trip" **also timed out identically**, confirming the spec's own claim of a pre-existing test-Supabase OTP environment issue, not a P1076 code defect |
| UAT-3: Invite dialog visually matches profile ShareDialog (AC-3) | PASS | Live authenticated Claude-in-Chrome check (real session injected via test fixtures, not Playwright): dialog title "Invite new members", Link box with full unclipped/wrapped URL incl. `?from=<inviter id>`, Copy icon, single primary-blue "Share…" button — matches UI Contract exactly. Zero console errors. |
| UAT-4: No existing post-auth redirect destination changes (AC-4) | PASS | `p458-auth-callback-position.spec.ts` 11/12 passed (only the magic-link round-trip failed, same known env issue above) |
| UAT-5: Attribution recorded on join, never surfaced in UI (AC-5) | PASS | `e2e/integration/p1076-org-invite-attribution-migration.spec.ts` 7/7 passed (column exists, valid inviter stored, column-gated from anon/authenticated SELECT) |
| UAT-6: Forged/nonsense `?from=` stores NULL, never raw string | PASS | same file, "forged/nonexistent invited_by is nulled" passed |
| UAT-7: Already-signed-in visitor flow unchanged — one tap, joins | PASS (not re-run this session) | Spec's Done-When cites full p1010 regression suite (18 tests) + full unit suite (2784 tests) passing unchanged — not independently re-executed this pass |
| UAT-8: Existing member opening invite link sees sane state, not error | PASS | "existing member opening the invite link lands on the org page…" passed on first attempt |
| UAT-9: Post-join prompt appears once, dismissible | PASS (re-run) | `/fix` commit `dd644e63` split the two banner tests out of the auto-join serial block into their own `test.describe` with independent fixtures. Re-run this session: both "post-join banner appears on a real join and is dismissible" and "an existing member opening the invite link does NOT see the just-joined banner" show clean `passed` on first attempt, no retry — confirms the fix, independently re-verified (not just trusting the fix commit's own claim, per epistemic gate 9) |
| UAT-10: `/org` present in `ALLOWED_REDIRECT_PREFIXES` (regression) | PASS | `p1076-org-invite-attribution-migration.spec.ts` "/org prefix is in ALLOWED_REDIRECT_PREFIXES" passed, plus a bonus bypass-redirect regression test passed |
| UAT-7 re-verification: p1010 regression suite (existing flows unchanged) | PASS | Re-run this session: `e2e/p1010-organizations.spec.ts` + `e2e/integration/p1010-organizations-membership-migration.spec.ts` — 30/30 passed |
| UAT full unit suite | PASS | Re-run this session: `npx vitest run` — 2784 passed, 19 skipped, 0 failed (Test Files: 247 passed, 2 skipped) — matches spec's Done-When claim, independently confirmed |
| Visual: desktop + 375px + 320px pass | PARTIAL (carried forward) | No `.tsx`/`.css` files changed since the prior `/verify` session's real authenticated Chrome check (UAT-3) and the `/dev` session's blind visual-QA + DOM-verified truncation fix (`scrollHeight <= clientHeight` at 320/375px). The only commit since (`dd644e63`) touches only test files. Not re-run via Chrome this pass — carried-forward evidence, disclosed rather than silently re-claimed as fresh. |

**Flakiness disclosure (Transparency Principle — not retried to green), reconfirmed this session:** Re-running the full spec, tests 1-5 (in the original serial block, alongside the still-failing auto-join test) show final status **"flaky"** (recovered on retry) rather than a clean single-attempt pass — same serial-describe-block cascade as the prior run, now compounded by Playwright's retry re-running the whole block after the auto-join test's timeout. Root cause unchanged: the auto-join test's `page.waitForURL` after `page.goto(magicLinkUrl)` times out at 30s. **Independently reconfirmed this session** (not just citing the prior run's note): re-ran the control test `p458-auth-callback-position.spec.ts` "Full magic-link round-trip" and it fails identically (`page.waitForURL: Timeout 30000ms exceeded` after the same magic-link navigation pattern) — confirms this is the pre-existing test-Supabase OTP environment issue, not P1076 code, and not new since the last /verify pass. Not root-caused further per `/verify`'s triage rule.
