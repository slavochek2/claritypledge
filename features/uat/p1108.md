# UAT: P1108 — Link previews must say true things, and keep saying them

Spec: [../p1108_link_previews_say_true_things.md](../done/2026-06-10/p1108_link_previews_say_true_things.md)

**Backend surface, no rendered output.** `feature_type: backend`, zero COMPARABLE rows in the
Verification Contract — `goal-gate.sh` CHECK 5 skips the blind-reviewer round by its own logic, and
no reviewer roster is owed. This UAT file exists to satisfy CHECK 4 (scorecard decay) and to give
every Done-When / contract row a place to carry a result.

Automated coverage (post adversarial-review fix pass, 2026-08-20):

| File | Count | Covers |
|---|---|---|
| `src/tests/p1108-pledge-claim.test.ts` | 7 | DW-2 + DW-3 — non-pledger omits the pledge sentence, pledger still asserts it; +3 post-review: the phrase cannot be forged through `name`/`role` |
| `src/tests/p1108-claim-binding.test.ts` | 6 | DW-4 — `bindClaim` throws when a claimed column is not in the select list; +2 post-review: nested-embed substring matching |
| `src/tests/p1108-fail-loud.test.ts` | 12 | DW-5 + SA-1 + SA-2 — non-OK response, rejecting fetch, no-agent/malformed-embed pair; +4 post-review: fetch-URL capture for story/point, array-shaped `profiles` embed fail-open |
| `src/tests/p1108-esc.test.ts` | 6 | ESC-1 — `esc()` escapes all five of `&`, `"`, `<`, `>`, `'` |
| `src/tests/p1104-og-agent-marker.test.ts` | 10 | REG-1 — every P1104 assertion still holds. **Not byte-identical:** two fixtures gained an explicit `has_pledged: true` (`git diff main...HEAD -- src/tests/p1104-og-agent-marker.test.ts` is 7 lines, not 0) — see `features/verification/p1108/assumptions.md` §1 for why. |

**31 new automated tests** (this spec) **+ 10 regression tests** (P1104) **= 41 total**. Run them with:

```bash
npx vitest run src/tests/p1108-pledge-claim.test.ts src/tests/p1108-claim-binding.test.ts src/tests/p1108-fail-loud.test.ts src/tests/p1108-esc.test.ts src/tests/p1104-og-agent-marker.test.ts
```

---

## Test Execution Log

| Scenario | Result | Notes |
|---|---|---|
| DW-1: four handlers audited, claim + backing column each | PASS | `## Claim Audit` in the spec; `grep -c '^- \*\*ogFor'` = 4 |
| DW-2: non-pledger card omits the pledge sentence | PASS | `p1108-pledge-claim.test.ts`, both role/no-role branches |
| DW-3: pledger card still asserts the pledge | PASS | `p1108-pledge-claim.test.ts`, both role/no-role branches |
| DW-4: claim-binding mechanism watched to fail | PASS | manual demonstration (column deleted, throw + exit 1 pasted, restored) — output in `## Forced-Failure Output` below — + permanent unit tests in `p1108-claim-binding.test.ts` |
| DW-5: failed agent lookup does not render the ordinary-person card | PASS | `p1108-fail-loud.test.ts` case (d) — malformed embed → subject-silent fallback; array-shaped `profiles` embed (post-review fix) also covered |
| SA-1: failed/unreadable lookup never yields the ordinary-person preview | PASS | `p1108-fail-loud.test.ts` — non-OK (all 4 routes), rejecting fetch, malformed-embed, and array-shaped-embed cases |
| SA-2: "no agent" vs "lookup failed" distinguishable | PASS | `p1108-fail-loud.test.ts` cases (c) vs (d) — same 200 response class, embed shape alone flips the outcome |
| ESC-1: `esc()` escapes all five characters | PASS | `p1108-esc.test.ts` |
| REG-1: shipped P1104 suite's assertions still hold | PASS | `p1104-og-agent-marker.test.ts`, 10/10 — but see the "Not byte-identical" note above; result is PASS on behavior, not on file diff |
| Adversarial review (4 Opus reviewers, distinct lenses) | PASS, with fixes | 1 CRITICAL + 4 HIGH found; CRITICAL + 3 HIGH fixed same session (pledge-claim forgery via name/role, `bindClaim` dead code for story/point, array-shaped `profiles` embed fail-open, cache staleness window). 1 HIGH (`og:image` unauthenticated) + several MEDIUM/LOW explicitly deferred — see `features/verification/p1108/assumptions.md` §4. Full ranked findings in session transcript, not filed as a separate doc. |
| DW-7: live `curl` with a crawler UA against a deployed non-pledger, pledger and agent URL | ⏭ HUMAN-ONLY | post-deploy, founder-run — needs a deployed target; no browser/DB reachable pre-deploy per the contract |

---

## Forced-Failure Output

Per epistemic gate 7 and DW-4 — the mechanism must be watched to fail, not asserted to. `/finish`
spec review (2026-08-20, MEDIUM) found the prior version of this row cited "output pasted" with no
locatable artifact — a session transcript is not durable. Pasted here.

**Run 1 (initial implementation, `PROFILE_COLUMNS` with `has_pledged` deleted):**

```
Error: og.ts claim binding violated: "signed the Clarity Pledge" requires column "has_pledged" to be
selected, but only [name, role, avatar_url, banner_url, agent_accounts(operator_name)] is fetched.
 ❯ bindClaim api/og.ts:49:11
 ❯ api/og.ts:196:1

 Test Files  1 failed (1)
      Tests  no tests
```
Confirmed exit code separately: `npx vitest run src/tests/p1108-claim-binding.test.ts > /tmp/bindclaim-fail.log 2>&1; echo "ACTUAL_EXIT=$?"` → `ACTUAL_EXIT=1`. Column restored, re-run green.

**Run 2 (post-review fix pass, `POINT_COLUMNS`'s agent embed dropped from the actual query string —
the exact regression `bindClaim` exists to catch, per the adversarial-review findings):**

```
Error: og.ts claim binding violated: "a machine-generated reading operated by {operator}" requires
column "agent_accounts(operator_name)" to be selected, but only [statement, banner_url,
profiles!points_first_validator_id_fkey(name)] is fetched.
 ❯ bindClaim api/og.ts:55:11
 ❯ api/og.ts:202:1

 Test Files  1 failed (1)
      Tests  no tests
```
`npx vitest run src/tests/p1108-claim-binding.test.ts > /tmp/mutation-test.log 2>&1; echo "EXIT=$?"` → `EXIT=1`. Restored; both `p1108-claim-binding.test.ts` and the full suite re-ran green afterward.

---

## Manual-Only Scenarios

| Scenario | Why manual |
|---|---|
| DW-7 — live `curl` against deployed URLs | `api/og.ts` is a Vercel serverless function, not served by `npm run dev`; no local browser or deploy target reaches it. Contract row DW-7 is HUMAN-ONLY by design (`## Resolved Decisions (goalify)`). |
