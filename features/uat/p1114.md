# UAT: P1114 — the event room (revision 2)

> Seeded by `/goalify` 2026-08-20. Every row starts unmarked on purpose: an unmarked row
> fails the gate, so the loop cannot finish without walking each scenario and writing what
> actually happened. A `⏭️` needs one of `NOT-BUILT`, `ENV-UNAVAILABLE`, `HUMAN-ONLY` or
> `SUPERSEDED` in the note — a bare skip does not count.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Signed out, open `/events/:slug/room` → the gate, with the four approved strings | - | |
| UAT-2: On the gate, nothing about the room is visible — no roster, no principle, no slider, no count | - | |
| UAT-3: Signed out, open `…/ready` and `…/meet` directly → the same gate on both | - | |
| UAT-4: Tap "Register for this event" → lands on `/events/:slug`, the existing RSVP button is there | - | |
| UAT-5: Signed in but NOT registered → still the gate | - | |
| UAT-6: Registered + signed in, first visit → lands on `…/ready`, no name field anywhere | - | |
| UAT-7: `…/ready` is the shipped `/ready` — same question, same slider, same anchors, no caption | - | |
| UAT-8: Set readiness, tap Continue → `…/meet` | - | |
| UAT-9: `…/meet` shows the real certificate, scrolling under a pinned bar carrying Opt in / Opt out | - | |
| UAT-10: The roster sits above that bar, below the certificate | - | |
| UAT-11: Roster rows show full name, avatar, pledge ring, ear badge — and link to the profile | - | |
| UAT-12: Opt in → you appear on the roster; no second "Opt in" control anywhere on the page | - | |
| UAT-13: No understanding-number step and no "Start meeting" button appears at any point | - | |
| UAT-14: Second browser opts in → the first browser's roster updates without a reload | - | |
| UAT-15: Opt out, then opt in again → the change is visible to the other browser | - | |
| UAT-16: Return visit with readiness already set → lands on `…/meet`, skipping readiness | - | |
| UAT-17: Return visit with readiness NOT set → lands on `…/ready` | - | |
| UAT-18: Neither page shows the marketing nav, the footer, or any Project/Present control | - | |
| UAT-19: Zero state — "No one has opted in yet." before anyone answers | - | |
| UAT-20: Past `EVENT_GRACE_HOURS` → "This has closed. Here's who opted in.", nothing editable | - | |
| UAT-21: Event page — the tab bar sits above the event card, under "← Events" | - | |
| UAT-22: Event page — Practice Rooms renders where it does on `main`, not inside a tab | - | |
| UAT-23: Switch tabs, press Back once → returns to the previous tab, one press | - | |
| UAT-24: Standalone `/ready` and `/meet` are unchanged — walk both end to end | - | |
| UAT-25: 320px, 375px and desktop — all of the above, no horizontal scroll | - | |
