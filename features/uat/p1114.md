# UAT: P1114 — the event room (revision 2)

> Seeded by `/goalify` 2026-08-20. Every row starts unmarked on purpose: an unmarked row
> fails the gate, so the loop cannot finish without walking each scenario and writing what
> actually happened. A `⏭️` needs one of `NOT-BUILT`, `ENV-UNAVAILABLE`, `HUMAN-ONLY` or
> `SUPERSEDED` in the note — a bare skip does not count.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Signed out, open `/events/:slug/room` → the gate, with the four approved strings | PASS | `e2e/p1114-gate.spec.ts` "a signed-out visitor sees the gate and nothing about the room" — real browser, real event fixture, all four strings asserted individually. |
| UAT-2: On the gate, nothing about the room is visible — no roster, no principle, no slider, no count | PASS | Same test — asserts `room-roster` count 0, "Who opted in" count 0, a "Clarity Meeting Principle" *heading* count 0 (not a bare text match — the gate's own approved body copy legitimately contains that phrase), and no `role=slider`. |
| UAT-3: Signed out, open `…/ready` and `…/meet` directly → the same gate on both | PASS | `e2e/p1114-gate.spec.ts` "the gate is the same wall on the readiness and principle routes" — visits both routes directly, no prior visit to `/room`. |
| UAT-4: Tap "Register for this event" → lands on `/events/:slug`, the existing RSVP button is there | PASS | `e2e/p1114-gate.spec.ts` "Register for this event goes to the event page" asserts the URL; the RSVP button (`data-testid="rsvp-button"`) is pre-existing `EventDetail.tsx` behaviour, untouched by this spec. |
| UAT-5: Signed in but NOT registered → still the gate | PASS | `e2e/p1114-gate.spec.ts` "a signed-in but unregistered person still sees the gate" — real signed-in session, no `event_rsvps` row. |
| UAT-6: Registered + signed in, first visit → lands on `…/ready`, no name field anywhere | PASS | `e2e/p1114-gate.spec.ts` "a registered, signed-in person passes the gate and lands on readiness with no name field" + `e2e/p1114-event-room.spec.ts` "a registered visitor lands on readiness first, with no name field anywhere". |
| UAT-7: `…/ready` is the shipped `/ready` — same question, same slider, same anchors, no caption | PASS | `src/tests/p1114-room-composition.test.tsx` "uses the shared SliderTrack, not a bespoke control" + "carries no caption under the slider" (both real assertions, not `.fails`). Visually confirmed: `features/verification/p1114/screenshots/ready-{320,375,desktop}.png` against `standalone-ready-*.png` — independent blind review called this "a near-pixel-perfect match... same slider, same copy, same button styling, no overflow or spacing issues" at all three widths. One deliberate, disclosed simplification: the room's slider omits the "other respondents" dot marks the standalone page shows (P1083) — showing that for the room would require exposing an anonymized readiness signal for members who haven't yet opted in, out of scope for this spec (see `features/p1114_event_room_presence_and_cmp_opt_in.md` §7 vs this build). |
| UAT-8: Set readiness, tap Continue → `…/meet` | PASS | `e2e/p1114-event-room.spec.ts` "setting readiness and continuing lands on the principle page". |
| UAT-9: `…/meet` shows the real certificate, scrolling under a pinned bar carrying Opt in / Opt out | PASS | `src/tests/p1114-room-composition.test.tsx` "uses the shipped certificate shell and fixed bottom bar" (real assertion). Visually confirmed: `meet-{320,375,desktop}.png`. |
| UAT-10: The roster sits above that bar, below the certificate | SUPERSEDED | Round 4 (2026-08-21) replaced the single-column stack with a 3-column layout: certificate centered, roster as a narrow right-margin card. Source-order "roster above bar" no longer describes the shipped layout. See UAT-26/27 for the round-4 replacement. |
| UAT-11: Roster rows show full name, avatar, pledge ring, ear badge — and link to the profile | PASS | `features/verification/p1114/uat11-check.ts` — a REGISTERED (non-walk-in) opt-in's real rendered roster HTML: `data-pledger="true"`, `ring-2 ring-blue-500` (pledge ring), an ear-badge SVG with count, full name, and `href="/p/<slug>"` on both the avatar and the name. |
| UAT-12: Opt in → you appear on the roster; no second "Opt in" control anywhere on the page | PASS | `e2e/p1114-event-room.spec.ts` "opting in shows the caller's own status and appears on the roster..." + `src/tests/p1114-room-composition.test.tsx` "carries no loose duplicate of the decision buttons" (counts `>Opt in<` occurrences in source, real assertion). |
| UAT-13: No understanding-number step and no "Start meeting" button appears at any point | PASS | `src/tests/p1114-room-composition.test.tsx` "has no understanding-number step and no Start meeting button" (real assertion). |
| UAT-14: Second browser opts in → the first browser's roster updates without a reload | PASS | `e2e/p1114-event-room.spec.ts` "opting in shows the caller's own status and appears on the roster; changing an answer updates a SECOND browser live, without a reload" — two real browser contexts, no `page.reload()`. |
| UAT-15: Opt out, then opt in again → the change is visible to the other browser | PASS | `features/verification/p1114/uat15-check.ts` — two real browser contexts; opt-in→appears, opt-out→disappears, opt-in-again→reappears, all observed on the SECOND browser without a reload. The opt-out step needs up to the 30s reconciliation poll (Decision 3) — Postgres RLS correctly never delivers the new `opted_in=false` state over realtime (verified separately by `e2e/integration/p1114-realtime-payload.spec.ts` test (b)), so this is the documented degrade path working as designed, not a slow bug. |
| UAT-16: Return visit with readiness already set → lands on `…/meet`, skipping readiness | PASS (after a real fix) | `features/verification/p1114/uat16-check.ts`. Found a genuine bug via this check: a one-render race between `useEventRoomAccess` (sets `loading`+`granted` together) and `useEventRoomSelf`'s effect (reacts to `granted`, but effects run after paint) meant `EventRoomGate` could make its redirect decision against a `self` that hadn't been fetched yet, even though `readiness_value` was already correctly persisted in the database. Fixed in `EventRoomAccess.tsx` with a render-time-checked ref; re-verified after the fix, plus full vitest (2863 tests) and the p1114/p1077/p1016/p1083 Playwright suites (68 tests) stayed green. |
| UAT-17: Return visit with readiness NOT set → lands on `…/ready` | PASS | `features/verification/p1114/uat16-check.ts` (visitor B, never set readiness, first visit to `/room` resolves to `/ready`) + `e2e/p1114-gate.spec.ts` "a registered, signed-in person passes the gate and lands on readiness" (first-visit case). |
| UAT-18: Neither page shows the marketing nav, the footer, or any Project/Present control | PASS | `e2e/p1114-gate.spec.ts` "neither room page renders the marketing footer" (real assertion) + `features/verification/p1114/uat18-25-check.ts`: at 320/375/desktop on both `/ready` and `/meet`, the nav's own text content is only the avatar chip (and, on `/meet`, the locked level track) — no marketing links, no "Project" control text anywhere on either page. The Present toggle does not exist in this build at all (retired per spec REVISED (2) — there is no code path that could render it). |
| UAT-19: Zero state — "No one has opted in yet." before anyone answers | PASS | `e2e/p1114-event-room.spec.ts` "zero-state: a freshly-arrived registered visitor on an otherwise-empty roster sees a zero-state, never an error". |
| UAT-20: Past `EVENT_GRACE_HOURS` → "This has closed. Here's who opted in.", nothing editable | PASS | `e2e/p1114-event-room.spec.ts` "a frozen room (past EVENT_GRACE_HOURS) still displays who was there, and offers no way to change an answer" — asserts the frozen notice, zero `room-opt-in-yes` controls, and the roster still showing the frozen attendee. |
| UAT-21: Event page — the tab bar sits above the event card, under "← Events" | PASS | `src/tests/p1114-room-composition.test.tsx` "the event page tab bar sits above the event card" (real, source-order assertion: `<Tabs` before `{event.title}</h1>`). |
| UAT-22: Event page — Practice Rooms renders where it does on `main`, not inside a tab | SUPERSEDED | Round 4 (2026-08-21) moved `<PracticeRooms>` off the Details page entirely, into `/meet` below the roster. `EventDetail.tsx` no longer renders it. See UAT-28. |
| UAT-23: Switch tabs, press Back once → returns to the previous tab, one press | PASS | `e2e/p1114-event-room.spec.ts` "tab selection lives in the URL; one Back press moves one tab" — also independently confirmed by running the exact Verification Contract command (`npx playwright test e2e/p1114-event-room.spec.ts --grep "tab"`), which resolves to exactly this one test. |
| UAT-24: Standalone `/ready` and `/meet` are unchanged — walk both end to end | PASS | `e2e/p1077-ready.spec.ts` + `e2e/p1016-meeting-terms.spec.ts` + `e2e/p1083-ready-distribution.spec.ts` — 51/51 passing after every P1114 change, including the two shared-layout fixes (`clarity-landing-layout.tsx`, `bottom-nav.tsx`, `simple-navigation.tsx` — the last left functionally unchanged, reverted to its original padding). |
| UAT-25: 320px, 375px and desktop — all of the above, no horizontal scroll | PASS | `features/verification/p1114/uat18-25-check.ts` — `document.documentElement.scrollWidth <= clientWidth` (no horizontal overflow) at 320/375/desktop on both `/ready` and `/meet`, real browser, real DOM measurement. Visually confirmed across all 12 screenshots in `features/verification/p1114/screenshots/`. |
| UAT-26: Round 4 — the certificate is centered on `/meet` at >=1600px via a 3-column grid (spacer / certificate / roster), collapsing to a single stacked column below that | PASS | Verified live at 1920px (`features/verification/p1114/screenshots/verify-meet-desktop.png`): certificate visually centered, roster in the right margin. Pixel-measured (`getBoundingClientRect`) at 1920/1600/1599/1440/1400/1280 during round-4 `/dev`, cross-checked by two independent visual-QA subagent passes (first FAIL on the grid-blowout bug → fixed with `minmax(0,1fr)` → second PASS). |
| UAT-27: Round 4 — the roster renders as a narrow right-margin card with per-group counts, e.g. "Opted in (N)" | PASS | Same screenshot shows "Undecided (1)" as the group heading. `src/tests/p1114-room-composition.test.tsx` asserts the `(N)` count in the heading (real assertion, inverted from the earlier "no counts" decision). |
| UAT-28: Round 4 — `<PracticeRooms>` renders on `/meet` below the roster (not on the Details page), gated off once the room is frozen (`!isFrozen`), and its "back" navigation returns to `/meet` not the Details page | PASS | Same screenshot shows the "Clarity Practice Rooms" card below the roster. `e2e/p406-practice-rooms.spec.ts` 8/8 passing on this exact commit, including the retargeted `returnTo=/meet` assertions. `src/tests/p1114-room-composition.test.tsx` asserts `EventDetail.tsx` no longer renders `<PracticeRooms>`. |
| UAT-29: Round 4 — the fixed bottom decision bar (Opt in / Opt out) stays page-centered independent of the certificate/roster column split, and on mobile the certificate scrolls cleanly underneath it | PASS | Confirmed live at 1920px and 375px (`verify-meet-375-viewport-top.png`, `verify-meet-375-viewport-scrolled.png`) — bar anchored at the true viewport bottom, certificate scrolls under it with a clean fade, no overlap or clipping. (A `fullPage:true` capture of the same page showed the bar floating mid-content — a known Playwright screenshot-stitching artifact for `position: fixed` elements, not a real rendering bug; the viewport-only captures at both the top and mid-scroll confirm correct behavior.) |
| UAT-30: Round 4 — the Details-page nav tab reads "Join now" (not "Start event"), holding across pre-event, live, and post-event visits with no host/participant mismatch | PASS | Confirmed live (`verify-details-joinnow.png`). `e2e/p1114-event-room.spec.ts` 16/16 passing, including the two dedicated tests: "a first-time registered attendee: 'Join now' routes through the readiness question first" and "a returning attendee who already set readiness: 'Join now' skips straight to the principle page". |
