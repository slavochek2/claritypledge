# UAT: P1134 — No channel attribution on outbound links

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: UTM params auto-promote to first-class Mixpanel event properties (no custom capture code needed) | pass | Verified live against prod: `window.mixpanel.persistence.properties()` showed `utm_source`/`utm_medium`/`utm_campaign` registered as super properties immediately on load with tagged URL; `Run-Query` filtering `$mp_web_page_view` by `utm_source = p1134test` (today) returned 2 events. |
| UAT-2: Convention documented in docs/technical/analytics.md | pass | "Channel Attribution (UTM)" section added with source/medium/campaign value tables. |
| UAT-3: `/cm` page's "Add this calendar to yours" link carries UTM params | pass | Unit test (`src/tests/p909-cm-fullscreen-layout.test.tsx`) asserts `utm_source=cm-page&utm_medium=calendar-subscribe&utm_campaign=chiang-mai-calendar` on the href. Confirmed live in a rendered browser (chrome-devtools `evaluate_script` against `localhost:5600/cm`) — same values present in the actual DOM attribute. |
| UAT-4: At least one `promote-*` skill's generated links carry UTM params | pass | `promote-facebook.md` template + Conventions section updated to `?utm_source=facebook&utm_medium=community-group&utm_campaign=[slug]`; committed to main (skill files must live on main, not the feature branch). |
| UAT-5: Follow-up query 2+ weeks post-ship shows a non-$direct utm_source | accepted-deferred | Founder decision 2026-08-21: structurally a post-ship check, cannot run pre-ship. Tracked in spec Done-When with a follow-up date (on/after 2026-09-04). |
| UAT-6: Visual — `/cm` page renders without regression after the href change | pass (with unrelated out-of-scope finding) | Screenshots at 1280px/768px/375px via chrome-devtools; independent QA subagent reviewed blind. The subscribe button itself is unchanged and renders correctly at all 3 widths. The subagent also flagged that Google Calendar's own embedded week-view iframe renders overlapping/clipped event chips at 1280px and 768px on the current (very busy) week — this is the third-party embed's own rendering of real calendar data, present on `main` before this change (diff touches only the href attribute, not the iframe/breakpoint logic), and out of scope for this UTM-tagging spec. |
