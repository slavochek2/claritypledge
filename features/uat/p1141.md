# P1141 — UAT scorecard

**Spec:** `features/p1141_story_carries_a_video_with_jumpable_quotes.md`
**Branch:** `feature/p1141-story-video-quotes`
**Run:** 2026-08-23, by the `/goal` loop in worktree w3.

This scorecard did not exist before this run — P1141 never went through `/generate-tests`, which
its own Verification Contract records. Every row below maps to a Done-When or Acceptance-Criteria
line and carries the command or the reason that decided it.

**Skip legend:** `HUMAN-ONLY` — the row needs a story to have been **filed**, which means running
the publish pipeline under an agent account: an external, irreversible, ALWAYS-ASK action the loop
may not take. Not taste; out of the loop's reach by design.

## Test Execution Log

| Scenario | Result | Notes |
|---|---|---|
| DW-1 a story with a video renders a player where the picture used to be | ✅ PASS | `p1141-story-media.test.tsx` (mode=player mounts the player, never the image) + `p1141-story-video.spec.ts` on the real route with real auth |
| DW-2 clicking a timecode seeks the player in place and brings it into view, no reload | ✅ PASS | **Corrected 2026-08-24.** The original evidence covered only the SEEK half — `p1141-video-seek.test.tsx` asserts seekTo(90, true) + playVideo through the imperative handle. Nothing asserted the scroll, and the shipped `behavior: 'smooth'` was a silent no-op (measured: player 677px above the viewport, scrollY 919.5 before AND after). Now covered by an e2e scroll-position test in `p1141-story-video.spec.ts`, confirmed to FAIL with the defect restored (player at top=-2021 after the click) and pass with the fix |
| DW-3 with the player blocked, the story renders in full and every timecode opens the source at the right second | ✅ PASS | `p1141-video-seek.test.tsx` (backstop timer → thumbnail fallback) + `p1141-story-video.spec.ts` with the embed aborted at the network layer |
| DW-4 a story with no video reference renders exactly as today, on every surface | ✅ PASS | `p1141-video-lib.test.ts` (13 rejection classes all collapse to null) + `p1141-story-media.test.tsx` (6 absent/unparseable inputs all fall through to the untouched image path) |
| DW-5 email/letter, feed card and crawler share card show the thumbnail with a play affordance, linking to the story | ✅ PASS | `p1141-story-media.test.tsx` (card links to `/story/:id`, never off-site) + `p1141-og-video-thumbnail.test.ts` (STORY_COLUMNS + derivation + banner_url fallback) |
| DW-6 a letter sealed before this change renders identically after it | ✅ PASS | `p1141-letter-snapshot-contract.test.ts` — absent keys map to no-video and every other field is byte-identical; `p1141-seal-rpc-video-canary.test.ts` failure path exercised (exit 1 regressed / 0 real) |
| DW-7 every agent story shows the byline, the machine marker and the footer line, with the footer link resolving | ✅ PASS | `p1141-agent-story-chrome.test.tsx` — RD-1 footer verbatim, RD-2 `/machines` route asserted present in App.tsx |
| DW-8 story text renders structure; a link whose label differs from its destination does not render as a link | ✅ PASS | `p1141-linkify-structure.test.tsx` — all five bypass classes (a) parsed-URL compare (b) confusable host (c) reference-style (d) autolink scheme (e) raw label token |
| DW-9 filed story text uses the subject's full name and contains no pronoun referring to the subject | ⏭ SKIP — HUMAN-ONLY | Needs a filed story. The RULE is asserted mechanically (`p1141-pipeline-rules.test.ts`: points-prepare owns it, states the reason, publish carries the backstop) — the filed TEXT is the founder's to check |
| DW-10 for one real story, every timecode lands within a few seconds of the words it points at | ⏭ SKIP — HUMAN-ONLY | Needs a filed story played end to end against its source. The mechanism is asserted (raw `.vtt`, never the cleaned ~30s transcript); the landing is not |
| DW-11 the voice rules and the section label live in exactly one skill, with the choice stated | ✅ PASS | `p1141-pipeline-rules.test.ts` — points-prepare authors, points-publish greps; only one file explains the pronoun rule |
| DW-12 an agent story shows no verified count and no Verify affordance; a human story is unchanged | ✅ PASS | `p1141-agent-story-chrome.test.tsx` — all 4 surfaces gated on `!isAgent && !identityPending`, asserted against source because the still-loading registry state is unreachable from mock props |
| DW-13 per-quote timecodes resolve from the retained raw caption file, not the cleaned transcript | ✅ PASS | `p1141-pipeline-rules.test.ts` — the raw store path, the trap named explicitly, and the filing-time enforcement line |
| AC-1 a reader can check any quote in one click, without leaving the page | ✅ PASS | `p1141-video-seek.test.tsx` + `p1141-story-video.spec.ts` — one click, ≥40px target, no navigation |
| AC-2 a reader can tell at a glance which words the machine wrote and which were quoted | ⏭ SKIP — ENV-UNAVAILABLE | COMPARABLE row: needs a blind reviewer that is NOT the agent that built this. See `features/verification/p1141/assumptions.md` A-7 |
| AC-3 a reader arriving from email or a shared link sees the video is a video and reaches the story | ⏭ SKIP — ENV-UNAVAILABLE | COMPARABLE row, same blocker as AC-2. The mechanical half (thumbnail + play affordance + link target) passes under `p1141-story-media.test.tsx` and `p1141-og-video-thumbnail.test.ts` |
| AC-4 nothing a reader sees claims the named person holds the position the agent states | ⏭ SKIP — HUMAN-ONLY | Needs filed story text |
| the whole unit suite is green (regression baseline) | ✅ PASS | 283 files / 3202 tests, up from 274 / 3054 at pin time |
| the video_url CHECK constraint rejects a non-allowlisted host through a raw REST insert | ✅ PASS | `e2e/integration/p1141-video-schema.spec.ts` — 13 passed against the real test database, 7 rejection classes |
| no test in this set is neutered (no it.fails / test.fails / .skip) | ✅ PASS | anti-cheat row exit 0; the row itself requires the files to exist first |

## Manual-Only Scenarios

| Scenario | Why manual |
|---|---|
| DW-9, DW-10, AC-4 | Each needs a story **filed** through the publish pipeline under an agent account — an external, irreversible, ALWAYS-ASK action. The loop may not take it, and no amount of test scaffolding substitutes for it |
| The real player's own chrome and aspect behaviour | A published artifact cannot load an external embed, and the e2e run deliberately blocks it. Judge in a real browser against a real video |
| AC-2, AC-3 | Require a blind reviewer given renders and nothing else. The agent that built this cannot be that reviewer — that is the one durable constraint in the contract's own words |
