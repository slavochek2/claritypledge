# UAT: P1179 — the event room "Links" menu and the locked stake surface

> Written by the /goal loop, 2026-08-28. Every row carries a result; a skip names a
> whitelisted reason. Results marked ⏭️ HUMAN-ONLY are the founder's to close.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: The Links button renders beside the avatar on /room, /ready, /meet and a stake page | ✅ | `p1179-links-menu.test.tsx` — all four paths. Confirmed in a real browser at the default desktop viewport (nav snapshot shows `button "Links"` as a sibling of the avatar "Menu" control). |
| UAT-2: The button is visible and tappable at a literal 320px, overlapping neither logo nor avatar | ✅ | `e2e/p1179-links-menu.spec.ts` — measured bounding boxes, `window.innerWidth` confirmed 320 before measuring. Height ≥44px, box inside the viewport, no overlap with either. Also run at 375px. |
| UAT-3: Tapping it opens a bottom sheet, not a dropdown; every entry ≥44px | ✅ | Same spec. Bottom edge polled to the viewport bottom (it slides in over 300ms), full-bleed width, top below the trigger, 5 entries each ≥44px. |
| UAT-4: An event with no extras lists exactly cmp7, cmp3, cmp10, Transcribe, Start a Clarity Session | ✅ | `p1179-links-menu.test.tsx` asserts the five labels VERBATIM and asserts the unapproved prototype copy ("Seven dimensions", "The triad", "All ten") is absent. |
| UAT-5: One configured extra yields six entries; a second event with none still yields five | ✅ | Same suite, plus `e2e/p1179-links-navigation.spec.ts` against a real `events.links` row (`[{tag:'tonight',label:'Tonight'}]`). |
| UAT-6: The stake page shows the tag's Points oldest-first, with no search, tag cloud, sort toggle or Share a Story | ✅ | `p1179-stake-surface.test.tsx` asserts the ordering on the ARGUMENT passed to the service (`ascending = true`), not on render order — a client-side reverse would pass a render assertion. Chrome absence asserted by query. |
| UAT-7: A Points-only tag shows no tabs | ✅ | `p1179-stake-surface.test.tsx`. Stories-only also shows none — the rule holds both ways. |
| UAT-8: A tag carrying both Points and Stories shows both tabs | ✅ | `p1179-stake-surface.test.tsx`. |
| UAT-9: Staking updates the count with no full-list reload or loading flash | ✅ | `p1179-no-refetch-on-position.test.tsx` (call count + skeleton absence + source assertion that `handlePointRemoved` never calls `fetchData`) and `e2e/p1179-stake-surface.spec.ts` (observed GET count on `/rest/v1/points` across the click == 0). |
| UAT-10: An attendee who opted OUT at /meet can reach the surface and record positions | ✅ | `e2e/p1179-stake-surface.spec.ts` — room row seeded `opted_in = false`, verified via service role before and after; the position row is read back from `point_positions` as ground truth, and the opt-out is unchanged by staking. |
| UAT-11: Transcribe reaches the working transcription room; Start a Clarity Session reaches /live | ✅ | `e2e/p1179-links-navigation.spec.ts`. |
| UAT-12: The button persists across destinations with no Back hop | ✅ | Same spec — asserted by ARRIVING at `/stake/cmp7?event=…` and finding the button there, then hopping straight to cmp3. Not by inspecting the href. |
| UAT-13: A bare /stake/:tag renders the cut-down feed with no button and no event context | ✅ | Same spec, plus the unit suite's empty-DOM assertion. |
| UAT-14: A route outside the room renders its nav unchanged; the button does not leak | ✅ | `p1179-nav-containment.test.tsx` — 12 outside routes render nothing, and the same component DOES render inside the room (so the assertion has teeth). |
| UAT-15: The nav centre slot is untouched and still absolutely positioned; /terms unaffected | ✅ | `p1179-nav-containment.test.tsx` — slot still present, still absolute, and nothing from this change was put in it. |
| UAT-16: An entry with an external or protocol-relative URL is rejected or ignored | ✅ | `p1179-entry-safety.test.ts` — 14 hostile shapes attempted (`https://`, `//`, `javascript:`, `data:`, traversal, whitespace); all dropped, and every produced path asserted internal and not protocol-relative. |
| UAT-17: The migration applies to test; `links` defaults to `[]` on every existing row | ✅ | Applied to the test DB 2026-08-28. `e2e/integration/p1179-events-links-column.spec.ts` asserts zero NULL rows, the `{tag,label?}` round-trip, and that the `jsonb_typeof = 'array'` CHECK is actually ENFORCING (a scalar/object/string write is rejected) — which reading the .sql cannot establish. |
| UAT-18: P1161 no longer calls the filtered feed the room's URL and names the Links menu | ✅ | Three pointers rewritten; the contract's own grep row passes (exit 0). |
| UAT-19: The control introduces no new colour, radius or control height | ✅ | `p1179-design-system-reuse.test.ts` — no raw hex literal in the component, no arbitrary `rounded-[…]`, no height token other than the shared 44px, and it imports `ANSWER_BUTTON_CLASS` from `meeting-terms-page` plus the existing `ui/drawer` primitive. |
| UAT-20: The open sheet matches the approved reference in density and structure | ⏭️ HUMAN-ONLY | Blind-reviewer round; see `features/verification/p1179/`. Comparison against the approved 2026-08-28 prototype is a judgement the loop must not make about its own build. |
| UAT-21: The four room screens still read as the same product with the button added | ⏭️ HUMAN-ONLY | Same. Requires renders at 320/375/desktop, sheet closed and open, plus the stake empty state. |
| UAT-22: The entry labels are the right words to say out loud to a room of strangers | ⏭️ HUMAN-ONLY | Contract row HUM-1 — explicitly the founder's call. The loop only enforced that no agent-authored copy reaches the screen. |

## Manual-Only Scenarios

| Scenario | Why manual |
|----------|-----------|
| The host says "open Links, tap cmp7" to a live room and it works one-handed while standing | The design's actual claim. Only a real event tests it; event #1 is P1161's run-of-show. |
| Whether anyone reaches for Transcribe unprompted during the fishbowl | The spec ACCEPTS this risk and calls the outcome itself a finding. Not assertable. |
