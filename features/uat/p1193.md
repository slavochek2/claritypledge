# UAT: P1193 — Clarity Groups: the rename, the organizer on screen, the last-organizer leave guard

> Auto-generated by /verify — edit freely.
> Environment: LOCALHOST (feature/p1193-clarity-groups, http://localhost:5200)
> Run: 2026-08-31. Functional 88/88 (39 unit + 49 e2e, all `--workers=1`). Visual: pass.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Organizer sees their role named in the header | pass | `p1193-org-header.test.tsx` — badge renders "Organizer"; classes copied verbatim from the Members-tab badge so the two readings of one fact cannot drift. |
| UAT-2: Plain member and signed-out see no role badge | pass | Same file, two tests. Also asserts no user-visible string anywhere in the header contains "lead" — the founder's own session word, explicitly banned from the product. |
| UAT-3: Co-create absent from group events list | pass | `p1060-org-scoped-events` D4(1/3), `p1010-organizations`, plus a source assertion that the guard wraps ONLY the Co-create link — a guard around the whole block would strip Host Event too. |
| UAT-4: Co-create still present on standalone /events list | pass | `p1060-org-scoped-events` (new counter-case) + `p1193-no-group-hosting` UAT-5. This is the false-positive half of change (2) — the half nothing else would catch. |
| UAT-5: Hosting with no group works end to end, org_id NULL | pass | `p1193-no-group-hosting` 3/3. Drives `/events/new` to submit and reads the stored row: `org_id IS NULL`, `host_id` = creator, and the event comes back out on the list. The spec names a regression here as the most expensive possible outcome. |
| UAT-6: Sole organizer cannot reach Leave, sees one reason line | pass | `p1193-org-header.test.tsx` — the line is the founder's verbatim string, and Leave is WITHHELD, not disabled (a rendered-then-disabled control is the P955 dead-control pattern). |
| UAT-7: Organizer with co-organizer, and any member, can leave | pass | Component test + `p1193-last-organizer-guard`, which also proves the guard RE-ARMS once the co-organizer leaves — that negative control is what distinguishes "counts organizers" from "rejects all organizers". |
| UAT-8: Sole-organizer leave rejected server-side (bypasses UI) | pass | `e2e/integration/p1193-last-organizer-guard.spec.ts` via a user-scoped JWT, the same path `leaveOrganization` uses. A RAISE, not a zero-row delete — zero rows already means "already left" in that function's contract. Row count unchanged after the refusal. |
| UAT-9: Profile and organization deletes still cascade | pass | Both directions, same file. The PROFILE case is the load-bearing one: the organization still exists there, so the "org already gone" stand-aside cannot be what saves it — only `pg_trigger_depth()` can. Exercised, not reasoned about. |
| UAT-10: Cross-group event leakage assertion exists and passes | pass | Answered the founder's open question 1: **it already existed.** `p1060-org-scoped-events` asserts Org B's event is absent from Org A's Upcoming AND Past, with the standalone list as the positive control. Nothing needed adding. |
| UAT-11: EventsList comment no longer claims Co-create travels everywhere | pass | `p1193-source-contract`. A comment contradicting the code is worse than none — the next reader trusts it and re-adds the button. |
| UAT-12: /org, /org/:slug, /org/:slug/join resolve; ?from= survives | pass | `p1193-groups-rename` 9/9, incl. `?from=` on both the group and the join path, and an unknown slug redirecting before reporting not-found. |
| UAT-13: No user-visible "Organization" string remains in src/ | pass | `p1193-source-contract`. **This gate was wrong twice before it was right** — case-sensitive at first (missed 3 real strings), then blind to HTML entities (`hasn&apos;t` reads as code to a `;`-based prose detector). Now proven against a known-good and two known-bad controls. |
| UAT-14: Nav renders one "Groups" item; active on /groups* and /events* | pass | Plus a folder-wide sweep for hardcoded "Events" labels — the desktop top-nav link is hand-written rather than mapped, so the nav-links-only check had missed it. |
| UAT-15: COA v6 titled "Clarity Group Terms", oath body by reference | pass | `coa-versions.test.ts` asserts object IDENTITY with v5, not string equality — a retyped copy would match today and silently diverge on the next oath edit. v4/v5 titles asserted UNCHANGED (the retroactivity guard). |
| UAT-16: terms_version CHECK admits 6, DEFAULT 6, no rows changed | pass | Migration contains no UPDATE and no backfill. The test reads the migration file directly, so a version added in code without the schema change fails the suite instead of failing at runtime. |
| UAT-17: A join after the migration records terms_version = 6 | pass | `p1010-organizations` join flow reads the stored value back after a real UI join. The only end-to-end bind between `CURRENT_COA_VERSION` and the column DEFAULT — they live in different files and nothing else fails if only one ships. |
| UAT-V: Visual pass — desktop, 375px, 320px, 390px | pass | Zero console errors/warnings at load. No overflow, clipping or truncation at any width; single primary CTA per view. **Disclosed:** every screenshot is SIGNED-OUT, so the organizer badge and the blocked-leave line appear in none of them — those states are covered by the 11 component tests, not visually. The 320px capture is an extreme aspect ratio, so fine-grained spacing/contrast there is unverified rather than passed. |

## Notes

- **Independent visual reviewer**: reported FAIL on a stale artifact — the desktop screenshot predated a copy fix while the 375px one postdated it, which reads as a viewport-specific bug. Verified live at 1280px (`document.body.innerText` contains zero occurrences of "organization"/"Organization"); the artifact was replaced. The reviewer's read of the image was correct; the cause was mine.
- **Code review**: 0 HIGH, 0 MEDIUM. One doc inaccuracy (a migration filename four digits off in a comment) verified against the filesystem and fixed.
- **Not run**: the full `e2e/integration/` directory. Out of P1193's blast radius, and running 1109 tests concurrently against the shared test DB produced contention-driven failures that are not evidence either way.
