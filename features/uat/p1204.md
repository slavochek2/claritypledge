# UAT: P1204 — Clarity Groups directory card polish + card-as-link

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: No event-status badge renders on any card | pass | `e2e/p1204-verify.spec.ts`; confirmed visually on `/groups` |
| UAT-2: Clicking anywhere on a card opens that group | pass | Force-click on the decorative "Open" text confirms the stretched-link `::after` pseudo-element is the topmost element there — a real pointer click lands on it |
| UAT-3: Card exposes exactly ONE named link, Enter activates it (a11y) | pass | `card.getByRole('link')` count === 1; keyboard Enter navigates |
| UAT-4: Card shows visible hover state and focus ring | pass | Hover screenshot shows border/shadow emphasis + underline; `has-[a:focus-visible]:ring-2` class present on card |
| UAT-5: Membership badge is not green | pass | Screenshot + class assertion (`bg-muted`/`text-muted-foreground`, no `green`) |
| UAT-6: A group page offers a way back to the directory | pass | "← Back to groups" link on `/groups/cm`, `href="/groups"` |
| UAT-7: Nav icon is not a calendar, no duplicate w/ Partners/My Profile | pass | Source-verified: Groups=LandmarkIcon, Partners=UsersIcon, My Profile=UserIcon (`bottom-nav.tsx`) |
| UAT-8: Members render as a count, not avatars; "Browse all events" stays at bottom | pass | `/^\d+ members?$/` visible, no avatar `img`s in member row; link renders below the card grid |
| UAT-9: Out-of-scope surfaces visually unchanged | pass | Group page tabs/roster/About and nav unaffected — no code diff outside listed surfaces |
| UAT-10: All existing P1060, P1193 and P1010 tests still pass | pass | 52/52 e2e (`p1060-org-directory`, `p1060-org-scoped-events`, `p1060-review-renders`, `p1193-groups-rename`, `p1193-no-group-hosting`, `p1010-organizations`) + 22/22 vitest (`p1060-source-contract`) |
| UAT-11: Regression — avatar row renders with participant data present | pass | Seeded 3 RSVPs on a past org-scoped event; "N have joined events" row renders |
| Visual: desktop pass | pass | Screenshot of `/groups` matches the approved after-state mockup |
| Visual: mobile (390px) | pass | No horizontal overflow (`scrollWidth === innerWidth === 390`); no console errors |
