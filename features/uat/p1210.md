---
p_number: p1210
spec: features/p1210_disagreement_pipeline_objective_and_point_unit.md
created_date: '2026-09-01'
---

# P1210 — UAT scorecard

Seeded by `/goalify` 2026-09-01 with **no results**, deliberately. `goal-gate.sh` CHECK 4 fails
while any row carries no result, so this file is red until the loop fills it. A skip must name a
reason from the whitelist: `NOT-BUILT`, `ENV-UNAVAILABLE`, `HUMAN-ONLY`, `SUPERSEDED`.

One row per Verification Contract row. Record what was actually observed, not that the command ran.

## Test Execution Log

| Scenario | Result | Notes |
|---|---|---|
| VC-1 points-process objective and ten-condition table, every row mapped to a real stage output |  |  |
| VC-2 point unit files on assert+deny, refuses when either side is missing |  |  |
| VC-3 redundancy three-fixture control set — distinct passes, verbatim fails, reworded fails |  |  |
| VC-4 run B regression reproduces 2-of-3 unspanned, 4-of-5 untraced, and the unfilled-count assert fires |  |  |
| VC-5 one gate — new downstream axis stops, sharpening of an approved axis passes |  |  |
| VC-6 counterpart hypothesized from transcript before any search; search-first refused |  |  |
| VC-7 arguer added only against a named new contradiction; filed-of-target reported on shortfall |  |  |
| VC-8 one story per person per point, two-point story caught, no timestamps in prose |  |  |
| VC-9 event run-of-show carries pre-read, per-point staking, stakeable statement |  |  |
| VC-10 no ls on a store; ledger-orphan found and does not stop the run; absent artifact still blocks |  |  |
| VC-11 same-vote three-way gate present, re-cast described as a new run |  |  |
| VC-12 audience-size question fires only below the recorded floor |  |  |
| VC-13 publish Stage 0 and orchestrator input block hold every input; no mid-stage turn |  |  |
| VC-14 star cast FLAGGED, balanced cast not, printed values differ |  |  |
| VC-15 two-artifact seal model implemented |  |  |
| VC-16 every new refusal passes the documented happy path and has been watched to FAIL |  |  |

## Manual-Only Scenarios

| Scenario | Why manual |
|---|---|
| VC-17 blind prose review of the changed skill files | RD-3 — the gate has no non-visual reviewer path; held at `/ship` by a reader given neither the diff nor the rationale |
| The AI safety re-run end to end via `/slava:disagreement:run-pipeline` | The forward acceptance test. Needs a real run and founder gates; blocked until the seal model lands (RD-1) |
| Block 6 timing at 12 min per point | Only an actual event can evaluate objective condition 7 |
