---
p_number: p1210
spec: features/p1210_disagreement_pipeline_objective_and_point_unit.md
created_date: '2026-09-01'
---

# P1210 — UAT scorecard

**Re-seeded 2026-09-03** for the second contract, with **no results**, deliberately. `goal-gate.sh`
CHECK 4 fails while any row carries no result, so this file is red until the loop fills it. A skip
must name a reason from the whitelist: `NOT-BUILT`, `ENV-UNAVAILABLE`, `HUMAN-ONLY`, `SUPERSEDED`.

**Each row names its contract row and the command that decides it** — the pre-repair version did
not, which made CHECK 4 pure self-report with nothing tying a scorecard line to an exit code
(review finding 12). Record what was **observed**, including the printed must-fail verdict, not that
the command ran.

## Test Execution Log

| Scenario | Result | Notes |
|---|---|---|
| DW-1 objective + ten-condition table resolve; bad-owner fixture REJECTED — `npx vitest run src/tests/p1210-objective-table.test.ts` |  |  |
| DW-2 admissibility FILES on assert+deny, REFUSES naming the missing side — `npx vitest run src/tests/p1210-admissibility.test.ts` |  |  |
| DW-3a redundancy: distinct PASS, verbatim repeat FAIL — `npx vitest run src/tests/p1210-redundancy.test.ts` |  |  |
| DW-4 run-B regression reproduces the pinned expectation incl. P3 AMBIGUOUS-PAIR — `npx vitest run src/tests/p1210-run-b-regression.test.ts` |  |  |
| DW-5 unfilled derived=1 vs field=0, assert FIRES; agreeing fixture does not — `npx vitest run src/tests/p1210-unfilled.test.ts` |  |  |
| DW-6 one-gate + sharpen-not-add rules PRESENT; stripped fixture REJECTED — `npx vitest run src/tests/p1210-rule-one-gate.test.ts` |  |  |
| DW-7 transcript-first rule PRESENT; stripped fixture REJECTED — `npx vitest run src/tests/p1210-rule-transcript-first.test.ts` |  |  |
| DW-8 shortfall prints filed-of-planned, at-target prints none — `npx vitest run src/tests/p1210-point-target.test.ts` |  |  |
| DW-9 story-unit rules PRESENT; stripped fixture REJECTED — `npx vitest run src/tests/p1210-rule-story-unit.test.ts` |  |  |
| DW-10 timestamp scan: clean PASS, planted timestamp FAILS naming the span — `npx vitest run src/tests/p1210-story-scan.test.ts` |  |  |
| DW-11 regression guard on the run-of-show; staking-deleted fixture REJECTED — `npx vitest run src/tests/p1210-event-contract.test.ts` |  |  |
| DW-12 store-inspection scan clean; planted inspection FLAGGED with its line — `npx vitest run src/tests/p1210-store-inspection.test.ts` |  |  |
| DW-13 + DW-18 fixture store tree: orphan FOUND and non-blocking, absent artifact still blocks — `npx vitest run src/tests/p1210-store-reconcile.test.ts` |  |  |
| DW-14 regression guard on the same-vote gate; stripped fixture REJECTED — `npx vitest run src/tests/p1210-rule-same-vote.test.ts` |  |  |
| DW-15 above-floor NO-ASK, below-floor ASK naming the metric — `npx vitest run src/tests/p1210-audience-floor.test.ts` |  |  |
| DW-16 Stage 0 and orchestrator blocks hold every input, zero asks between; planted ask FLAGGED — `npx vitest run src/tests/p1210-input-blocks.test.ts` |  |  |
| DW-17 star cast FLAGGED, balanced not, printed value sets differ — `npx vitest run src/tests/p1210-cast-controls.test.ts` |  |  |
| DW-19 seals VERIFY untouched, return TAMPERED naming the seal on one changed character — `npx vitest run src/tests/p1210-seal.test.ts` |  |  |
| DW-20 sweep runs every predicate against must-pass and must-fail; exits non-zero on a missing must-fail — `node scripts/points/verify-all.mjs` |  |  |
| DW-21 zero skipped/todo/only in the p1210 suite, non-zero executed count; it.skip fixture FAILS the scan — `node scripts/points/no-vacuous-tests.mjs p1210` |  |  |
| DW-22 every predicate module called by a skill and imported by a test; test-only module FLAGGED — `npx vitest run src/tests/p1210-two-callers.test.ts` |  |  |
| DW-23 fixture carries zero real surnames and zero verbatim sentences; derivation reproduces it byte-for-byte — `node scripts/points/verify-fixture.mjs` |  |  |

## Manual-Only Scenarios

| Scenario | Why manual |
|---|---|
| DW-3b reworded near-miss verdict | RD-7 — §2 rules exact-string identity cannot decide it and §12's non-goal forbids tuning a threshold. The harness prints the verdict; the founder judges it at `/ship` |
| VC-24 blind prose review of the changed skill files | RD-3 — the gate has no non-visual reviewer path; held at `/ship` by a reader given neither the diff nor the rationale |
| The AI safety re-run end to end via `/slava:disagreement:run-pipeline` | The forward acceptance test. Needs a real run and founder gates; blocked until the seal model lands (RD-1) |
| Block 6 timing at 12 min per point | Only an actual event can evaluate objective condition 7 |
