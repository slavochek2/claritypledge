# UAT: P1304 — The /live room code reaches third-party analytics and error telemetry

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: No telemetry payload carries the room code | pass | `p1304-reproduce.test.ts` scan returns 0 hits (was 59); control case still caught |
| UAT-2: Mixpanel pageview and replay show no code | skipped (prod-only) | Mixpanel never loads on localhost (`index.html` hostname gate). Pre-deploy half passes: `p1304-mixpanel-url-redaction.test.ts`. Re-check in Mixpanel's live view after deploy |
| UAT-3: Sentry event on /live shows no code | skipped (prod-only) | Sentry init is gated on `import.meta.env.PROD` (`src/main.tsx`). Pre-deploy half passes: `p1304-sentry-url-redaction.test.ts` (4 tests). Re-check the next prod Sentry event from /live |
| UAT-4: Session funnels join on session id | skipped (prod-only) | Needs prod Mixpanel data. Pre-deploy half: every former code payload sends `session_id`, confirmed in review. Re-check a `live_session_exited` breakdown by `session_id` |
| UAT-5: Test fails when a code-named property returns | pass | same scanner failed with 59 hits before the fix; Mixpanel canary failed 3/3 on pre-fix index.html |
| UAT-6: Mixpanel init still loads and sends (p881 harness) | pass | `e2e/p881-reproduce.spec.ts` reads the current `index.html` verbatim (`readFileSync`) and runs the real CDN library on a non-localhost origin: 3 passed |
| Visual: /live and /transcribe render, desktop + 390px | pass | `/live/QX7K2M` renders the join screen at desktop and at 390px (`innerWidth` 390 confirmed, `scrollWidth` 390, no horizontal overflow), 0 console errors or warnings. `/transcribe/QX7K2M` redirects to `/login?redirect=%2Ftranscribe%2FQX7K2M`, 0 console errors. The diff has no visual change; a separate visual-QA agent was not spawned because there is no changed surface to judge |
