# UAT: P451 — Story CTA missing on all surfaces

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: CTA visible on point-detail after refresh (position pre-set) | ✅ | CTA shows immediately on load — "Disagrees" position pre-set, button visible without interaction |
| UAT-2: CTA visible on point-detail after staking new position | ✅ | Changed Disagree→Agree, CTA persisted. Also shows when position already exists (multi-story). |
| UAT-3: CTA visible on profile page points tab (position pre-set) | ✅ | All 3 visible point cards (Agrees/Unsure/Disagrees) show CTA on page load |
| UAT-4: CTA links correctly to /chat?from=position&pointId=... | ✅ | URL = /chat?from=position&pointId=83ca6af4-c27a-4b4a-90e6-223df5f64795 |
| UAT-5: No CTA when user has no position | ✅ | Verified by code: showStoryCTA = !!userPosition — null position = no CTA |
