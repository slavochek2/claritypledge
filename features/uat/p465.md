# UAT: P465 — Point card footer — unified row, no actor confusion

Manual validation checklist. Run on staging/test DB after implementation, before `/ship p465`.

**Setup required:**
- **Own profile**: Log in as Slava. Need one point with "Agree" and no story (for D-1/D-2), and one with an existing story (for D-3/D-4).
- **Other profile**: Log in as Slava. Need a point where Slava has a position but no story (VS-1), and one where Slava has both a position and a story (VS-2).
- **Migration check**: Confirm `story_points.author_id` column exists before UAT (`p465-story-points-migration.spec.ts` green).

---

## 1. Own-Profile Duplication Fix

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| D-1 | Own profile, agreed point, no story | CTA row visible: `"Why do you agree? →"` | | |
| D-2 | Own profile, agreed point, no story | Stories row below CTA: `"▷ 0 stories"` | | |
| D-3 | Own profile, agreed point, story exists | NO CTA — unified row: `"▷ 1 story [edit] [delete]"` | | |
| D-4 | Own profile, agreed point, story exists | Story count appears exactly **once** (not duplicated in two rows) | | |

---

## 2. Actor Confusion Fix (No "✓ Agree ·" Prefix)

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| AC-1 | Own profile, any position, with CTA | CTA text: `"Why do you agree? →"` — **no** `"✓ Agree ·"` prefix | | |
| AC-2 | Other profile, viewer has position, no story | CTA: `"Why do you agree? →"` — **no** `"✓ Agree ·"` prefix | | |
| AC-3 | Other profile, stories row | `"▷ N stories by [Alice]"` — **not** `"✓ Agree · N stories by [Alice]"` | | |
| AC-4 | "Disagree" position on own profile | CTA: `"Why do you disagree? →"` (correct copy, no symbol prefix) | | |

---

## 3. Viewer Story Count on Other Profile

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| VS-1 | Other profile, viewer has no story | Stories row: `"▷ N stories by [Alice]"` — no `"· by you"` suffix | | |
| VS-2 | Other profile, viewer HAS a story | Stories row: `"▷ N stories by [Alice] · 1 by you"` | | |
| VS-3 | Other profile, viewer has story → CTA hidden | NO `"Why do you agree? →"` CTA | | |

---

## 4. One Story Per User Per Point

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| OS-1 | Own profile, story exists | CTA absent — no path to create a second story | | |
| OS-2 | Other profile, viewer has story | CTA absent — cannot reach `/chat?from=position&pointId=X` create mode | | |
| OS-3 | `/chat?from=position&pointId=X` when story exists | Opens in **edit mode** (pre-populated content, edit heading) | | |
| OS-4 | DB constraint check | `p465-story-points-migration.spec.ts` all 5 tests green | | |

---

## 5. CTA Row Ordering

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| ORD-1 | Own profile, no story | CTA row renders **above** stories row | | |
| ORD-2 | Other profile, viewer has no story | CTA row renders **above** `"▷ N stories by [Alice]"` row | | |

---

## 6. Regression Checks

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| REG-1 | P451 legacy "Tell your story →" button | Blue button **gone** from all profile surfaces | | |
| REG-2 | Own profile: edit/delete controls | `[edit]` and `[delete]` still accessible when viewer has story | | |
| REG-3 | Feed view (home timeline) | Stories row shows `"▷ N stories"` without owner name — unchanged from P456 | | |
| REG-4 | /live session mode | No CTA during live session — `liveSessionMode` suppression intact | | |
| REG-5 | Stories tab (QuotedPointCard) | Single footer row, no duplication in Stories tab | | |
| REG-6 | Unauthenticated visitor on profile | Stories row visible, no CTA, no position buttons | | |
| REG-7 | Existing Playwright suite | `npm run test:e2e` green — no regressions | | |

---

## 7. Mobile Layout (375px viewport)

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| MOB-1 | Own profile | CTA + stories row — no overflow or wrapping issues | | |
| MOB-2 | Other profile, viewer has story | `"▷ N stories by [Alice] · 1 by you [share] [open]"` — readable | | |
| MOB-3 | CTA tap target | CTA link ≥ 44px height | | |

---

## Test Execution Log

| Tester | Date | Environment | Result |
|--------|------|-------------|--------|
| | | localhost (test DB) | |
| | | staging | |
