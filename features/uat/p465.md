# UAT: P465 — Point card footer — unified row, no actor confusion

Manual validation checklist. Run on staging/test DB after implementation, before `/ship p465`.

**Setup required:**
- **Own profile**: Log in as Slava. Need one point with "Agree" and no story (for D-1/D-2), and one with an existing story (for D-3/D-4).
- **Other profile**: Log in as Slava. Need a point where Slava has a position but no story (VS-1), and one where Slava has both a position and a story (VS-2).
- **Migration check**: Confirm `story_points.author_id` column exists before UAT (`p465-story-points-migration.spec.ts` green).

---

## 1. Own-Profile Duplication Fix

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| D-1 | Own profile, agreed point, no story | CTA row visible: `"Why do you agree? →"` | ✅ | Verified live |
| D-2 | Own profile, agreed point, no story | Stories row below CTA: `"▷ 0 stories"` | ⚠️ | Trigger hidden when 0 stories (filteredStories.length === 0 → button not shown) |
| D-3 | Own profile, agreed point, story exists | NO CTA — unified row: `"▷ 1 story [edit] [delete]"` | ✅ | Fixed: isOwnProfile suppresses "by name" + "· by you"; Pencil/Trash2 buttons added |
| D-4 | Own profile, agreed point, story exists | Story count appears exactly **once** (not duplicated in two rows) | ✅ | No duplication |

---

## 2. Actor Confusion Fix (No "✓ Agree ·" Prefix)

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| AC-1 | Own profile, any position, with CTA | CTA text: `"Why do you agree? →"` — **no** `"✓ Agree ·"` prefix | ✅ | Verified live |
| AC-2 | Other profile, viewer has position, no story | CTA: `"Why do you agree? →"` — **no** `"✓ Agree ·"` prefix | ✅ | Verified live |
| AC-3 | Other profile, stories row | `"▷ N stories by [Alice]"` — **not** `"✓ Agree · N stories by [Alice]"` | ✅ | Verified live |
| AC-4 | "Disagree" position on own profile | CTA: `"Why do you disagree? →"` (correct copy, no symbol prefix) | ✅ | Confirmed via code (getPositionCTACopy) |

---

## 3. Viewer Story Count on Other Profile

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| VS-1 | Other profile, viewer has no story | Stories row: `"▷ N stories by [Alice]"` — no `"· by you"` suffix | ✅ | Verified live |
| VS-2 | Other profile, viewer HAS a story | Stories row: `"▷ N stories by [Alice] · 1 by you"` | ⏭️ | No test data available (no other user with story on a point where Slava also has a story) |
| VS-3 | Other profile, viewer has story → CTA hidden | NO `"Why do you agree? →"` CTA | ✅ | Verified live |

---

## 4. One Story Per User Per Point

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| OS-1 | Own profile, story exists | CTA absent — no path to create a second story | ✅ | Verified live |
| OS-2 | Other profile, viewer has story | CTA absent — cannot reach `/chat?from=position&pointId=X` create mode | ✅ | Verified live |
| OS-3 | `/chat?from=position&pointId=X` when story exists | Opens in **edit mode** (pre-populated content, edit heading) | ✅ | Confirmed at pointId=eb364fc2-ae02-405e-97fb-458af455c7e1 — "Edit your story" heading, pre-populated content |
| OS-4 | DB constraint check | `p465-story-points-migration.spec.ts` all 5 tests green | ✅ | 5/5 pass |

---

## 5. CTA Row Ordering

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| ORD-1 | Own profile, no story | CTA row renders **above** stories row | ✅ | Code: CTA at line 266, footer at line 288 (quote pattern) |
| ORD-2 | Other profile, viewer has no story | CTA row renders **above** `"▷ N stories by [Alice]"` row | ✅ | Code: same ordering in both quote and feed patterns |

---

## 6. Regression Checks

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| REG-1 | P451 legacy "Tell your story →" button | Blue button **gone** from all profile surfaces | ✅ | Verified live |
| REG-2 | Own profile: edit/delete controls | `[edit]` and `[delete]` still accessible when viewer has story | ✅ | Fixed: Pencil + Trash2 icon buttons in quote-pattern footer, conditional on isOwnProfile && viewerStoryCount > 0 |
| REG-3 | Feed view (home timeline) | Stories row shows `"▷ N stories"` without owner name — unchanged from P456 | ✅ | Code: feed pattern line 404 — `profileOwner ? \` by ${name}\` : ''` — no name when no profileOwner; no "by you" suffix in feed pattern |
| REG-4 | /live session mode | No CTA during live session — `liveSessionMode` suppression intact | ✅ | Code: both patterns check `!liveSessionMode` before rendering CTA |
| REG-5 | Stories tab (QuotedPointCard) | Single footer row, no duplication in Stories tab | ✅ | Verified live |
| REG-6 | Unauthenticated visitor on profile | Stories row visible, no CTA, no position buttons | ✅ | Code: `userPosition` null when no currentUserId → no CTA; `currentUserId` falsy → no buttons; stories row visible regardless |
| REG-7 | Existing Playwright suite | `npm run test:e2e` green — no regressions | ⚠️ | Smoke 2/2 ✅; unit 917/917 ✅; OS-4 migration 5/5 ✅; functional E2E Flow 3/4 hit Supabase Auth rate limit during test user setup (infra issue, not feature regression) |

---

## 7. Mobile Layout (375px viewport)

| ID | Scenario | Expected | ✓/✗ | Notes |
|----|----------|----------|-----|-------|
| MOB-1 | Own profile | CTA + stories row — no overflow or wrapping issues | ⏭️ | Browser screenshots unavailable this session (blank captures) |
| MOB-2 | Other profile, viewer has story | `"▷ N stories by [Alice] · 1 by you [share] [open]"` — readable | ⏭️ | Browser screenshots unavailable this session |
| MOB-3 | CTA tap target | CTA link ≥ 44px height | ⏭️ | Browser screenshots unavailable this session |

---

## Test Execution Log

| Tester | Date | Environment | Result |
|--------|------|-------------|--------|
| Claude (verify agent) | 2026-03-02 | localhost:5001 (test DB) | ❌ 2 failures — D-3, REG-2 (edit/delete missing from PointCardWithLinks) |
| Claude (verify agent) | 2026-03-02 | code analysis + unit tests | ✅ D-3 + REG-2 fixed — 917/917 unit tests, build pass |
| | | staging | |
