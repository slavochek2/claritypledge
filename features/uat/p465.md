# UAT: P465 — Point card footer redesign

Manual checklist. Run after implementation ships to staging/feature branch.

**Test accounts needed:**
- Account A (Viewer): a verified account that has taken positions on points from other users.
- Account B (Owner/Alice): a different account whose profile Viewer will browse.
- Use a fresh browser session (no cached session) for anonymous tests.

---

## Pre-flight

Before running UAT, verify automated tests pass:
```bash
npx playwright test e2e/p465-smoke.spec.ts
npx playwright test e2e/p465-point-card-footer.spec.ts
npx playwright test e2e/a11y/p465-accessibility.spec.ts
npx playwright test e2e/integration/p465-author-id-migration.spec.ts
npm test src/tests/viewer-story-count.test.ts
```

---

## Problem 1: Own-profile duplication (fixed)

### Own profile — no story

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P1-A | Log in as Viewer. Go to your own profile (`/[your-slug]`). Find a point you have agreed on but have NOT written a story for. | Exactly ONE story-count row: "▷ 0 stories [share] [open]". CTA "Why do you agree? →" visible above the stories row. NO second row with the same count. | | |
| P1-B | Same as above but with a disagree position. | CTA reads "Why do you disagree? →". One story-count row. No duplication. | | |
| P1-C | Same as above but with unsure position. | CTA reads "Why are you unsure? →". One story-count row. No duplication. | | |

### Own profile — story exists

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P1-D | Log in as Viewer. Go to your own profile. Find a point you have agreed on AND you have one story linked to it. | Single unified row: "▷ 1 story [edit] [delete] [share] [open]". NO CTA row ("Why do you agree?" must NOT appear). NO duplicate count row. | | |
| P1-E | Confirm the count says "1 story" (singular) not "1 stories". | "1 story" (singular). | | |

---

## Problem 2: Actor confusion (fixed)

### Other profile — no actor prefix

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P2-A | Log in as Viewer. Go to Account B's profile (`/[alice-slug]`). Find a point you have agreed on. | CTA text reads ONLY "Why do you agree? →". NO "✓ Agree ·" prefix before the CTA text. | | |
| P2-B | Same but with disagree position. | CTA reads only "Why do you disagree? →". No "✗ Disagree ·" prefix. | | |
| P2-C | Same but with unsure position. | CTA reads only "Why are you unsure? →". No "~ Unsure ·" prefix. | | |

### CTA ordering — CTA before stories row

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P2-D | On Alice's profile, find a point with the CTA visible (you have position, no viewer story). Visually scan the footer area. | CTA ("Why do you agree? →") appears ABOVE the "▷ N stories by Alice" row — not after it. The CTA is sandwiched between the position buttons (top) and the stories row (bottom). | | This is the key actor-confusion fix: CTA no longer reads as Alice's stance |
| P2-E | On your own profile, find a point with the CTA visible. Confirm same ordering. | CTA above stories row ("▷ 0 stories"). | | |

---

## Problem 3: Viewer story count on other profiles (fixed)

### Viewer count appears when viewer has a story

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P3-A | As Viewer, navigate to Alice's profile. Find a point you have agreed on AND you have an existing story linked to it. | Single footer row: "▷ N stories by Alice · 1 by you [share] [open]". NO CTA (viewer already has a story — create path suppressed). | | Requires data pipeline fix (D1 in arch) |
| P3-B | Verify "· 1 by you" text color is readable (not faded). | Contrast ≥ 4.5:1 — `text-gray-600` or darker. | | |
| P3-C | Confirm no CTA appears when "· 1 by you" is shown. | No "Why do you agree?" anywhere on that point card. | | |

### Viewer has no story — CTA shows correctly

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| P3-D | As Viewer on Alice's profile, find a point you have a position on but NO story for. | CTA visible above stories row. "· N by you" NOT present. | | |

---

## 1-story-per-user constraint

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| S-A | On your own profile, find a point where you have a story. Confirm the CTA is absent. | No CTA. No "+ add story →" button. Single stories row with edit/delete icons. | | UI prevents second story |
| S-B | Click the Edit icon on a story. Verify navigation goes to `/chat?from=position&pointId=X` and the page opens in **edit mode** (pre-populated with existing story content, NOT a blank new story). | Chat page opens with existing story pre-filled. Heading should indicate "Edit" context (not "Tell your story"). | | D3 edit-mode routing |
| S-C | As a second check: try navigating directly to `/chat?from=position&pointId=X` for a point where you already have a story. | Page opens in edit mode, pre-populated. NOT a blank new story. | | |

---

## Edit/delete controls on own profile

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| E-A | Own profile, story exists. Click the edit (pencil) icon. | Navigates to `/chat?from=position&pointId=X&storyId=Y` or `/chat?from=position&pointId=X` (per arch Decision 3). Opens in edit mode with existing story. | | |
| E-B | Own profile, story exists. Click the delete (trash) icon. | Confirmation dialog appears: "Delete this story?" with "Delete" and "Cancel" buttons. | | |
| E-C | In the delete confirmation, click Cancel. | Dialog closes. Story count unchanged. No deletion. | | |
| E-D | In the delete confirmation, click Delete. | Story deleted. Footer transitions: stories row count drops (e.g. "1 story" → "0 stories"). CTA appears: "Why do you agree? →". | | |

---

## P451 dead code removed

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| DC-A | On your own profile, any point with a position. | Generic "Tell your story →" button does NOT appear anywhere on the page. | | P451 legacy button removed |

---

## Surfaces NOT changed (regression)

| ID | Surface | Steps | Expected | Result |
|----|---------|-------|----------|--------|
| R-A | Point detail page `/point/:id` | Navigate to a point you have agreed on. | CTA "Why do you agree? →" still appears (P456 behavior preserved). No actor prefix. | |
| R-B | /live session | Start a /live session, take a position on a point card inside the session. | No CTA appears (liveSessionMode suppresses it — unchanged). "Available after the session" hint may appear if /live implements the disabled state. | |
| R-C | Stories tab (QuotedPoint) | On your own profile, go to the Stories tab. Open a story that has a linked point. Look at the quoted point's footer. | Footer behavior per P456 spec — not changed by P465. | |
| R-D | Feed / home page | Browse the feed. Find a point card with a position taken. | Feed footer behavior unchanged (two-row structure acceptable in feed — P465 explicitly out of scope for feed). | |

---

## Zero stories edge cases

| ID | Steps | Expected | Result | Notes |
|----|-------|----------|--------|-------|
| Z-A | Own profile, position taken, no story. | Stories row shows "▷ 0 stories [share] [open]". Not hidden — 0 is informative. | | |
| Z-B | Other profile, owner has zero stories. Alice has 0 stories on a point. Viewer has a position. | Stories row: "▷ 0 stories by Alice [share] [open]". CTA above it. | | |
| Z-C | Other profile, Alice has 0 stories, viewer has a story. | Stories row: "▷ 0 stories by Alice · 1 by you [share] [open]". No CTA. | | |

---

## Accessibility spot-checks

| ID | Check | Expected | Result |
|----|-------|----------|--------|
| A-1 | Tab to CTA button on own profile. Press Enter. | Focus lands on CTA button. Enter navigates to /chat. Focus ring visible. | |
| A-2 | Tab to Edit button on own profile (story exists). | Focus visible on pencil icon. aria-label present (check via browser DevTools). | |
| A-3 | Tab to Delete button. Press Enter. | Delete confirmation dialog appears. Focus trapped in dialog. Escape closes it. | |
| A-4 | Screen reader (VoiceOver/NVDA) on CTA: reads "Tell your story about your agreement" (not "checkmark Agree Tell your story"). | No position symbol prefix in screen reader announcement. | |

---

## Plural/singular count labels

| ID | Check | Expected | Result |
|----|-------|----------|--------|
| PL-1 | 0 stories linked | "0 stories" (not "0 story") | |
| PL-2 | 1 story linked | "1 story" (singular) | |
| PL-3 | 2 stories linked | "2 stories" | |
| PL-4 | "· 1 by you" viewer count | "1 by you" (not "1 stories by you") | |
