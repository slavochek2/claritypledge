# UAT: P493 — PWA Install Prompts

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Settings page shows install card | ✅ | App section with icon, title, fallback text. Clean layout. |
| UAT-2: Install card shows "installed" state when in standalone | ⏭️ | Requires standalone mode — verify after installing PWA on real device |
| UAT-3: Celebration dialog has install link (no "Start /live") | ✅ | Source verified: old link removed, CelebrationInstallLink imported |
| UAT-4: Session end shows install banner (registered user) | ⏭️ | Requires two-party /live session end — verify manually post-deploy |
| UAT-5: Session end shows signup CTA for guest (no install) | ⏭️ | Guest flow unchanged (P396), no install banner added for guests — code-verified |
| UAT-6: iOS drawer shows manual instructions | ⏭️ | Requires iOS Safari — verify on real iOS device post-deploy |
| UAT-7: Dismiss sets 30-day cooldown in localStorage | ✅ | Key set correctly, settings card persists after dismiss |
| UAT-8: Unsupported browser shows fallback text | ✅ | Verified on settings page — shows "Open in Chrome or Safari to install." |
