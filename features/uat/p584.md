# UAT: P584 — Session End Screen Redesign

> Auto-generated by /verify — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Unified CTA "Start a Clarity Session" for both roles | ✅ | Live verified: host shows "Start a Clarity Session". Code: no role branch. Tests: 19 pass. |
| UAT-2: CTA hidden during upload | ✅ | Code + unit tests verified. Cannot trigger upload on localhost (prod-only recording). |
| UAT-3: Upload progress for ALL users incl guests | ✅ | Code: no `!isGuest` gate. Unit tests pass for both guest and registered. |
| UAT-4: Nav guard dialog during upload | ✅ | Code: popstate+pushState pattern. Dialog with "Stay on this page" / "Leave anyway". Cannot trigger on localhost. |
| UAT-5: Transcript msg only when rounds > 0 | ⬜ | BLOCKED: Cannot complete a full round via automation (requires interactive rating on both sides). User reported issue — needs manual verification with debug log. |
| UAT-6: No transcript msg when rounds = 0 | ✅ | Live verified on BOTH tabs: no transcript message with 0 rounds. |
| UAT-7: Upload failure shows subtle message | ✅ | Code + unit test: "Recording could not be saved" shown. Cannot trigger on localhost. |
| UAT-8: Guest conversion msg when rounds > 0 | ⬜ | BLOCKED: Same as UAT-5 — needs rounds > 0 which requires interactive rating. |
| UAT-8b: Guest CTA when rounds = 0 | ✅ | Live verified: guest tab shows "Create Free Account" + "Already have an account? Log in". No transcript mention. |
| UAT-9: Guest upload progress same as logged-in | ✅ | Code: same upload block, no guest exclusion. Unit tests pass. |
| UAT-10: No Home text link on end screen | ✅ | Live verified: no "Home" or "Back to Home" on either tab. |
| UAT-11: Title logic "Session ended" / "{Name} has left" | ✅ | Live verified: host sees "Session ended". Guest sees "Session ended". Unit tests cover "{Name} has left" variant. |
| UAT-12: Navigate away and return shows start view | ✅ | Code: React state resets on unmount. handleStartNewAfterPartnerLeft resets all flags. |
