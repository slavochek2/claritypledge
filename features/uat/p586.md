# UAT: P586 — Visibility & Privacy Foundation

> Auto-generated by /generate-tests — edit freely.

## Pre-requisites
- Migration applied (`supabase db push` or `./scripts/migrate.sh`)
- At least one public story with linked points exists
- Test user account available

## Scenarios

### UAT-1: `shared` option removed from story creation
**Given:** User is on create-story-page or story guide chat
**When:** User opens visibility selector
**Then:** Only "Public" and "Private" options appear. No "Shared" option.
**Verify:** Visual inspection on create-story-page + story guide chat save flow

### UAT-2: `shared` stories migrated to public
**Given:** Stories that previously had `visibility = 'shared'` exist
**When:** User views them on profile or feed
**Then:** They display with Globe (public) badge. No "Shared" badge anywhere in app.
**Verify:** DB query `SELECT count(*) FROM stories WHERE visibility = 'shared'` returns 0

### UAT-3: Visibility dropdown removed from story detail page
**Given:** User is the author of a story
**When:** User views their story detail page
**Then:** Visibility shows as static badge (lock/globe), no dropdown chevron, not clickable
**Verify:** Visual inspection on story detail page as author

### UAT-4: Visibility dropdown removed from profile page
**Given:** User views their own profile
**When:** User looks at their story cards
**Then:** Visibility shows as static badge, same as non-author view. No dropdown.
**Verify:** Visual inspection on profile page

### UAT-5: Visibility read-only in story guide chat edit mode
**Given:** User edits an existing story in story guide chat
**When:** The save/visibility area renders
**Then:** Current visibility shown as non-interactive pill. Helper text: "Visibility cannot be changed after creation."
**Verify:** Visual inspection in story guide chat edit flow

### UAT-6: Point cards show visibility badge
**Given:** A public point exists linked to a story
**When:** User views the point card (on story detail, profile, feed)
**Then:** Globe icon appears in point card header area
**Verify:** Visual inspection across story detail, profile points tab, feed

### UAT-7: Private points not visible to non-authors (DB-seeded)
**Given:** A private point is inserted via DB (no P586 UI creates private points)
**When:** Another user queries for that point via API
**Then:** Point is not returned (RLS blocks)
**Verify:** Integration test p586-visibility-privacy.spec.ts section 3

### UAT-8: Cross-visibility constraint blocks public story → private point
**Given:** A public story and a private point exist (DB-seeded)
**When:** Attempting to INSERT into story_points (public story → private point)
**Then:** INSERT is rejected by trigger
**Verify:** Integration test p586-visibility-privacy.spec.ts section 4

### UAT-9: Story visibility cannot be changed after creation
**Given:** A story exists with visibility = 'public'
**When:** Attempting to UPDATE visibility to 'private'
**Then:** Update is rejected by trigger
**Verify:** Integration test p586-visibility-privacy.spec.ts section 7

### UAT-10: Amber border on private cards (DB-seeded)
**Given:** A private story or point exists (DB-seeded)
**When:** Author views the card
**Then:** Card has amber left border instead of blue/slate. Lock icon in amber-600.
**Verify:** Visual inspection (requires DB-seeded private content)

### UAT-11: Public cards unchanged
**Given:** Existing public stories and points
**When:** User views feed, profile, story detail
**Then:** All cards look exactly as before (blue border for stories, slate for points, globe icon)
**Verify:** Visual regression check — screenshot comparison with pre-P586 state
