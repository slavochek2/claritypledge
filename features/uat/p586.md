# UAT: P586 — Visibility & Privacy Foundation

> Auto-generated by /generate-tests — edit freely.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: `shared` removed from creation selector | ⬜ | Chrome unavailable — manual visual check needed |
| UAT-2: `shared` stories migrated to public | ✅ | DB rejects `shared` enum value — migration applied |
| UAT-3: Visibility dropdown removed from story detail | ⬜ | Chrome unavailable — manual visual check needed |
| UAT-4: Visibility dropdown removed from profile | ⬜ | Chrome unavailable — manual visual check needed |
| UAT-5: Read-only visibility in chat edit mode | ⬜ | Chrome unavailable — manual visual check needed |
| UAT-6: Point cards show visibility badge | ⬜ | Chrome unavailable — manual visual check needed |
| UAT-7: Private points RLS blocks non-authors | ✅ | Integration test passes (21/24 pass, 1 test data collision not feature bug) |
| UAT-8: Cross-visibility constraint blocks public→private | ✅ | Integration test passes |
| UAT-9: Story visibility immutability trigger | ✅ | Integration test passes |
| UAT-10: Amber border on private cards | ⬜ | Chrome unavailable — needs DB-seeded private data + visual check |
| UAT-11: Public cards unchanged | ⬜ | Chrome unavailable — manual visual regression check needed |

## Pre-requisites
- Migration applied (`supabase db push` or `./scripts/migrate.sh`) ✅
- At least one public story with linked points exists
- Test user account available
