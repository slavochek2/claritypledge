# UAT: P745 — Letter-hosted /live injection with pause/resume

> Generated by /generate-tests. Edit freely to mark results.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Author trigger "Start Clarity Live now" visible on per-recipient surface | | |
| UAT-2: Author trigger disabled with tooltip "Invite already pending" when invite exists | | |
| UAT-3: Receiver sees banner in realtime when author triggers invite | | |
| UAT-4: Banner shows exact copy: "{senderName} is inviting you to Clarity" | | |
| UAT-5: Banner shows Join (primary) and Later (secondary) buttons | | |
| UAT-6: Later dismisses banner locally; invite still findable in inbox | | |
| UAT-7: Join opens /live session; letter reader remains mounted beneath overlay | | |
| UAT-8: Letter reader state (story index) survives /live round-trip | | |
| UAT-9: Completing /live shows "Welcome back — continuing your letter" | | |
| UAT-10: Completing /live returns receiver to saved story position | | |
| UAT-11: Author cancel → banner disappears on receiver side in realtime | | |
| UAT-12: Second author trigger reuses/resets existing invite row (no duplicate) | | |
| UAT-13: Unverified guest on public letter sees NO injection banner | | |
| UAT-14: P703 inbox invite flow unaffected (regression) | | |
| UAT-15: GravatarAvatar shows inviter photo + pledge ring correctly in banner | | |

## Manual-Only Scenarios

| Scenario | Why manual |
|----------|-----------|
| Letter reader scroll position survives /live overlay mount | Scroll position is session memory, not DOM state — browser-only |
| Banner GravatarAvatar visual rendering (photo vs initials fallback) | Pixel-level avatar rendering requires visual review |
| Mobile: banner + overlay layout at 375px viewport | Touch interaction + overflow behavior |
| /live overlay back-button behavior (back button should not dismiss, only /live controls do) | Browser navigation UX — Playwright cannot fully simulate back-swipe |
| Focus trap inside /live overlay (tab should not reach underlying letter) | Complex focus management — manual keyboard walk required |

## Acceptance Criteria Status

- [ ] Author can start /live for an identified registered recipient from a per-recipient surface
- [ ] Receiver sees the banner while actively reading the letter (realtime, via `useOpenLiveInvite`)
- [ ] Accept → /live runs → return to letter at saved position
- [ ] Defer → banner dismisses locally; invite persists
- [ ] Cancel → banner disappears on receiver side in realtime
- [ ] Exactly one outstanding invite per delivery
- [ ] Regression: inbox-based invite flow (P703) continues to work
- [ ] Unverified guest sees NO banner (structural gate)
