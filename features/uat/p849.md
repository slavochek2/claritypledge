# UAT: P849 — Letter reveal dwell instrumentation

> Post-deploy smoke test. /verify auto-generated.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Prod homepage loads — no React crash, no console errors at boot | pass | Only LogRocket script-load warn (3rd-party network), unrelated to p849 |
| UAT-2: `/letter/*` route renders without runtime error | pass | `/letter/st1` shortcode → letter 7bd0d109-f001-43ff-9910-b56459f45671. Cover → Open → point-engage → submit Agree → point-revealed all rendered correctly. No regression. |
| UAT-3: `letter_reveal_viewed` event fires with correct properties in Mixpanel | pass | Verified via Network capture (reqid=279). Properties: letter_id=7bd0d109-..., stage_type=`anti-point`, stage_index=1, time_to_next_click_ms=36296, gap=5 (Agree=+2 minus Disagree=-3, signed math correct). Mixpanel response: `{"error":null,"status":1}`. |
| UAT-4: No new Sentry errors related to letter rendering in first 10 min post-deploy | pass | Only Sentry envelope POST was the initial app-boot ping (reqid=220). Three console errors observed are all pre-existing CSP violations (LogRocket CDN, Mixpanel sourcemap, blob worker) — unrelated to p849. |

## Verdict: Ready to ship — already shipped + verified on prod.

Evidence:
- Mixpanel event payload: `event=letter_reveal_viewed`, all 5 properties correct, signed gap math validated (reader Agree=+2, author Disagree=-3, gap=+5).
- No console errors introduced by p849.
- No new Sentry events from letter rendering.
- Letter flow renders identically to pre-change (no layout regression — wrapper-div revert successful).
