# UAT: P860 — Story-rate centering above the pinned drawer

> /verify run in LOCALHOST mode against the w1 branch dev server (port 5100).
> Reached the real story-rate phase via the preview route (`/letter/:docId/preview`),
> which renders the real LetterFlowContent (the reading-page route stalls at the
> cover in the seeded Playwright harness — orthogonal to P860). Regression test:
> `e2e/p860-verify.spec.ts`.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: Short story, desktop — card centered, no top-strand | PASS | Live: 9.5% off region-center (gapTop 302 / gapBottom 188), clearly centered not stranded. `p860-live-desktop.png` |
| UAT-2: Short story, mobile 390 — centered, no dead band | PASS | Live: card visible + centered, no horizontal overflow. `p860-live-mobile.png`. Landscape-phone: CSS-contract harness (top-aligns + scrolls) |
| UAT-3: Long story — top-aligns + scrolls, no clip, fade+chevron | PASS (CSS-contract) | Verified via faithful CSS harness at 4 viewports; live run used a short story. fade+chevron logic unchanged by P860 |
| UAT-4: Drawer (question + 0–10 + Continue) visible without scrolling | PASS | Live, desktop + mobile |
| UAT-5: Other phases (point-engage/revealed/remaining-*/story-revealed) unchanged | PASS (code+tests) | Only the `isStoryRate` branch added to the wrapper ternary; 21 letter tests green |
| UAT-6: Existing P852 letter tests pass, no regression | PASS | tsc clean; p777 5/5 + p745/p772/p778/p734 16/16 |
| UAT-7: No flicker/jump on mount/resize as drawerHeight settles | PASS (partial) | No visible jump in the live preview run; static-CSS approach. Full resize matrix not exhaustively driven |

## Findings (blind QA, triaged)

- "Card not centered / sparse" (desktop + mobile): the accepted ultra-short-no-image edge case — measured 9.5% off center (centered, not stranded). Negligible with a real 50vh image.
- Bottom-bar edge rendering, scale cell widths, label wrap, ~30px touch targets: all in FixedBottomBar / ComprehensionRatingCard — out of P860 scope, pre-existing.

**Verdict: Ready to ship** (core centering live-verified; peripheral ACs verified via CSS-contract / code-scope / passing tests, noted above).
