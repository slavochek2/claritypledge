# UAT: P952 — Reveal-moment response CTAs + author responses gate

> Generated by /generate-tests — edit freely.
> **Status:** Pre-/dev (components not yet built). Run after /dev completes.

---

## UAT-1: Author sets responses_mode at seal — private letter path

### UAT-1.1: responses_mode control visible at seal (private doc)
**Given:** Author has a private (one-to-one) letter in draft with at least one story
**When:** Author navigates to the Letter Review screen (seal step)
**Then:** A "Responses" control is visible with options: Off, Invite (default selected), and Push (disabled/"with P948")
**Verify:** E2E — visual QA, `features/uat/p952.md`

### UAT-1.2: Author selects "Off" and seals — letter persists off
**Given:** Author on Letter Review screen with default Invite selected
**When:** Author changes to "Off" then clicks "Seal & send"
**Then:** Letter is sealed; `clarity_letters.responses_mode = 'off'`; reading flow is advance-only
**Verify:** Integration — `e2e/integration/p952-seal-responses-mode.spec.ts`; DB query confirms `responses_mode='off'`

### UAT-1.3: Author seals with Invite selected — reading flow shows CTAs
**Given:** Author on Letter Review screen with "Invite" selected (default)
**When:** Author clicks "Seal & send"
**Then:** Letter sealed with `responses_mode='invite'`; receiver's reading flow shows "Explain back" at story-revealed
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (invite mode tests)

---

## UAT-2: Author sets responses_mode at seal — public/one-to-many letter path

### UAT-2.1: Public letter gets LetterSealConfirmCard (new step)
**Given:** Author composes a public (one-to-many) letter and completes the prediction walk
**When:** Prediction walk completes
**Then:** Instead of silent auto-seal, a "LetterSealConfirmCard" appears with the Responses control and an explicit "Send" button — the author is not dropped directly to "sent"
**Verify:** Manual; E2E once seal-confirm phase is built

### UAT-2.2: Public author sets "Off" and sends
**Given:** Author on LetterSealConfirmCard, selects "Off"
**When:** Author clicks "Send"
**Then:** Letter sealed with `responses_mode='off'`; reading flow is advance-only for all token readers
**Verify:** Integration — `e2e/integration/p952-seal-responses-mode.spec.ts`; DB confirms `responses_mode='off'`

---

## UAT-3: Receiver reading flow — story-revealed, invite mode

### UAT-3.1: "Explain back" is the primary CTA at story-revealed (invite)
**Given:** Authenticated receiver reading an invite letter, reaches story-revealed phase
**When:** Receiver sees the reveal (gap card shown)
**Then:** Bottom bar shows two CTAs: "Explain back what you understood" as the prominent blue primary pill; "Skip to [dynamic target]" as a visibly-lighter secondary/ghost below it. Only one blue pill, one ghost pill — no two competing blue CTAs
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (story-revealed CTA hierarchy test); visual QA at 375px, 320px, desktop

### UAT-3.2: "Skip to..." secondary is always reachable (AC #8)
**Given:** Receiver at story-revealed in invite mode
**When:** Receiver sees the two-CTA bar
**Then:** The skip/advance secondary CTA is visible and tappable — never hidden behind safe-area inset; never requires interaction with the primary first
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (AC#8 skip-path tests); manual check at 320px viewport

### UAT-3.3: Tapping "Explain back" opens capture Dialog
**Given:** Receiver at story-revealed, sees both CTAs
**When:** Receiver taps "Explain back what you understood"
**Then:** The gap card stays visible behind; the capture Dialog opens (tap = dwell gate, Dialog = action gate); recording idle state shown
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (capture Dialog open test)

---

## UAT-4: Capture Dialog cancel — no dead-end loop (AC #3)

### UAT-4.1: Cancelling Dialog promotes advance to primary
**Given:** Receiver has opened the capture Dialog at story-revealed
**When:** Receiver cancels the Dialog (tap away / cancel button / Escape)
**Then:** Dialog closes; the response CTA downgrades to secondary or link; the advance becomes the primary — receiver can move forward without being forced to explain back
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (AC#3 cancel test)

---

## UAT-5: point-revealed — advance primary, "Add a story" quiet inline

### UAT-5.1: Bottom bar at point-revealed has ONE primary (advance), inline link below
**Given:** Authenticated receiver at point-revealed in invite mode
**When:** Receiver sees the point reveal card (author's position + gap shown)
**Then:** Bottom bar shows one primary CTA (advance — "Next point" / "Next story"); ABOVE or INSIDE the card a small "Add a story" inline text link — NOT a second blue pill, NOT in the bottom bar as a peer
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (point-revealed test); visual QA: one primary only

### UAT-5.2: Contextual heading on "Add a story" link
**Given:** Receiver has set a position (e.g. "slightly disagrees") at point-engage
**When:** Receiver sees point-revealed card
**Then:** A small heading reads "Explain why you slightly disagree" (dynamic to their position) above the "Add a story" link — answers "why would I add a story here"
**Verify:** Manual; [FOUNDER DECISION: exact wording confirmed in UAT]

---

## UAT-6: off mode — advance-only, no affordances anywhere

### UAT-6.1: off letter reading flow has no response CTAs
**Given:** Receiver reading an off-mode letter (any authenticated state)
**When:** Receiver passes through all reveal phases (point-revealed, story-revealed, remaining-point-revealed)
**Then:** At no phase does "Explain back", "Add a story", or any response affordance appear; each phase shows only the advance CTA — identical to pre-P904 behavior
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (off mode tests)

### UAT-6.2: off letter results page has no response affordances
**Given:** Receiver has completed an off-mode letter and navigates to results
**When:** Results page loads
**Then:** No "Explain back what you understood" row, no "Add a story" row, no response-related UI anywhere on the page; the page renders normally otherwise
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (off mode results test)

---

## UAT-7: Anonymous/public reader — zero response CTAs (AC #6)

### UAT-7.1: Anonymous token reader sees no response affordances in reading flow
**Given:** Reader opening the letter via invitation token (not authenticated)
**When:** Reader advances through all reveal phases
**Then:** No "Explain back", no "Add a story", no capture affordance appears at any phase — the letter reads identically to pre-P904 for anonymous users
**Verify:** E2E — `e2e/p952-responses-mode.spec.ts` (anonymous reader test)

---

## UAT-8: Visual QA at 375px, 320px, desktop (AC #7)

### UAT-8.1: Two-CTA bar holds at 320px (narrowest mobile)
**Given:** Receiver at story-revealed on a 320px viewport
**When:** Two CTAs are rendered in the FixedBottomBar
**Then:** Both buttons are fully visible, not clipped; skip/secondary button clears the safe-area inset; neither button's text wraps onto a third line that pushes it offscreen; Dialog `max-w-sm` fits within 320px without horizontal scroll
**Verify:** Screenshot at 320px; `e2e/a11y/p952-accessibility.spec.ts` (touch target test)

### UAT-8.2: One primary per story at 375px and desktop
**Given:** Receiver reading on 375px and desktop viewports
**When:** story-revealed renders
**Then:** Exactly one blue primary pill visible; the secondary/ghost has clearly different visual weight — no two competing blue pills, no orphaned heavy items
**Verify:** Screenshots at 375px and desktop; visual QA checklist applied
