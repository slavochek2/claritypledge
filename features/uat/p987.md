# UAT: P987 — CP Front-Door Realignment

> Auto-generated by /verify — edit freely.

**Re-run 2026-07-15.** The previous log was written against P987's *first* iteration and never updated across the branch's commits. It had gone actively misleading: it recorded **passes for things that are false** (UAT-5 claimed the pledge CTA + avatar stack were *removed* from `/`; both are deliberately present) and a **fail for something since fixed** (UAT-11). `/ship`'s per-AC gate reads this file, so a stale "pass" here is worse than no evidence at all — it would wave through a page that contradicts its own evidence log. Every row below was re-derived from the current spec's Done-When and re-verified. Rows whose *requirement* changed are restated, not silently re-ticked.

## Test Execution Log

| Scenario | Result | Notes |
|----------|--------|-------|
| UAT-1: `/` hero shows key-hire pain + single primary free-audit CTA; no coach/price/co-founder content | pass | Playwright `p987-verify.spec.ts` (15 passed): h1 "Keep the hire you can't / afford to lose."; CTA "Book your free alignment audit" → `/intro`; no "I've lost co-founders", no "€950" on `/`. **Requirement restated:** the original row said "no pledge content" — that is no longer the AC (see UAT-5). |
| UAT-2: The **gate** is disclosed before booking — the click books a call, not the audit | pass | **Requirement narrowed 2026-07-15 (founder); the original is retired, not failed.** The row formerly read "audit disclosed as a live 1:1 session". Sequence: an intermediate pass deleted `AUDIT_MICROCOPY` entirely, leaving the promise only in the SEO meta tag — no visitor reads that, so the disclosure was gone while this row still read "pass". Restored, then the founder cut it to the gate alone: "audit" already carries gap-finding and a "call" is self-evidently live and 1:1, so the format clause restated the CTA. Now: `AUDIT_MICROCOPY` = "Starts with a 15-min call." under BOTH CTAs. Pinned by an exact-match assertion; failure path exercised (delete the line → `Expected: 2, Received: 0`). **Residual risk accepted, not mitigated:** a reader may still imagine the audit as a report delivered after the call — see the spec's Risks → "Async-expectation mismatch" (ACCEPT + falsifier). |
| UAT-3: The co-founder homepage is reachable at `/founder` (prod), and the old dev route is gone | pass | **Route changed:** was `/tree/old-landing-2` (dev-gated), now `/founder` (prod). Playwright asserts the `/founder` h1 **scoped to `main`**, plus a negative check that `/tree/old-landing-2` renders no h1 and no "I've lost co-founders" in `main`. **Scoping is load-bearing:** the previous unscoped assertion passed on the *deleted* route by matching the nav link "For co-founders" — a green test guarding nothing. Verified by reproducing the false pass, then confirming the scoped version fails on absence. |
| UAT-4: `/about → Work with Slava` still reachable | pass | Playwright: link visible on `/about`. Unchanged this session. |
| UAT-5: "Take the Pledge" secondary CTA + pledger avatar stack **present** under the primary CTA | pass | **Requirement inverted — the old row was false.** Spec Done-When requires both PRESENT: a subordinate `Link to="/sign-pledge"` text link (not a button) + `<PledgerAvatarStack />`. P955's one-full-width-primary rule holds because the pledge link is inline text. Playwright asserts the link exists with href `/sign-pledge`; the avatar stack is asserted as one of its two legitimate DOM shapes (loaded link vs height-reserving placeholder) rather than guessing which state the async profile fetch lands in. Tap target measured 40px. |
| UAT-6: Stakes section = one stat beat closed by the bridge; the replacement cost is claimed ONCE, in the hero | pass | **Requirement changed twice.** Originally a single beat worded "nearly half"/"attitude, not skill". Then two beats: 46% → "Small gaps compound." → 200% (Gallup). Now: `46%` + "of new hires fail within 18 months — 9 out of 10 of them because of attitude, not a lack of technical skills." (Leadership IQ, ref 2) → "Small understanding gaps compound." — the bridge now names the gap as one of UNDERSTANDING, which is the only place the alignment thesis may enter (the stat itself must stay in the source's own words, per the logged "thesis smuggled into a stat" incident). The 200% beat has moved to the hero sub — stating the replacement cost in both places, one screen apart with the scroll cue pointing at the restatement, made the section an echo. Playwright scrolls `#stakes` in and asserts the **text**, never the digits (they count up 0→N); it also asserts the 200% line is **absent** here, so a re-add fails loudly rather than silently duplicating the hero. |
| UAT-7: `HardTruthChat` on `/` = key-hire scenario; `/coach` chat unaffected | pass | `contact="Katie"`, `subtitle="your new Head of Sales"`, `variant="they-withheld"`. `/coach` is unaffected **by construction, not by care**: it defines its own local `HardTruthChat` and never imports the shared component (verified by grep of all importers — the shared component's only consumers are `program-page.tsx` and `old-landing-2.tsx`). The "props only, protect /coach" constraint that shaped the original Done-When rested on an unverified premise. |
| UAT-8: Closing copy | pass | **String changed twice:** "Stop before they quit." → "Stop before they give up on you." → (2026-07-15, founder) "Make misalignment easy to reveal and safe to bridge.", swapped up from the hero sub. Current: "Your new hire nods." / "And maybe holds back." / "Make misalignment easy to reveal and safe to bridge." Playwright asserts the first two via non-exact match (they share one `<h2>` joined by `<br>`, so an exact match on either alone fails) and the mission line exactly. See the spec Done-When for the recorded dissent + falsifier — the closing no longer carries an imperative. |
| UAT-9: `?referrer`/`?login` redirects still fire on `/` | pass | Playwright: `?login=1` → `/login`, `?referrer=x` → `/sign-pledge`. `landing_page_viewed` call site unchanged; the event itself is a documented prod-only no-op in dev (`src/lib/mixpanel.ts`), so its firing is **not** verified here. |
| UAT-10: `/program` out of nav/footer + `noindex`; route reachable; price kept | pass | Playwright: 0 "Co-founder Program" nav links; `/program` shows €950 + `noindex` meta. This is the **parked** state and is intended — see `docs/decisions.md` 2026-07-15 "`/program` is parked". A code review flagged it as a suspected accident; it is not. This row is the executable record and should fail loudly if either half is "fixed" alone. |
| UAT-11: `/`'s FAQ uses key-hire copy, not co-founder copy | pass | **Was `fail`, now fixed.** `KEY_HIRE_FAQS` (5 Qs) is wired into `/`'s FAQ section; `PROGRAM_FAQS` untouched and still serves `/program` and `/founder`. |
| UAT-12 (new): `/founder` hero fits one fold; scroll cue above the fold | pass | Regression found this session: the nav-padding opt-out was hardcoded to `pathname === "/"`, so `/founder` got `<main>`'s 80px nav padding stacked on a hero that already carries its own offset and sizes to `lg:min-h-screen` — overflowing the fold by exactly the nav height. Measured at 1440×900: cue bottom **908 → 828**, matching `/` exactly. No nav overlap at 1440 or at a confirmed 320px viewport; `/coach` unaffected (content-height hero). |
| Visual-QA pass (375 / 320 / desktop, per `.claude/rules/visual-qa.md`) | pass | **Cleared 2026-07-15** by a blind subagent (screenshots + checklist only). No CRITICAL survived verification; it found three real pre-existing prod bugs desktop-width checking had hidden (`/intro` unbookable on phones, TEMPLATE watermark invisible <520px, pledger badge wrong + unreadable) — all fixed with regression tests. See the spec Done-When for the full finding list, what was refuted, and the capture-method note. **Caveat:** the hero sub / closing copy swap landed AFTER this pass. Text-only, inside already-QA'd components, and re-verified at 320 (no overflow). **Known gap, pre-existing:** the reference `<sup>` tap target measures 21×16, under the checklist's 40px — unchanged by the swap, but the swap puts one in the hero for the first time. |

## Evidence

- `npx playwright test e2e/p987-verify.spec.ts e2e/landing-no-horizontal-scroll.spec.ts --project=chromium` → **15 passed**, exit 0.
- Unit suite: 2674 passed / 19 skipped. Typecheck: 0 errors. Lint: clean.
- Both e2e specs were themselves stale before this re-run (written against iteration 1, not in CI, so nothing caught them) and were rewritten against verified current copy.
