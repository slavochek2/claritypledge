# Fixture skill file (P1210 DW-22)

The wired predicate is invoked here, the way a real stage invokes one:

    node scripts/points/wired-predicate.mjs <input.json>

The orphan predicate is deliberately absent from this file — that is the must-fail case.
