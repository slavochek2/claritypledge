// Fixture test file (P1210 DW-22). Both modules are imported here; only one is
// invoked by the fixture skill file, which is what separates the two controls.
import '../../../../scripts/points/wired-predicate.mjs'
import 'points/orphan-predicate.mjs'
