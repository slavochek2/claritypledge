# Must-flag: markers BALANCE but are misordered

An `end` before a `start` passes a count check and leaves the region open to EOF —
the exact fail-open the counting repair claimed to have closed.

<!-- store-naming:end -->
<!-- store-naming:start -->

```bash
ls "$DIARIZE_STORE"/<video-id>/
```

~/.local/share/diarize-store
